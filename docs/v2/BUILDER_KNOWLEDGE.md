# TMDB ID Lookup v2 — Builder Knowledge Base

Status: Active isolated builder and contract groundwork

Last reviewed: 2026-09-03

This is a living record of confirmed v2/Nuvio findings, current decisions, unsupported behaviour, and open questions. GitHub issues remain the source of truth for implementation scope. See [`BUILDER_PRODUCT_PLAN.md`](./BUILDER_PRODUCT_PLAN.md) for durable product direction, [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md) for the shared hierarchy-family standard, and [`PROJECT_WORKFLOW.md`](./PROJECT_WORKFLOW.md) for the Dave/ChatGPT/Codex process.

## 2026-09-03 — Selectable-choice presentation contract (#178; local owner-review implementation)

- Issue [#178](https://github.com/davecollections/tmdb-id-lookup/issues/178) codifies one corrected cross-Builder selection language. Immediate launchers remain plain actions. Full-card single choices keep visually hidden native radios; multi-select entity cards keep visually hidden native checkboxes; both use the complete card's teal/cyan surface, border, structural inset, and focus ring without a visible dot, circular substitute, or tick. Poster/Landscape choices also strengthen the selected schematic. Compact pills, segments, tabs, and retained-choice buttons use selected styling and complete pressed/selected semantics without plus-to-tick markers. Independent Decade overview, Individual years, and Genre breakdown booleans retain conventional visible checkboxes and neutral row treatment.
- Workspace hierarchy selection, temporary Movie-franchise results, and inherited Streaming Add Source Media choices no longer use a leading selection rail. Rails remain valid only for semantic warnings, errors, and explanatory notices. Green remains success rather than a second selection colour; selected choices retain a neutral structural edge for grayscale and explicit forced-colours behavior. Missing `aria-pressed` state is now present on Decade presets and Genre exclusion buttons.
- Shared `choice-card-input` CSS owns contained hidden-control positioning and full-label focus. Obsolete marker rendering and CSS have been removed. A test-owned eight-family choice-presentation registry is part of `scripts/check-all.mjs` and guards launcher semantics, native input ownership, independent booleans, marker absence, structural selected treatment, forced-colours behavior, and rail/green regressions for current and future families.
- This owner correction supersedes the earlier #172 audit wording that described dots/ticks as optional. The authoritative current rule is [`BUILDER_UI_SHELL.md`](./BUILDER_UI_SHELL.md#selectable-choice-presentation-contract), cross-referenced by [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md#5-selectable-card-accessibility) and the repository instructions. The work is local, uncommitted, unpushed, unmerged, and unpublished at this checkpoint; it changes presentation and accessibility only, not selection behavior, project data, Nuvio output, the Worker, V1, or dependencies.

## 2026-09-03 — People Sort parity and cross-family capability contract (#176)

- Issue [#176](https://github.com/davecollections/tmdb-id-lookup/issues/176) closes the one confirmed cross-context source-capability omission: selected-folder People Add now renders the same shared Popular, Recent, and Top-rated semantic Sort choices as People hierarchy creation. Popular remains the default; Recent retains its media-correct Movie/Series mappings; and no speculative Most votes value is added. The selected option stays in the existing `sortOptionId` state and therefore flows through exact canonical drafts, Preview, duplicate evaluation, and one atomic Save without changing People duplicate identity.
- The implementation adds a test-owned capability matrix for Movie franchise, TMDB Lists, People, Studios, Networks, Streaming, Genres, and Decade across selected-folder Add, guided New Collection/New Folder, and physical Source Edit. Each of Media, Sort, filters/Advanced, role/credit type, Preview, Source name, and safe physical identity is classified as configurable, fixed, unsupported, or not applicable. Every configurable cell must retain semantic UI evidence, and all three existing registries must continue covering exactly the eight contracted families. This is a regression contract rather than a new production registry or architecture.
- Network already had Add Sort and Preview and remains unchanged. No other family behavior, source mapping, identity rule, schema/model, importer, serializer/export, Worker, V1, dependency, deployment, or production publication changes. Source-name parity remains an explicit open product decision and is only recorded, not resolved.

## 2026-09-02 — Eight-family completion and live reversible titles (#170, #172)

- TMDB Lists completed integration through issue [#170](https://github.com/davecollections/tmdb-id-lookup/issues/170) / merged [PR #171](https://github.com/davecollections/tmdb-id-lookup/pull/171) at `3e1ceace849d137e17ddfedb8637bc147f511285`. The issue is closed, Pages publication passed, the reviewed narrow Worker was manually deployed by Dave, and the live production Worker/TMDB/image-CDN, current-Nuvio Movie-only/Series-only/mixed List, immediate-export, and owner-review gates passed. The canonical persisted recipe remains `LIST` / `MOVIE` / `original` / empty filters. Preview renders the complete normalized page-1 sample only; an up-to-100-title shared Preview remains parked future work.
- Issue [#172](https://github.com/davecollections/tmdb-id-lookup/issues/172) / merged [PR #173](https://github.com/davecollections/tmdb-id-lookup/pull/173) at `459be092738b2a70884347fd87a782d099986512` makes title visibility live and reversible in People, Studios, Networks, Franchises, Genres, Decades, Streaming Services, TMDB Lists, and Node Editor. Exact-merge CI and Pages passed. `reversibleTitleFieldProps` and `transitionReversibleTitleDraft` in `builder/src/nuvio/titles.js` own the shared field/transition semantics; `HiddenTitleFieldHelp` owns the four quiet locked-state messages, and Node Editor mechanically reuses the transition helper.
- The proved sequence `Original → Custom → hide → show → Custom → Custom 2 → hide → show → Custom 2` retains the latest valid visible draft. Multiple generated titles restore independently by stable logical key. Only Folder Hide everywhere blanks/disables Title; Hide on home screen only retains a visible editable title. Planning labels remain visible even when final persisted titles are hidden. Hidden Collection and Folder values retain the established U+200E/`hideTitle` mappings, no remembered draft state is exported, no schema or source identity/name changes, and creation remains one atomic controller operation.
- The post-#172 screenshot audit identified selection consistency as follow-up work. Issue #178 later resolves its provisional optional-marker wording with the authoritative marker-free contract above. Native semantics, programmatic state, non-hue visual distinction, card-level focus, and the no-selection-rail rule remain required. Teal/cyan means selected/active/current, green means meaningful success, amber/orange needs review, red means error/destructive, and muted grey means disabled/unavailable. Routine valid rows need no ready badge; full rows use **Remove**, source Preview actions use **Preview titles**, edit footers place the primary action before Cancel, and creation/edit backdrops remain non-discarding. View-only Preview backdrop behavior remains undecided.
- The follow-up implementation inspection confirmed Network Add Source already exposed Sort and **Preview titles** and identified People Add Source Sort as the only source-capability omission. Issue #176 closes that gap and adds the enforceable test-owned matrix described above. Context may change container settings but must not silently reduce source-level capability. Source-name parity remains open.
- The dense three-column workspace, selection-first hierarchy rows, explicit editing, useful counts, Nuvio terminology including **Focus GIF**, and humanized user-facing Series/sort/source wording remain the approved direction. No substantial desktop spacing redesign is approved. **Global display settings** remains the current one-shot bulk operation; **Bulk display settings** is only the preferred future term. Single-node booleans versus bulk tri-state controls, context-specific Show All behavior, and Focus GIF enablement independent of URL presence remain intentional current boundaries. Origin-country/original-language filters, TMDB Recommendations runtime/schema behavior, and shared up-to-100-title Preview remain parked investigations rather than commitments.

## 2026-08-30 — Native TMDB Lists owner-accepted implementation (#170)

- TMDB Lists is the eighth selected-folder Add Source family and the eighth guided New Collection/New Folder family. It accepts uncapped ordered public List IDs/URLs, resolves each exact signed-int32 identity through one injected provider, keeps failed lines editable, and generates native `LIST` sources only in `sources`. The canonical saved representation is `LIST` / `MOVIE` / `original` / empty filters even for a mixed remote List. Current Nuvio runtime testing confirmed that Movie-only, Series-only, and mixed Lists retain this source-level identity while each item routes by TMDB `media_type`; composition inference would be unstable for mutable Lists.
- The family reuses the creation registry, modal shell, fixed footer, shared nested poster Preview, source-editor adapter registry, existing duplicate/elsewhere presentation, and existing atomic controller operations. Guided New Collection creates exactly one Collection and one user-named Folder; New Folder creates exactly one user-named Folder. Add Source may explicitly override same-Folder duplicates; New Folder omits identities already in the destination Collection and blocks an empty Folder. Source Edit owns title only and fails closed for noncanonical List shapes.
- The tracked Worker admits only `/3/list/<canonical int32>?language=en-US&page=1` with both parameters unique and exact. Service-token access remains excluded. At this checkpoint, the owner-deployed bytes had passed the bounded real Worker/TMDB/image-CDN gate, desktop and physical-phone review, and current Nuvio runtime plus immediate-export acceptance. The later final integration is recorded above. See [issue #170](https://github.com/davecollections/tmdb-id-lookup/issues/170) and [`BUILDER_TMDB_LISTS.md`](./BUILDER_TMDB_LISTS.md).

## 2026-08-29 — Add Source chooser alignment owner-review implementation (#164)

- At the #164 checkpoint, the selected-folder Add Source chooser contained Movie franchise, People, Studios, Networks, Streaming, Genres, and singular Decade in that order. Issue #170 later adds TMDB lists after Movie franchise without changing the #164 behavior of the existing cards.
- Add Source and the New Collection/New Folder launcher now share one narrow launcher-card/icon rendering seam plus the same responsive grid, spacing, typography, border/surface, hover, and explicit focus-visible CSS. Builder launcher cards use an icon, family name, and concise context-appropriate helper: Add Source explains the immediate source action, while Creation explains the guided structure. The Creation registry remains the single source for its helpers, and option order, scope behavior, and rendered class contract remain unchanged; no wizard or universal creation-mode framework was introduced.
- Add Source retains its navigational semantics: no Blank option, retained selection, radio/check control, selected accent rail, or hidden selection state. Seven concise family helpers replace the long arrow-list descriptions, and one compact heading disclosure preserves the common TMDB provider without repeating it on every card.
- Mounted Builder coverage passes at 360, 384, 393, 402, 412, 899, 900, 901, and 1280 pixels plus 393×320. It proves two comfortable columns at the tested phone widths and four at the tested wider widths, readable contained helpers, 42px shared icon shells, at least 76px card targets, balanced rows, no horizontal overflow, one intentional scroll owner, reachable final card, initial first-card focus, trusted keyboard activation/focus visibility, immediate navigation, exact Back focus restoration, unchanged Close/Escape restoration, body lock, and zero project mutation. Owner desktop and physical-phone visual review is complete and approved; no push, PR, merge, deployment, or production publication is implied.

## 2026-08-28 — Streaming Services guided hierarchy owner-review implementation (#162)

- Streaming Services is the seventh guided New Collection/New Folder family, ordered after Genres, while selected-folder Add Source and physical Source Edit remain separate. Choose owns ordered Regions, one global Movies/Series/Both requirement, and uncapped ordered providers. Provider eligibility intersects the chosen media across every selected Region. When Region or media changes, ordered selected providers are retained whenever they remain eligible and only newly ineligible providers are pruned. Search remains deliberate and browse-first with no input auto-focus.
- Configure owns one global Streaming Sort and, only for multiple Regions, Group regions by service (default) or Separate folders by region. Grouped plans create `P` provider folders; separate plans create `P × R` provider/Region folders; both create exactly `P × R × M` canonical simple native Streaming DISCOVER sources in provider, selected-Region, Movie-before-TV order. One explicit context-aware helper emits standalone `Netflix Movies (AU)`, grouped `Netflix` plus `Movies (AU)`, or separate `Netflix (AU)` plus `Movies` defaults without rewriting persisted/custom titles.
- Review uses shared presentation controls. Provider logos remain transient UI identity, new unknown artwork fields stay unassigned, and reused existing artwork/focus fields remain exact. No temporary artwork warning is shown. Explicit Preview materializes one exact provider/Region/media/Sort source, requests first Region/first media only on open, switches Region/media lazily, reuses the strict deployed Streaming requester and bounded successful cache, shows at most ten poster-only real results, restores trigger focus, and never mutates the project.
- The owner-review refinement reuses ordinary `inspectStreamingSourceDuplicates` for New Collection project-wide exact matches. Exact evidence identifies possible existing Collections, but every candidate is qualified and fully replanned through the same existing-Collection Streaming planner: relevant trusted candidates remain, ambiguous/mixed/opaque candidates fail closed, and ordinary Sort conflicts stay visible and blocking. Safe candidates rank by distinct proposed exact identities already present, then project order, with no automatic selection. Raw elsewhere evidence remains available even when no safe destination can be offered.
- Review offers explicit existing-Collection choices plus **Create new collection instead**. Equal visible Collection titles receive UI-only project-order `· Collection N` labels everywhere the destination appears; unique titles stay clean and internal IDs remain authoritative. Cards use exact already-here/will-be-added or nothing-to-add copy and reliable Folder/Source context for duplicate titles. Only the selected route's full Review is active: existing uses **What will change**, complete existing uses non-mutating **Nothing to add** / **Close** with **No project changes are needed**, and New uses **What will be created** with the established complete-overlap duplicate confirmation. The selected plan alone applies atomically; a candidate that disappears or becomes unsafe clears the choice, and the ordinary final rebuild stale-rejects changed deltas before mutation.
- New folders alone expose an initially collapsed stable-key Folder-name editor in Review. Ordinary visible Folder-title validation is authoritative; route switching preserves dormant Collection-title and logical Folder-title drafts, surviving logical keys retain drafts across Back, removed keys are discarded, and custom grouped or provider/Region display titles do not change canonical source identities or deterministic source names. Existing reused imported/custom folder titles remain read-only and byte/semantically preserved.
- Placement never trusts Folder title. Grouped folders require consistent canonical simple Streaming sources for one provider; separate folders also require one Region. Trusted partial folders receive only missing identities. Exact destination sources are complete, elsewhere sources are informational, same provider/Region/media with another Sort blocks, and mixed/opaque/unsupported evidence fails closed. Plans are ephemeral and rebuilt before one atomic Apply; no relocation, rename, deletion, or global deduplication occurs.
- The focused controller audit verified that the six existing guided families create new collections through `createCollectionsWithFoldersAndSources`, create new folders through `createFoldersWithSources`, and use `addSourcesToFolder` only for one existing folder. None could combine zero or more multi-folder source appends with zero or more new folders in one existing Collection commit. The narrow `extendCollectionWithFoldersAndSources(collectionInternalId, { existingFolderAdditions, newFolders })` operation validates all internal-ID targets and bundles, constructs one detached candidate, checks uniqueness, and commits once; an empty batch is a zero-revision no-op. It exposes no callback/generic transaction, delete, move, rename, presentation, or cross-Collection authority.
- Controller tests cover create-only, one/multiple existing additions, mixed and larger mixed batches, invalid late work, cross-Collection and duplicate targets, true no-op, one successful notification/revision, imported overlay/opaque/order preservation, ID collision, and late factory rollback. Streaming planner tests cover grouped/separate counts/order, the exact existing Apple TV AU plus AU/US Apple TV/Dekkoo six-source delta, multiple existing destinations, no/partial/complete New Collection overlap, different-Sort exclusion, custom-title equality, stable-key new-folder names, existing-title preservation, title non-authority, ambiguous/opaque evidence, stale rejection, and New Collection reuse.
- Mounted production-path checks cover 360, 384, 393, 402, 412, 899, 900, 901, and 1280 pixels with the live Apple TV Store/Dekkoo New Collection flow, two overlap-ranked candidates and no auto-choice, New/existing title-state restoration, the selected candidate's honest delta, invalid/custom new-folder naming, Back preservation, one untouched candidate, zero mutation before Apply, and one atomic existing-Collection revision without a new Collection. At 393px and 900px they use the production Worker, real TMDB provider catalogue and Discover responses, and real `image.tmdb.org` posters for exact AU/US Movie/TV Apple TV Store queries. Responsive checks also prove selected Region border/surface/tick without the redundant cyan rail. A deterministic mounted complete-overlap flow proves zero-change Close and the separately confirmed duplicate-New route. Deterministic checks cover 20/50/100 provider scale. Owner desktop and physical-phone review is complete and approved. No Worker, schema/model, importer, serializer/export, V1, dependency, deployment, production publication, push, PR, merge, or Nuvio-client result is implied.

## 2026-08-27 — Ordinary Decade Add Source implementation (#160)

- The selected-folder chooser adds singular **Decade** seventh, after Genres. The owner-corrected compact editor orders Media, Sort, one Decade radio choice, its exclusive whole period or multiple individual Year checkbox pills, optional additive text-only Genre checkbox pills, Advanced, generated review, exact Preview, and ordinary Cancel/Save. The former 89-option Period select, single-Year restriction, visible Genre checkbox circles, and redundant configuration heading are absent. Changing Decade resets to All; individual periods remain canonical regardless of click order; complete 2020–2029 and Before 1950 plus 1950–1959 stay available.
- `buildCanonicalDecadePeriodDrafts` remains the shared physical one-period construction seam. `buildDecadeSourceBundleDrafts` composes selected canonical periods × one mandatory general plus catalogue-ordered Genre logical sources × Movie then Series, ordered by those same dimensions. Guided `buildDecadesSourceDrafts` retains byte-equivalent delegation and Decade Source Edit retains canonical reconstruction plus stored/custom title. No hierarchy bulk output is filtered into Add Source.
- Genre eligibility uses existing official Movie/TV applicability and the strict Both intersection. Select all has no product cap; Clear returns to the general source only; a media change prunes only incompatible choices. Each physical source still has zero or one included Genre. Advanced global filters apply everywhere while ordinary exclusions belong to the general source and each Genre owns its Guided-compatible exclusion selection; self-exclusion remains invalid.
- Full title-free Discover identity drives current-folder, elsewhere, mixed partial-bundle, all-duplicate, and stale-override review. Normal Save rechecks and adds only missing candidates; explicit exact-set override remains available; every accepted bundle calls the existing atomic `addSourcesToFolder` once and a failed large insertion leaves zero partial mutation.
- Ordinary Preview materializes only one exact would-be saved physical source—never Decade sample or Period sample. Optional Year, Source (`General` then selected Genres), and Movies/Series selectors keep the Cartesian bundle structured. Opening requests only the first period's general initial media; each dimension change requests only the newly active exact combination, unvisited combinations remain deferred, and successful revisits reuse the existing bounded cache. Retry/fail-closed/focus/inert behavior comes from shared Preview infrastructure, and up to ten real posters retain the five-column mobile presentation. Renewed mounted production acceptance passed at 393px and 900px through the production Worker, real TMDB, and image CDN before final owner desktop/physical-phone approval.
- The change adds no Worker route or deployment and no schema, controller, model, import, export, serializer, Guided product, artwork, Streaming hierarchy, generic Discover, dependency, V1, push, PR, or production publication. Final owner desktop/physical-phone review is approved; repository publication remains separately gated.

## 2026-08-27 — Preview Titles consistency implementation (#158)

- Decades Configure derives a default-closed lazy Preview catalogue from the exact `buildDecadesSourceDrafts` output. Opening it mounts one compact Genre-standard action row per configured Decade and reports the logical overview/year/Genre source count; the Preview-only representative sample is not counted.
- The nested Decades dialog defaults to **Decade sample** or **Period sample**, then lists the exact configured sources in logical order on one non-wrapping horizontal selector. Both-media choices remain Movie-first and lazy-Series. Exact whole-Decade, year, and Genre choices retain their physical draft semantics.
- A representative sample uses current sort and approved Advanced filters over canonical year buckets, keeps the first poster-bearing response per bucket in chronological order, and is capped at ten requests per explicitly selected media. Current-Decade buckets stop automatically at the injected current year, while the complete configured selector retains future individual years for explicit lazy exact-source Preview; 1950s & Earlier evenly selects at most ten buckets from Before 1950 plus 1950–1959. Every viewport presents up to ten representatives. Matching exact-year choices reuse the shared query cache.
- Source Edit Preview is a detached projection of the current validated editor draft across the seven existing adapters. Its final-scroll-area action uses the same compact treatment as Genre Configure, overlays only the adapter's minimal patch on the exact opening source, and never enters Save, duplicate, controller, or serialization paths.
- Shared Preview behavior remains poster-only, one nested body portal, and up to ten visible posters at every viewport. Phones use five columns and up to two rows without captions or horizontal grid scrolling. Preview surfaces stay centred in the visual viewport when they fit; bounded overflow uses one intentional Preview scroll owner without changing poster size, underlying body lock, focus containment, or exact trigger restoration. Explicit loading/empty/error/Retry, abort/stale guards, and bounded successful-response caching remain unchanged.
- The Worker adds only strict date-only Decade and simple single-provider Streaming Discover branches. Both require exactly one `include_adult=false` and one media-correct sort; arbitrary Discover, compound identities, extra filters, wrong-media fields, duplicate keys, and service-token authorization remain rejected.
- The representative correction changes no Worker source or production route: it composes only the already-approved canonical queries through the existing requester/cache. Dave confirmed deployment of the complete 14,718-byte reviewed Worker source with SHA-256 `94CD976AA642A732D969D535D5F09E09D9B1033DC51B02756FCAF3BB28436E91` on 2026-08-27; no deployment version identifier was supplied. Four canonical Decade/Streaming production probes returned live TMDB results and six missing, noncanonical, mixed, lowercase, compound, or extra-parameter probes returned HTTP 403. Deterministic domain/provider/UI coverage and gated mounted production acceptance cover older/current Decades, Both-media laziness, sample/exact distinction, bounded responsive posters, shared-cache reuse, focus restoration, and zero mutation at 393px and 900px. No commit, stage, push, PR, Worker change/deployment, or production Builder publication is implied.

## 1. Product direction

- v1 is working and stable.
- v2 changes the product from primarily an ID lookup/export utility into the visual **Dingo's Collection Builder**, built for Nuvio collections and powered primarily by TMDB.
- The active React/Vite builder remains isolated under `/builder/`, unlinked, and `noindex, nofollow`; it is not a released replacement for v1.
- **Merged through issue #104 / PR #105 from the requirement recorded by issue [#85](https://github.com/davecollections/tmdb-id-lookup/issues/85):** the welcome footer's text **About** control and the Builder workspace header's single global **?** open a compact **About & Credits** modal; the workspace control replaces the former separate V1 backlink. One shared borderless credits group uses the same approved local TMDB square as V1 beside the required notice **“This product uses the TMDB API but is not endorsed or certified by TMDB.”**, plus an official local gold JustWatch mark beside centralized watch-provider attribution. Its divided footer credits and links davecollections, opens the **TMDB ID Lookup Tool**, links to the GitHub issue chooser, and ends with one muted text-only Nuvio independence note.
- Lookup and copy-ID tools remain available.
- v2 should be mobile-first, modern, and sleek.
- No login is required for the complete core build-and-export journey. Any future Nuvio connection must remain optional.
- Playback is outside project scope and should normally remain unmentioned.
- Startup routes, Dave’s 1-Click Setup, templates/recipes, the Kaptain onboarding comparison, product privacy, branding, and the optional Nuvio connection are maintained in the product plan rather than duplicated here. Exact template contents, final naming, and the connection product contract remain open.

## 2. Evidence levels

Future findings must use one of these labels:

- **Manually confirmed in Nuvio:** observed in an actual Nuvio client using a recorded input.
- **Confirmed from current Nuvio source code:** directly supported by the reviewed, pinned Nuvio source revision.
- **Confirmed by current official Nuvio documentation:** explicitly documented by Nuvio on the cited page and review date; this does not prove undocumented client behaviour.
- **Confirmed by repository tests:** behaviour or an invariant demonstrated by deterministic repository tests and successful CI.
- **Confirmed by live deployment:** behaviour directly verified on the deployed production site through recorded HTTP or browser checks.
- **Strongly inferred:** consistent with available code or behaviour but not directly verified end to end.
- **Experimental:** a candidate shape or behaviour that still requires controlled testing.
- **Currently unsupported:** absent from, rejected by, or not reliably implemented in the published model currently under review. This does not mean impossible forever.

## 3. Confirmed Nuvio source types

**Confirmed from current Nuvio source code:** the published native TMDB source model exposes:

- `LIST`
- `COLLECTION`
- `COMPANY`
- `NETWORK`
- `DISCOVER`
- `PERSON`
- `DIRECTOR`

**Currently unsupported:** the current published source models do not expose native direct-movie, direct-series, season, or custom-item source types. Describe these as “currently unsupported by the published source model,” not “impossible forever.”

## 4. Native TMDB Discover

**Manually confirmed in Nuvio — Shark Movies experiment:** a folder using the following source loaded dynamic shark-movie results without the Nuvio Catalog Addon:

- provider: `tmdb`
- `tmdbSourceType`: `DISCOVER`
- `mediaType`: `MOVIE`
- `sortBy`: `popularity.desc`
- `filters.withKeywords`: `15097`
- TMDB keyword `15097`: `shark`

**Confirmed from current Nuvio source code — issue [#106](https://github.com/davecollections/tmdb-id-lookup/issues/106), reviewed 2026-08-12:** the authoritative persisted Discover filter model includes exactly:

- `withGenres`
- `withoutGenres`
- `releaseDateGte`
- `releaseDateLte`
- `voteAverageGte`
- `voteAverageLte`
- `voteCountGte`
- `withOriginalLanguage`
- `withOriginCountry`
- `withKeywords`
- `withoutKeywords`
- `withCompanies`
- `withoutCompanies`
- `withNetworks`
- `year`
- `watchRegion`
- `withWatchProviders`
- `withoutWatchProviders`

The four additions are nullable-string fields for Movie and TV: `withoutGenres`, `withoutKeywords`, `withoutCompanies`, and `withoutWatchProviders`. Current NuvioTV `f6e504868765aa6e5657c383723bdd2a9e6ebf7b` and NuvioMobile `f9ad843b14cd3be7fa3ddb800b6961233e0e5b56` persist and forward them. Exclusion expression comparison remains exact/order-sensitive until stronger semantic evidence exists. Included or excluded provider filtering activates the same effective Watch Region rule: explicit nonblank region wins, otherwise comparison uses `US`; the inferred default is never persisted.

**Historical issue #47 source evidence, reviewed 2026-07-23:** the pinned client snapshots then declared the earlier 14-field contract. Thirteen mapped to official Movie parameters and all fourteen to official TV parameters; Movie `withNetworks` was omitted by NuvioTV and sent as an undocumented query by NuvioMobile. Five fields were media-mapped or conditional, and included-provider filtering injected a non-selectable all-monetization union. See [`TMDB_DISCOVER_COMPATIBILITY.md`](./TMDB_DISCOVER_COMPATIBILITY.md) for that dated official inventory, pinned evidence, preservation findings, and manual-test boundary.

**Historical owner-supplied Windows visual evidence, reviewed 2026-07-23 but not version-pinned:** screenshots of the then-current Nuvio Windows TMDB Sources > Custom editor show exactly the earlier 14 fields and no additional Windows-only Discover field. Movies/Series/Both is a media/source control, the four visible sorts are Popular/Top Rated/Most Voted/Recent, and quick chips are convenience inputs rather than fields. Network IDs remains visible for Movies with the helper “For series only.” Genre and watch-provider help explicitly says comma=AND and pipe=OR. Language/country comma examples are placeholders only and do not establish official delimiter semantics. Because the exact app version was not captured, this evidence confirms that dated visible UI surface only.

## 5. Nuvio Catalog Addon

**Confirmed from the published manifest (reviewed 2026-07-10):**

- Name: `Nuvio Catalog Addon`
- Addon ID: `com.nuvio.tmdb.catalogs`
- Manifest: <https://catalog.nuvio.tv/manifest.json>
- Types: `movie`, `series`
- Resources: `catalog`, `meta`
- Meta prefixes: `tt`, `tmdb:movie:`, `tmdb:series:`

The 16 confirmed catalog IDs are:

| Movies | Series |
| --- | --- |
| `trending-movies` | `trending-series` |
| `popular-movies` | `popular-series` |
| `critically-acclaimed-movies` | `critically-acclaimed-series` |
| `new-and-upcoming-movies` | `new-and-returning-series` |
| `international-cinema` | `international-series` |
| `independent-and-festival-picks` | `limited-series` |
| `documentaries` | `documentary-series` |
| `family-movies` | `family-series` |

**Manually confirmed in Nuvio:**

- A generated Trending Movies source worked.
- Addon genre filtering worked.
- `popular-movies` with `genre: "Horror"` loaded horror results.
- Disabling the addon caused addon-backed folders to return no results.
- Native TMDB Discover remained conceptually separate.

The addon is a convenient optional source, not the basis of all TMDB builder functionality.

## 6. Important failed or inconclusive tests

- **Currently unsupported:** `tmdb:series:456` and `tmdb:series:615` failed when incorrectly placed in addon `catalogId`. These are meta identifiers, not catalog IDs. The result does not prove that direct media can never be represented through another future model.
- **Experimental and inconclusive:** guessed addon search persistence using `filters.search` was accepted structurally but did not produce convincing filtered results. Do not treat persisted addon search as supported.

## 7. Source-array behaviour

**Confirmed from current Nuvio source code:**

- `sources` is authoritative.
- `catalogSources` is an addon compatibility projection/fallback.
- Runtime does not execute both arrays independently.
- Native-only canonical folders use populated `sources` and empty `catalogSources`.
- Addon-backed sources may have matching projections in both arrays when compatibility output requires them.
- In a mixed folder, compatibility output mirrors only addon-backed entries into `catalogSources`.
- All active source data must remain represented in `sources`; never emit an empty `sources` array while placing active addon data only in `catalogSources`.
- Do not change existing v1 exports merely to enforce this future policy.
- Multiple sources resolve independently.
- The optional All presentation interleaves and deduplicates results rather than combining them into one TMDB query.

Source and folder order is meaningful and must be preserved.

## 8. Preservation contract

- **Confirmed from current Nuvio source code:** Mobile Nuvio preserves unknown fields more strongly than TV.
- The future builder must retain raw imported objects and overlay only edited known paths.
- Unknown collection, folder, source, filter, and community fields must survive unrelated edits.
- Imported opaque sources must remain preservable, movable, and removable without being guessed into a known provider type.
- Each source needs an internal stable identity independent of mutable filter values.

## 9. Contract fixture baseline

**Confirmed from current Nuvio source code and existing v1 exporters — issue [#31](https://github.com/davecollections/tmdb-id-lookup/issues/31), 2026-07-10:** the repository now has deterministic Nuvio contract fixtures and Node built-in tests that establish a pre-builder baseline.

The positive fixture set proves that:

- all seven currently confirmed native TMDB source types have representative source envelopes;
- the manually confirmed Shark Movies Discover source retains `DISCOVER`, movie media, `popularity.desc`, and keyword `15097`;
- addon-backed active data remains in `sources`, with an optional matching compatibility projection in `catalogSources`;
- a mixed folder projects only its addon-backed source into `catalogSources`;
- source and folder array order is retained by the validation path;
- import-preservation mode accepts an opaque community source and leaves unknown collection, folder, and source sentinel fields untouched.

The negative fixture set detects:

- addon data placed only in `catalogSources`;
- a native TMDB source incorrectly copied into `catalogSources`;
- a guessed direct-movie TMDB source type used as canonical builder output.

The fixture suite deliberately does **not** prove a complete or final Nuvio schema, client rendering parity, live TMDB/addon responses, every Discover filter combination, or production parsing/serialization behaviour. The opaque community fixture is a synthetic preservation probe based on confirmed raw-object overlay behaviour; its sentinel fields do not claim to define a real community provider schema.

### Manual Desktop and Mobile verification — 2026-07-10

**Runtime source behaviour — manually confirmed in Nuvio Desktop for Windows and Nuvio Mobile on iOS:** importing `mixed-native-and-addon.json` loaded the native TMDB Discover source first and the addon-backed Trending Series source second. The addon-backed source was not duplicated despite its matching `catalogSources` compatibility projection. Disabling the Nuvio Catalog Addon removed only the addon-backed results while native Discover remained available; re-enabling the addon restored the addon-backed results. Export retained native-first, addon-second source order.

**Import/export preservation behaviour — manually confirmed in Nuvio Desktop for Windows and Nuvio Mobile on iOS:** `opaque-community-import.json` imported successfully, and its deliberately fake addon produced the expected addon-not-found runtime message. Export preserved `communityMetadata`, `communityLayout`, `communityOptions`, `unknownBoolean`, `provider`, `addonId`, `type`, and `catalogId`. This proves that the unknown sentinel fields survived import/export round trips on both tested clients. It does not prove byte-for-byte preservation and does not make the fake community provider a supported schema.

**Client normalisation behaviour — manually confirmed in both tested clients:** export added a matching `catalogSources` projection, expanded the compact imported source into a fuller explicit-null envelope, and added default collection and folder presentation fields. Compact valid input can therefore be accepted and normalised by these clients, but client-normalised imported data is not automatically the canonical shape the future builder should emit.

`scripts\check.cmd` remains the Windows entry point. It and the dedicated GitHub Actions workflow both execute `node scripts/check-all.mjs`, which runs the frontend checks and the Nuvio contract, builder domain, importer, serializer, migration, and application-controller tests without secrets or network requests.

## 10. Current architecture decision

- Preserve v1 at the repository root.
- Keep builder source and dependencies isolated under `builder/`; no root npm package is needed.
- Use `builder/dist/` only as generated local/CI output and `.pages-site/` only as generated combined deployment staging. Neither directory is committed.
- Use Vite's relative `./` base so generated JavaScript, CSS, and imported local assets resolve from `/tmdb-id-lookup/builder/` without hard-coding the repository name or incorrectly targeting `/assets/` at the domain root.
- The Pages staging process selects only the explicit v1 public contract—`index.html`, `robots.txt`, `sitemap.xml`, and the `css/`, `js/`, and `data/` trees—then places the contents of `builder/dist/` at the dedicated public `builder/` path. This produces `.pages-site/index.html` and `.pages-site/builder/index.html` without a public `builder/dist/` level.
- Preparation and validation import the same slash-normalized public-path contract. Unknown, absolute, traversal, repository-only, manual-evidence, source, documentation, and test paths fail closed instead of being deployed by default.
- Branch and pull-request CI builds the builder, runs existing frontend and Nuvio contract checks, and validates the combined artifact without calling `actions/deploy-pages` or using the `github-pages` environment.
- The existing Pages workflow remains the only publisher and retains its `main` push, manual dispatch, successful maintenance `workflow_run` triggers, permissions, concurrency, asset-version injection, and deployment steps.
- During development, the builder is intentionally not linked from v1 and its entry page requests `noindex, nofollow` from search engines.
- The robots directive controls discoverability, not security: the deployed builder URL remains publicly reachable, and no client-side login or fake password gate is being added.
- Genuine restricted testing would require a separately protected staging environment, which is outside issue [#33](https://github.com/davecollections/tmdb-id-lookup/issues/33). A future release-readiness issue can remove the noindex directive when the builder is ready to be advertised.
- Framework-independent Nuvio source, parsing, validation, migration, serialization, and ID modules should stay outside React components.
- **Confirmed by live deployment — issue [#33](https://github.com/davecollections/tmdb-id-lookup/issues/33), 2026-07-10:** recorded GitHub Pages HTTP and browser checks confirmed that stable v1 remained at the project root and the isolated React/Vite builder loaded successfully under `/builder/`. Generated JavaScript, CSS, and imported local assets loaded correctly, the backlink to v1 worked, and mobile-width checks passed. The builder remained unlinked from v1 and retained `noindex, nofollow`. Isolated React/Vite under `/builder/` is therefore the confirmed builder direction.
- **Currently unsupported:** history-based direct Builder subroutes remain unproven and unsupported. Issue #33 did not test or adopt React Router or history fallback behaviour. This does not preclude the bounded dialog-local history used by shared mobile Genre context/detail surfaces in issues #110 and #113.

### Framework-independent builder domain — issue #34

**Confirmed by repository tests — issue [#34](https://github.com/davecollections/tmdb-id-lookup/issues/34), 2026-07-10:** builder editor state now uses a plain-data project → collection → folder → source hierarchy under `builder/src/domain/`.

- Every node has a stable builder-only `internalId` supplied by an injectable ID factory; the default uses `crypto.randomUUID()` with no weak fallback.
- The browser entry injects a secure environment-specific factory: it uses `crypto.randomUUID()` when available and generates RFC 4122 version 4 UUIDs with `crypto.getRandomValues()` for same-network HTTP previews where `randomUUID()` is unavailable. The strict framework-independent defaults remain unchanged.
- Current known values live under `editable`, while optional deep-cloned imported JSON lives under `rawImported`. Editing and reordering do not mutate the raw snapshot.
- Sources require an explicit `native-tmdb`, `addon`, or `opaque` category. Fields such as `addonId`, `type`, `catalogId`, and `filters` are not used to guess a category.
- A folder has one authoritative editable `sources` array. Imported `catalogSources` may remain inside the raw folder snapshot, but the domain does not expose a second editable list or create a compatibility projection.
- Pure immutable helpers cover creation, editable updates, ordered insertion, sibling movement, removal, traversal, lookup, and project-wide duplicate-ID detection.
- The domain permits incomplete editor drafts and remains separate from import parsing, export validation, raw overlay, and serialization.
- Node tests prove deterministic identity, identity stability, ordering, immutability, opaque sentinel preservation, the `catalogSources` boundary, explicit categories, duplicate detection, `structuredClone`, and JSON encoding compatibility.

### Preservation-first Nuvio importer — issue #35

**Confirmed by repository tests — issue [#35](https://github.com/davecollections/tmdb-id-lookup/issues/35), 2026-07-11:** a framework-independent importer under `builder/src/import/` now converts JSON text or already-parsed collection arrays into the builder domain.

- Structural validation is atomic. Invalid roots and non-object collection, folder, or active source entries return stable diagnostics and no partial project.
- Collection, folder, and authoritative `sources` order is retained exactly. One injectable ID factory supplies every builder-only internal ID, and project-wide collisions reject the import.
- Recognised fields alone enter `editable`; complete detached collection, folder, and source objects remain in `rawImported`, including unknown/community fields and unknown Discover filter keys.
- Provider-led classification is conservative. Only explicit `tmdb` with one of the seven confirmed types becomes `native-tmdb`, and only explicit `addon` becomes `addon`.
- Unknown, missing, and `community` providers remain `opaque`. Addon-looking fields alone do not prove addon category.
- Explicit TMDB sources with missing or unsupported types remain importable as opaque sources with warnings rather than being rejected or guessed into support.
- `catalogSources`-only compatibility data is detected and preserved but is not promoted, merged, or migrated into authoritative active `sources`.
- Already-parsed sparse arrays are rejected recursively as invalid JSON values, producing a structured atomic importer failure rather than normalisation or partial hydration.
- Ultra MAX compatibility still requires a real exported sample in a later controlled compatibility issue. This importer does not establish a canonical export shape.
- Thirty-two importer tests cover JSON parsing, public diagnostic contracts, structural and sparse-array errors, all seven native types, addon and opaque classification, ordering, conservative editable extraction, filters, raw preservation, legacy projection detection, input immutability, ID collisions, and plain-data encoding.

### Preservation-first Nuvio serializer — issue #36

**Confirmed by repository tests — issue [#36](https://github.com/davecollections/tmdb-id-lookup/issues/36), 2026-07-11:** a framework-independent serializer under `builder/src/serialize/` now converts the builder domain into detached Nuvio collection values or deterministic JSON text.

- Imported unknown and community collection, folder, source, filter, and matched projection fields survive unrelated recognised edits through safe editable-over-raw overlays.
- Current domain collections, folders, and sources replace raw structural arrays and retain current order. Inserted nodes appear, removed nodes disappear, and raw child arrays are never authoritative.
- New nodes use compact supported output. Optional recognised values appear only when present; explicit null envelopes, presentation defaults, backdrop/glow defaults, language fields, and v1 exporter defaults are not invented.
- Recognised field lists are centralised under `builder/src/nuvio/known-fields.js` and shared with the importer without changing importer behavior.
- Only explicit addon-category sources generate `catalogSources` compatibility projections. Native TMDB and opaque sources never generate projections, even when opaque raw data contains addon-looking fields.
- Raw projections are matched deterministically by original identity first and current identity second. Matched unknown metadata survives, and duplicate identities consume projections in original order.
- A source moved between folders can carry only the approved `id`, `addonName`, `manifestUrl`, and `showInHome` compatibility metadata from its own raw snapshot when no folder projection matches.
- Unmatched old projections are removed with stable warnings because current authoritative sources no longer reference them.
- Unresolved `catalogSources`-only legacy data blocks serialization instead of being silently lost or promoted. The serializer does not establish Ultra MAX migration behavior.
- Opaque sources require raw import evidence and serialize with preservation warnings. Incomplete supported native or addon sources fail atomically with stable diagnostics and no partial output.
- Recognised Discover filters are replaced as a group from editable state while unknown raw filter keys survive. Explicit property-removal semantics beyond supported value replacement remain deferred.
- Fifty-six serializer tests cover the original 70 required API, compact-output, ordering, preservation, filter, native, addon, opaque, validation, prototype-safety, determinism, and contract-integration behaviors plus the narrow no-genre identity alias. The existing 8 contract, 14 domain, and 32 importer tests remain unchanged and passing.
- No visible UI calls the serializer yet.

### Evidence-based addon projection migration — issue #37

**Manually confirmed in Nuvio Desktop from owner-reviewed private import/export pairs, and confirmed structurally by repository tests — issue [#37](https://github.com/davecollections/tmdb-id-lookup/issues/37), 2026-07-11:** compact active addon sources and one deterministic legacy projection-only promotion are now separately evidenced.

- One active-addon pair retained 8 collections, 129 folders, 201 active compact addon sources, and 201 matching projections. Ordering and identities were unchanged while Nuvio added explicit defaults/nulls. A source `genre: ""` could correspond to a projection `genre: null`.
- One AIO Metadata-based projection-only pair retained 8 collections, 83 folders, and all 174 projections in order while Nuvio created 174 authoritative addon sources in the same order from `addonId`, `type`, `catalogId`, and normalized genre.
- In that projection-only pair, 140 exact `"None"` genre values became `null`; actual genres such as `"Action"` and `"Sci-Fi & Fantasy"` remained unchanged. The addon type `anime` was preserved alongside `movie` and `series`.
- The full third-party files and unique configuration URLs remain private manual evidence. Repository fixtures are small, sanitised structural reproductions using synthetic identities and `example.invalid` artwork.
- Migration is an explicit framework-independent step under `builder/src/migrate/`. The importer remains preservation-first and never invokes it. The serializer still blocks unresolved projection-only data and succeeds only after authoritative sources exist.
- The migration is atomic, immutable, deterministic with an injected ID factory, collision-checked across the whole project, and independent of addon manifests and networking.
- Addon projection matching now treats missing genre, `null`, empty string, and exact `"None"` as one no-genre identity. This is a narrow evidence-based compatibility alias, not a universal string-normalization rule, and unrelated serialized values are not rewritten.
- Manifest validation, live catalog validation, automatic import conversion, visible UI, Ultra MAX conversion, and AIO Metadata runtime integration remain outside this issue.
- Twenty migration test functions plus the unchanged regression suites cover the 76 requested migration, integration, fixture, and regression behaviours. Issue #38 now records the builder-generated sanitised Nuvio Desktop follow-up alongside the existing private-pair evidence.

### Reproducible Nuvio Desktop migration round trip — issue #38

**Manually confirmed in Nuvio Desktop and confirmed by repository verification — issue [#38](https://github.com/davecollections/tmdb-id-lookup/issues/38), 2026-07-11:** the reproducible sanitised manual-test evidence under `manual-tests/nuvio-desktop/addon-projection-migration/` completed a controlled round trip.

- The issue #37 migration implementation is complete, and its builder-generated client checkpoint is now recorded.
- `builder-migrated-input.json` is generated through the production `importNuvioCollections` → `migrateLegacyAddonProjections` → `serializeNuvioProject` path rather than being authored as an independent expected result.
- The committed input contains 1 collection, 3 folders, 3 authoritative addon sources, and 3 matching compatibility projections in movie, series, anime order with `null`, `"Action"`, `null` genres.
- Canonical contract validation succeeds, and `generation-report.json` records the deterministic input SHA-256 `c14d7e9f9c4c3becccb95718d5b91e94e059652adbd6f8192dbb0c5794491970`.
- Nuvio Desktop `0.1.11-alpha` build `11`, based on Nuvio `0.2.19` on Windows, accepted the collection; one collection and all three ordered folders appeared, every folder opened, and each showed the expected addon-not-found message for `example.sanitised.metadata`.
- The untouched export SHA-256 is `6390428217959af42572038fdd818def5fc9136a98285b6e879504826a0aa7bc`. It retained every collection, folder, source, projection, addon, catalog, genre, order, sentinel, and non-null presentation value.
- Nuvio added explicit source nulls, folder defaults and missing presentation nulls, and collection defaults, including lowercase `tileShape: "poster"`. The recursive semantic comparison found 52 approved additions and zero removals, changed values, array-length changes, order changes, or unexpected differences.
- `scripts/check-migration-round-trip-export.mjs` verifies both exact hashes, all semantic invariants, the deterministic Stage B report, and canonical validation fully offline.
- This is manually confirmed Nuvio Desktop compatibility for the sanitised generated collection shape only. It does not validate live addon resolution, playback, every future client version, automatic migration, Ultra MAX conversion, or AIO Metadata runtime behaviour.

### Pages publication boundary recovery — issue #38

**Confirmed by repository verification — issue [#38](https://github.com/davecollections/tmdb-id-lookup/issues/38), 2026-07-11:** the initial controlled merge `ebf2b8f523d862a80a45405c610d94115a7152ad` made the sanitised manual-test evidence reachable through GitHub Pages because `scripts/prepare-pages-site.mjs` began with `git ls-files` and copied almost every tracked path. The artifact validator checked required runtime files and compiled builder portability but did not reject unexpected paths. Commit `538d681e9c663be4e14f7ca7e28770f43bf3f2b0` immediately reverted the merge and restored the prior production artifact.

Stage C recovered the feature branch from that revert without rewriting the approved Stage A or Stage B commits, restored the evidence byte-for-byte, and hardened the publication boundary in the same pending change set. The reviewed v1 runtime inventory is limited to three root files plus `css/`, `js/`, and `data/`; compiled `builder/dist/` output is copied separately to `.pages-site/builder/`. A shared explicit allowlist now controls both selection and exhaustive artifact validation, and regression tests cover separators, prefix tricks, absolute paths, traversal, repository-only roots, and the dedicated compiled-builder path.

All four issue #38 evidence JSON files, including the untouched owner export, are explicitly forbidden from the Pages artifact. The owner export bytes and its recorded SHA-256 are also checked for absence. The sanitised evidence remains available in the public source repository for auditability, while `manual-tests/`, `tests/`, `docs/`, `scripts/`, builder source, and other repository-only material are not deployed as website content. No Pages workflow, v1 runtime behaviour, builder production module, Worker setting, dependency, or UI/controller behaviour changed.

### Framework-independent application controller — issue #39

**Confirmed by repository tests — issue [#39](https://github.com/davecollections/tmdb-id-lookup/issues/39), 2026-07-11:** the application/controller layer under `builder/src/application/` now coordinates the existing production modules without moving their rules into UI state.

- `createBuilderController` owns one current project plus deeply frozen, stable-identity snapshots containing revision, hierarchical selection, dirty state, migration preview, and four diagnostic scopes.
- Project and diagnostic snapshot commits increment revision once and notify a stable listener snapshot once. Successful selection-only commits create and notify a frozen snapshot without incrementing revision or dirty state. True no-ops retain the previous snapshot identity, and previous snapshots remain unchanged.
- New-project and import replacement are blocked while dirty until the caller explicitly supplies `discardChanges: true`; a blocked replacement does not consume the configured production ID factory.
- Import delegates to the production text/parser entry points, remains atomic and preservation-first, clears selection on success, stores importer diagnostics, and never applies migration automatically.
- Migration preview invokes the production migration with deterministic disposable IDs that avoid all current internal IDs and never consume the controller's real factory. It exposes only unavailable, available, or blocked status, potential counts, and preview errors.
- Migration application remains explicit, uses the real configured factory, retains production atomicity and diagnostics, and marks dirty only when sources are created.
- Creation, editable update, sibling move, and removal delegate to the existing domain factories and immutable operations. Controller insertion adds project-wide collision protection, while selection reconciliation retains surviving ancestors without choosing siblings.
- Serialization and stringification delegate to the production serializer, store export diagnostics, do not auto-migrate, and do not clear dirty state.
- The visible React placeholder does not import the application layer. Browser file reading and download, persistent project format, storage, undo/redo, networking, and visible editor UI remain deferred.
- Thirty-three controller test functions cover 85 requested behaviours. Together with the unchanged 141 existing functions, the Node suite contains 174 test functions.
- The next planned work is the first visible builder shell/list interface.

### First visible builder shell and hierarchy navigator — issue #40

**Confirmed by repository tests — issue [#40](https://github.com/davecollections/tmdb-id-lookup/issues/40), 2026-07-11:** the `/builder/` deployment placeholder has been replaced by the first controller-connected, mobile-first application shell.

- `builder/src/main.jsx` creates one production `createBuilderController` at module scope with the `Untitled project` title and passes it into `BuilderApp`; React StrictMode does not create additional controllers.
- React subscribes through `useSyncExternalStore` with a server/static snapshot reader. Project, selection, revision, dirty state, migration preview, and diagnostics remain controller-owned rather than mirrored into component state.
- Pure UI helpers derive safe collection, folder, source, count, selection, and read-only-detail values while preserving controller order and explicit source categories. Raw imported data and builder internal IDs are not shown.
- Desktop uses a three-panel collections, folders, and sources/details workspace. Mobile workspace drill-down uses explicit parent navigation and no routes or workspace browser-history model; nested task surfaces may use the bounded dialog-local history documented below.
- Deterministic UI helpers create the smallest available `collection-N` or project-wide `folder-N` draft identity through existing controller actions and select only successful creations. They never create sources automatically.
- Operation errors use one inline alert. Legacy migration preview can produce a small available or blocked notice, but no migration action or automatic migration was added.
- The builder keeps its relative marked v1 backlink and `noindex, nofollow` state. The generated Pages validation marker now checks the real product title and `data-builder-shell` while retaining the existing allowlist and evidence-containment safeguards.
- Twenty focused UI test functions preserve the bootstrap, shell, view-model, creation, hierarchy, diagnostics, metadata, environment, and style contract. Together with the unchanged 174-function baseline, the Node suite contains 194 test functions.
- Import/export, forms, source creation, deletion, reordering, storage, networking, routing, dependencies, v1, Worker, workflow, Pages allowlist, Ultra MAX, AIO Metadata, language, and Trakt work remain outside this shell milestone.

### Welcome screen and local JSON import — issue #41

**Confirmed by repository tests — issue [#41](https://github.com/davecollections/tmdb-id-lookup/issues/41), 2026-07-12:** the public builder entry now opens on a real welcome screen under the corrected **TMDB Collection Builder** product name before entering the preserved issue #40 workspace.

- `BuilderApp` remains subscribed to the single production controller on both screens. Its local screen state is presentation-only and is absent from controller snapshots.
- Start New Project calls the existing controller replacement action with `Untitled project`, creates no hierarchy children, leaves the project clean, and enters the workspace only after success.
- Pasted JSON is passed unchanged to `controller.importJsonText` after a UI-only empty check. File import validates explicit selection, JSON filename/MIME support, and a 10 MiB limit before `file.text()` and the same controller call.
- File-derived project titles remove one final case-insensitive `.json` suffix and fall back to `Imported project`; imported hierarchy titles and order remain importer-owned.
- UI transport diagnostics use only stable `code`, `path`, and `message` fields. Controller parse/import diagnostics pass through without parser internals or partial-project replacement.
- Successful importer warnings enter the workspace as a collapsed, bounded details summary without becoming fatal or exposing raw JSON.
- Imported content remains local: no networking, upload, persistence, object URL, logging, query parameter, or original-text controller state is introduced.
- Welcome controls are labelled, keyboard-operable, busy-aware, and mobile-first at the required 360–412px widths; wider action columns remain restrained and the existing 1024/1280px workspace is preserved.
- Thirty-three focused welcome/import test functions cover 116 tracked behaviours. With the unchanged 194-function baseline, the Node suite contains 227 test functions.
- Export, persistence, dirty replacement UI, editing, source creation, deletion, reordering, migration actions, routing, dependencies, v1 runtime, Worker, Pages allowlist/preparation/deployment, Ultra MAX, AIO Metadata, language, and Trakt work remain outside this milestone.

### Essential collection and folder editing — issue #42

**Confirmed by repository tests — issue [#42](https://github.com/davecollections/tmdb-id-lookup/issues/42), 2026-07-12:** the workspace now supports one controlled inline editor for collection/folder Nuvio-facing IDs and titles.

- The editor targets stable builder-only internal identity but never displays or changes it. Duplicate Nuvio-facing IDs remain valid and independently selectable.
- Local React state contains only target identity/type, two form strings, original string/presence/support metadata, touched flags, and structured field diagnostics. Complete nodes, projects, children, sources, and raw imported snapshots remain outside the draft.
- Absent or non-string imported known values are not stringified or rendered. They remain untouched unless the user enters valid replacement text for both required fields.
- Validation checks trimmed length without trimming submitted data. Minimal patches contain only effectively changed `id` and/or `title` fields and are delegated through `controller.updateNode`.
- Opening, cancelling, and unchanged Apply are controller-free. Actual edits retain selection, mark the project dirty, and increment controller revision once; controller failure keeps the form and remains structured.
- While editing, hierarchy selection, both mobile back controls, collection/folder creation, both edit triggers, and selection-changing folder-summary navigation are natively disabled. Apply and Cancel stay available.
- Opaque/community and mixed native/addon fixtures confirm raw/unknown-field preservation, source category/order retention, `catalogSources` compatibility evidence, serializer overlay behavior, and unchanged migration eligibility.
- The single responsive editor keeps labelled fields, unique descriptions, one local alert, first-invalid-field focus, edit-trigger focus restoration, strong focus styles, and approximately 48px controls.
- Forty-five focused node-editing test functions cover 86 requested behaviours. With the existing 227-function baseline, the Node suite contains 272 test functions.
- Project/presentation/source editing, source creation, export, deletion, reordering, persistence, migration actions, networking, dependencies, v1, Worker, language, Ultra MAX, AIO Metadata, and Trakt remain outside this milestone.

## 11. Automatic IDs and workspace flow — issue #43

**Confirmed by repository tests — issue [#43](https://github.com/davecollections/tmdb-id-lookup/issues/43), 2026-07-12:** collection/folder Nuvio IDs are securely generated, hidden, preserved when usable, and silently repaired when missing, invalid, or duplicated. Controller import remains clean and preservation-first, title editing is UI-only and title-only, visible project/dirty status is removed, workspace return is guarded by inline discard confirmation, and collection/folder empty-state creation controls are functional. Presentation settings must be completed next, followed by a mandatory owner UI/flow review before source creation.

- Twenty-seven focused automatic-ID/workspace-flow test functions bring the Node suite from the existing 272-function baseline to 299 test functions.

### Collection and folder presentation settings — issue #53

**Confirmed by repository tests — issue [#53](https://github.com/davecollections/tmdb-id-lookup/issues/53), 2026-07-25:** owner visual review superseded the original inline-editor direction. The corrected workflow now uses one compact Edit action on every collection and folder card plus one responsive collection/folder settings modal. Selection-only snapshots no longer advance project revision. The shared collection known-field contract now includes the evidence-backed `focusGlowEnabled` boolean; folder and source inventories remain unchanged.

- Manual blank collections explicitly receive `pinToTop: false`, `focusGlowEnabled: true`, `viewMode: "TABBED_GRID"`, and `showAllTab: true`. Manual blank folders explicitly receive `tileShape: "POSTER"` and `hideTitle: true`; automatic Nuvio IDs and unique draft titles remain unchanged.
- At the issue #53 checkpoint, every collection and folder card owned one always-visible Edit text button and the earlier quick-rename form was absent. Issue #63 supersedes only that action placement by moving Edit into an in-card overflow menu and adding a smaller mobile selected-context rename variant; at the #63 checkpoint, the large selected-entity blocks remained absent and source cards had not yet gained editing. Issue #78 and later family adapters supersede that source-menu boundary for supported shapes.
- At the shared 900px UI breakpoint, desktop draft creation retains its original select-and-advance behavior. Mobile collection creation stays on Collections without selecting the new card; mobile folder creation keeps the parent collection selected and stays on Folders. Repeated mobile creation preserves insertion order, IDs, defaults, one revision per creation, and the absence of source creation. New cards scroll into view with reduced-motion awareness where supported.
- Edit directly selects and targets its exact card without a preliminary click, project mutation, dirty change, or revision increment. It opens the modal with initial Title focus; Apply and Cancel retain selection and restore the exact Edit trigger.
- The modal is a single accessible DOM instance with bounded desktop scrolling, full/near-full-screen mobile presentation, contained focus, safe Escape cancellation, non-discarding backdrop clicks, exact trigger focus restoration, body-scroll control, and an inert/dimmed workspace with a non-blur fallback.
- Collection editing offers source-level Tabs and Rows: each folder presents each source as a tab or streaming-style row. For a folder with two or more sources, Tabs plus Include an All tab when using Tabs adds an All tab combining those sources; a one-source folder has no visible All tab. Rows shows no tabs, but the saved All-tab preference remains visible and editable for a later switch to Tabs. Pin to top forms the group shown before unpinned collections; Builder exports retain each group's relative collection-list order and store no separate interactive pin sequence. Issue #69 supersedes this checkpoint's source-layout wording and removes the visible focus-glow switch; deliberate replacement of the collection title with exactly one U+200E LEFT-TO-RIGHT MARK remains independent.
- Folder editing places one Folder title visibility radio-card group immediately below Title, followed by Poster/Landscape. Show everywhere maps a valid visible title to `hideTitle: false`; Hide on home screen only maps a valid visible title to `hideTitle: true`; Hide everywhere maps the title to exactly one U+200E and maps `hideTitle: true`. The former two interacting switches are absent.
- Imported titles containing only repeated U+200E are recognised and preserved exactly while untouched. An imported invisible folder opens with an empty disabled Title field and Hide everywhere selected; its raw title is absent from draft/DOM state. Unrelated edits preserve the exact title and every supported, absent, or unusual `hideTitle` presence state. Either visible choice requires valid visible text; returning to Hide everywhere without a replacement restores exact preservation, while re-hiding after a deliberate replacement emits canonical one-character U+200E and `hideTitle: true` only when needed. A visible-mode → Hide everywhere → original visible-mode cycle is a clean no-op, and no modal-only visibility field enters serialization. Empty/whitespace titles never become invisible automatically; U+200B and other format-only alternatives are not recognised. Builder cards, actions, and summaries use `Hidden title` / `Invisible in Nuvio` display-only fallbacks and meaningful accessible names, none of which enter Nuvio JSON.
- Imported `focusGlowEnabled: true|false` values, absence, and unusual values retain their exact preservation behavior and are never stringified. Issue #69 removes the visible switch and its replacement guidance; programmatic canonical replacement support remains internal compatibility plumbing, and summaries show Focus glow enabled only for supported values.
- Imported `FOLLOW_LAYOUT` and `SQUARE` remain preservation-only. Supported imported choice casing remains unchanged while untouched. Absent, unsupported, and unusual JSON-compatible values are not copied or rendered and survive unrelated edits.
- Draft construction, touched tracking, validation, and minimal patch generation remain UI-only helpers. Actual changes still commit once through `controller.updateNode(internalId, patch)`, while opening, cancel, no-op apply, and touched-then-reverted values remain controller-free.
- Preservation tests cover raw evidence, unknown/community fields, identity, children, source order/category, compatibility projections, artwork fields, repeated invisible titles, second-cycle serialization stability, and absence of builder-only flags/fallbacks.
- Artwork, focus GIF, cover, logo, backdrop, hero, source creation, and screenshot-evidenced expanded Nuvio controls remain deferred. Future focus-GIF support defaults off unless deliberately enabled.
- Source-specific Search/Add, template, and recipe defaults are recorded in [`BUILDER_PRODUCT_PLAN.md`](./BUILDER_PRODUCT_PLAN.md) but remain unimplemented.
- Collection/folder/source reordering is desired as a separate focused milestone and is recommended before Search/Add; issue #53 contains no reordering implementation.
- Bulk presentation settings are desired but deferred to a separate focused issue; issue #53 contains no bulk implementation.
- The mandatory next gate is independent review plus Dave's focused UI/flow review. No pull request, reordering implementation, or source creation begins within this issue.

### Collection and Folder settings polish — issue #69

**Merged through issue [#69](https://github.com/davecollections/tmdb-id-lookup/issues/69) / PR [#70](https://github.com/davecollections/tmdb-id-lookup/pull/70) after focused repository validation and owner UI/flow review, 2026-07-30:** the established settings schema and touched-only lifecycle remain unchanged while the Collection and Folder settings presentation is clearer and more compact.

- The Collection source-presentation heading is **How sources appear in this collection**, its helper is **Choose how each folder in this collection displays its sources in Nuvio.**, and the visible **Tabs (recommended)** option still stores and serializes `viewMode: "TABBED_GRID"`. Rows remains `ROWS`; imported `FOLLOW_LAYOUT` remains preservation-only.
- The visible focus-glow control is removed. `focusGlowEnabled` remains in the shared recognised collection field inventory and serializer overlay. Manual blank collections still explicitly create `focusGlowEnabled: true`; imported explicit `false` remains false, explicit `true` remains true, missing stays missing, and unusual values remain raw-preserved. Opening/cancelling settings creates no revision, and changing another setting does not touch the field.
- Full Folder settings use **Basic details** for Title and **Display** for title visibility plus tile shape. The section component is the narrow future-group seam; no Artwork heading, placeholder, media picker, preview pane, control, or schema field is added.
- Folder title visibility keeps the existing `title`/`hideTitle` mapping and all three outcomes, but presents them as compact native radios with one accessible fieldset name and normal radio keyboard behavior.
- Poster and Landscape keep `tileShape: "POSTER" | "LANDSCAPE"` and are presented as compact visual cards with CSS aspect-ratio previews, native checked state, an additional visible check mark, and the existing focus ring. Imported `SQUARE` remains preservation-only.
- Focused rendered-component, helper, serializer, lifecycle, CSS-contract, and no-artwork assertions cover the revised copy, unchanged stored values, hidden focus control, preservation behavior, semantic groups, radio naming/check state, visual shape cards, mobile boundaries, and no-op revision behavior.
- V1, Worker routes, Add Source providers/recipes, sources/catalog projections, node IDs, deletion/reordering, dependencies, lockfiles, Pages boundaries, artwork runtime, and `nuvio-assets` remain unchanged.

### Accessible hierarchy reordering — issue #59

**Confirmed by repository tests, completed owner local browser/visual review, and bounded Nuvio client evidence — issue [#59](https://github.com/davecollections/tmdb-id-lookup/issues/59), implementation commit `326efe0bf78ee095f1d9efd5420b18d509d5c14f`, 2026-07-27:** the existing collection, folder, and source hierarchy cards now provide one compact six-dot handle for pointer/touch dragging and keyboard-accessible movement without permanent arrow controls, ordering metadata, or a second domain mutation path.

- Folder and source visible siblings use their authoritative array order. Collections display the stable `pinToTop: true` group before the stable ordinary group; absent, false, and unusual preserved pin values remain ordinary.
- Collection movement is bounded inside the current visible pin group and never changes `pinToTop`. The UI retains both group-relative movement positions and overall visible-list positions, maps the adjacent visible collection back to that sibling's raw-array index, and announces the completed overall position. This covers interleaved imported arrays where visual and raw neighbors differ without exposing raw indexes.
- A completed pointer drop or successful keyboard arrow action delegates exactly once to `controller.moveNode(internalId, targetIndex)`. Hovering, cancellation, lost capture, same-position drops, and invalid keyboard boundaries remain controller-free. Stable internal IDs retain the selected node and React card identity; folder moves cannot change collection, and source moves cannot change folder or source category.
- The 18px six-dot grip and its 46px target sit inside the visual card, while collection/folder Edit remains a compact independent action beside that card. Pointer Events begin on the handle only and use a small vertical threshold. A pointer session is created only after the handle successfully establishes and, where supported, confirms Pointer Capture; a failed attempt declines that pointer gesture without overlay, data change, or loss of keyboard availability. After the threshold, a fixed clone of the complete associated row follows the pointer at its original grab offset above panel clipping; the measured original row becomes a same-sized provisional placeholder, and crossed siblings shift with a restrained 150ms FLIP-style transform. The insertion line remains secondary emphasis. These transforms and the overlay are UI-only until the final drop, while reduced-motion mode retains the overlay and placeholder without sliding or settle animation.
- Pointer destinations clamp to the current pin group and provide restrained viewport-edge auto-scroll. `touch-action` is scoped to the handle so ordinary page scrolling remains available elsewhere. While a captured pointer session is active or its 150ms drop settle is pending, hierarchy selection, Edit, creation, workspace/root navigation, and keyboard-mode entry are ignored; the active handle remains enabled so capture is retained, and Escape can still cancel before settling. Changed drops settle the overlay toward the provisional placeholder before one controller call; same-position drops settle home without a call, and cancellation removes the overlay, placeholder, sibling transforms, and grabbing state immediately. Normal interaction resumes as soon as completion or cancellation clears the session.
- The same handle is focusable and named `Reorder …`; Enter or Space toggles keyboard reorder mode, Arrow Up/Down moves one valid position, and Escape exits. Hidden instructions, meaningful blank/hidden-title fallbacks, focus restoration to the moved handle, and one polite live region avoid a drag-only accessibility dependency.
- A valid changed move advances project revision exactly once. Same-index and cancelled pointer actions do not advance revision, and invalid or out-of-range controller targets retain structured diagnostics without advancing project revision.
- The former desktop and mobile Selection details summaries are removed. At the issue #59 checkpoint, source cards remained read-only hierarchy entries with one reorder handle and no Edit action; issue #78 and later adapters subsequently added Edit for supported shapes.
- Preservation tests cover collection/folder/source identity and selection, pin values, folder artwork, unknown/community fields, opaque sources, native/addon categories, authoritative source order, addon compatibility projections, second-cycle serialization stability, and the absence of Builder-only movement metadata.
- Search/Add, source editing/creation, deletion, bulk movement, artwork-runtime consumption, v1, Worker, dependencies, Pages allowlists, and `nuvio-assets` remain unchanged.

#### Nuvio client ordering evidence — 2026-07-27

The deterministic evidence bundle is recorded under [`manual-tests/nuvio-clients/issue-59-builder-reordering/`](../../manual-tests/nuvio-clients/issue-59-builder-reordering/). The checker replays the six production controller moves from the preserved seed, verifies revision 7, exact final serialization, second-cycle stability, hierarchy/pin/parent/source/projection order, exact raw hashes, normalization, and privacy fully offline.

| Client | Result | Evidence method |
| --- | --- | --- |
| Nuvio Desktop `0.1.11-alpha` build `11`, based on `0.2.19`, Windows 11 | Passed | Owner visual review plus exact raw export |
| nuvio.tv/web, build/browser unknown | Passed with normalization | Owner visual review plus exact raw export |
| Nuvio mobile, version/OS/device unknown | Passed with raw-artifact limitation | Owner visual review plus owner-supplied exported JSON text |
| Nuvio TV, version/device unknown | Passed with no independent export | Owner visual review plus synced profile backed by the verified web export |

Desktop retained raw D/B/C/A, visible C/A/D/B, pin groups, D folders C/A/B, C/A/B source and projection arrays, parents, IDs, and all sentinel levels; its raw export contains exactly 102 approved default/null additions and no removals, changed values, array-length changes, or order changes. Web retained the same hierarchy and order while normalizing source `genre` from `null` to `""`, omitting optional nulls, changing folder `focusGifEnabled` from Desktop's `true` to `false`, adding collection `focusGlowEnabled: false`, retaining source sentinels, and dropping collection/folder/projection sentinels. Mobile retained the required hierarchy, parents, IDs, pins, sentinels, and `genre: null` in the reviewed export text, but no exact raw mobile file was available and no hash is claimed. TV supplied no independent export; its C/A/B source-array proof is explicitly the synced exact web export.

The issue #59 client-ordering gate is complete. Its implementation is integrated through PR #60, and issue #61 / PR #62 integrated the Windows line-ending-safe evidence check without changing hierarchy behavior.

### Persistent hierarchy creation and safe deletion — issue #63

**Merged through issue [#63](https://github.com/davecollections/tmdb-id-lookup/issues/63) / PR [#64](https://github.com/davecollections/tmdb-id-lookup/pull/64) after focused repository validation and owner desktop/mobile review, 2026-07-28:** populated collection and folder panels retain their header actions and now add one visually secondary full-row Add button beneath the final card. Sources deliberately have no Add action until the next Search/Add and source-creation issue.

- Bottom Add buttons call the existing `createDraftCollection()` and `createDraftFolder()` flows, preserving automatic titles/IDs, defaults, viewport-aware selection, mobile drill-down, scrolling, ordering, and one revision per creation. They remain outside semantic lists and reorder calculations.
- Every collection, folder, and existing source row owns one in-card overflow trigger named `Actions for <type> “<safe name>”`. At the issue #63 checkpoint, collection/folder menus contained Edit and Delete while source menus contained Delete only. Issue #78 and later adapters supersede that source boundary: current supported-source menus show Edit source before Delete, while unsupported sources remain Delete-only. The visual card order is grip, primary identity/navigation, chevron where applicable, and the vertical-dots trigger. The former external Edit/Delete rail is absent.
- Only one reusable actions menu can be open. Its body portal and fixed positioning escape card/panel clipping; pure geometry measures the complete rendered menu against `visualViewport` offsets and dimensions (falling back to the layout viewport), flips upward when the full menu will not fit below, and clamps to a 10px visible margin. Placement precedes prevent-scroll first-item focus, so opening last and second-last visible cards does not move the page. Pointer, Enter/Space, Escape, outside press, arrow navigation, exact trigger focus restoration, selection/node/modal/viewport-change closure, keyboard-reorder exit, drag locking, and pointer-inert drag clones remain explicit interaction contracts.
- Below 900px, the selected collection context above Folders and selected folder context above Sources each add one pencil action using the safe accessible name. It opens a rename-only form containing Title, the relevant invisibility switch, diagnostics, Apply, and Cancel. Desktop has no duplicate pencil.
- The collection rename switch hides the title in Nuvio. The folder switch hides it everywhere; switching it off restores the original non-invisible `SHOW_EVERYWHERE` or `HIDE_HOME_SCREEN` outcome rather than silently changing home-screen visibility.
- One pure impact helper resolves target identity/type/name, parent and visible sibling order, descendant folders, active sources, imported `sources` entries, legacy/projection `catalogSources` evidence, confirmation copy, and selection/mobile/focus recovery before mutation.
- Empty collections and folders without active or imported source evidence delete immediately. A collection with any folder and a folder with active or imported/legacy source evidence require the focused Cancel-first confirmation dialog. Every individual source also confirms, using `This source will be permanently removed from this folder.` and `Delete source`. Projection-only imported content is never treated as empty.
- The dialog uses the existing inert/dimmed modal pattern, contains focus, cancels on Escape, restores the exact trigger, and prevents double submission. Cancel performs no controller action or revision.
- `controller.removeNode(internalId)` remains the only project mutation. One successful deletion advances revision once. Selection-only recovery then prefers next sibling, previous sibling, or parent without advancing revision; collection recovery follows the complete pinned-then-ordinary visible order. Unaffected selection is preserved.
- Mobile remains at the same visible hierarchy level when a sibling survives and falls back to the parent/empty level only when needed. Focus moves to the nearest sibling card, relevant Add action, or parent/back control, and a polite live region announces success.
- Removing a subtree leaves unrelated identities, order, presentation, pin state, artwork, unknown/community fields, and raw imported values unchanged. Removing an addon source rebuilds `catalogSources` from surviving authoritative sources, removes only the matching projection, preserves unrelated projection values/order, passes canonical validation, and remains textually stable through a second import/serialize cycle. No deletion metadata is serialized.
- Add/menu/rename/Delete share the existing editor, delete-confirmation, return-confirmation, pointer-drag, and 150ms settle gate. Opening a menu or delete confirmation exits keyboard reorder mode.
- Focused hierarchy and node-editor tests cover impact rules, names/plurals, menu semantics, rename-only scope and visibility restoration, selection/revision recovery, double-submit/failure/cancel behavior, subtree preservation, addon projection removal, canonical/cycle stability, locking, accessibility, and responsive static contracts.
- Within issue #63, Search/Add, source creation/editing, bulk deletion, multi-select, reparenting, undo/redo, presentation changes, artwork-runtime consumption, Network Poster, v1, Worker, dependencies, workflows, Pages boundaries, and `nuvio-assets` remained unchanged.

### Add Source foundation with TMDB movie franchises — issue #65

**Merged through issue [#65](https://github.com/davecollections/tmdb-id-lookup/issues/65) / PR [#66](https://github.com/davecollections/tmdb-id-lookup/pull/66) after focused repository tests, final owner physical-iPhone acceptance, and successful current Nuvio Desktop import/runtime/round-trip evidence, 2026-07-29:** a selected folder now exposes one staged Search/Add flow for a native TMDB movie `COLLECTION` source. The focused contract is documented in [`BUILDER_SOURCE_ADD.md`](./BUILDER_SOURCE_ADD.md).

- Sources shows `Add source` in its header and either `Add first source` or `+ Add another source` in its body only when a folder is selected. All three controls open one body-portalled dialog and no disabled placeholder appears without a folder.
- At the issue #65 checkpoint, Movie franchise · TMDB was the only visible mode. Later issues extend the ordered Add Source registry documented below.
- A framework-independent parser accepts positive decimal IDs and strict HTTPS `/collection/{id}` or `/collection/{id}-{slug}` URLs on exactly `themoviedb.org` and `www.themoviedb.org`. It rejects unsafe or non-whole IDs, non-HTTPS and lookalike hosts, other entity paths, query-only IDs, embedded URLs, credentials, ports, and malformed paths. Eligible remaining text searches after two trimmed characters.
- The provider adapter uses only the existing `/3/search/collection` and `/3/collection/{id}` Worker routes. Vite extracts exactly one anchored Worker declaration from root v1 `js/config.js`; after trimming, build-time and runtime validation accept only the parsed HTTPS origin or that exact origin plus one trailing slash, rejecting credentials, explicit ports, paths, queries, fragments, and raw normalization aliases. Vite injects only the canonical origin without executing the classic script or adding a second endpoint literal.
- Search always sends `include_adult=false`, including in its cache identity. Search normalization excludes only result objects explicitly marked with boolean `adult: true`; otherwise it preserves TMDB's returned result set and order without local word, prefix, singular/plural, accent, spelling, or mature-text admission rules. The Builder relies on TMDB's collection results and is not an age-classification service or age guarantee.
- Every result selected from search receives a collection-details request before Review. Search remains visible while only that result shows and locks its loading state. Failure preserves query/results/page/scroll and exposes a persistent focused/scrolled alert before the results; a retry can proceed. Exact ID/URL input also resolves through the same details policy. Structurally valid details are not rejected from collection/contained-title wording or collection/part adult flags. Normalization keeps only collection identity, bounded overview, validated poster path, movie count, and ordered contained movie title/release-year rows. The UI builds only `w185`/`w342` URLs under the already-approved TMDB image origin. Posters, overview, and contained titles are UI-only and never serialize.
- The official name seeds the editable title, while the recipe remains fixed to `native-tmdb`, `provider: "tmdb"`, `tmdbSourceType: "COLLECTION"`, `mediaType: "MOVIE"`, `sortBy: "original"`, positive numeric `tmdbId`, and explicit empty `filters`.
- Semantic validation requires the exact draft shape and rejects extra, raw, addon, Trakt, or UI-only fields. New native sources have no Nuvio-facing source `id`, no `rawImported`, no serialized Builder `internalId`, and no `catalogSources` projection.
- Duplicate identity is `tmdb|COLLECTION|numeric tmdbId|MOVIE` within the selected folder; title is irrelevant. The first Apply warns without mutation and names the existing source. Explicit Add anyway retains both only when its approval carries the exact duplicate identity still under review; imported duplicates remain untouched.
- Apply revalidates, checks interaction locks and the still-selected/still-existing folder, activates a synchronous one-shot gate before any async boundary, appends through exactly one `controller.createSource()` call, then selects the created source without another content revision. Success closes the modal, scrolls and focuses the source control, and announces through a polite status region. Close, lookup/validation failures, duplicate warning, missing-folder, lock, and repeat-tap paths make no source mutation.
- Existing source reorder and Delete continue through their established controller paths immediately after insertion. Canonical validation, addon projection stability, unrelated raw/unknown preservation, and textually stable second-cycle serialization pass in focused tests.
- Search and Review are explicit stages in one semantic dialog. Search contains no empty action footer and hides pagination for a single page. Review adds Back/Close, poster or placeholder, TMDB ID, count, editable title, fixed recipe, the explicit poster-only **Preview titles** action, and exactly one contextual Add action. The earlier expandable contained-title text list is absent; Back restores query, results, page, selected result, scroll, and focus without another request.
- Below 900px the responsive dialog uses an isolated z-indexed portal with an opaque layout-viewport guard behind an opaque Visual-Viewport-sized task surface; the mobile surface removes desktop translucency and cyan decoration. Initial geometry is available during render, and a pre-paint layout effect establishes body locking, live Visual Viewport observation, and focus before the first visible frame. The same guard remains while Search/Review, keyboard, browser-chrome, portrait, and landscape geometry changes. From 900px the intentional centered desktop backdrop remains. The dialog has one DOM implementation and one scroll owner, four-edge safe-area padding, focus containment, Escape/Close, exact invoking-trigger restoration, inert hierarchy locking, no-scroll focus, and exact pre-existing body-style/class/scroll restoration. The uncropped mobile review poster uses a responsive 180–220px maximum, can shrink further in short keyboard-height viewports, and moves beside the editable content where short landscape width permits. The mobile Sources heading renders its count inline to protect Add-source space.
- At the issue #65 checkpoint, Dave's final physical-iPhone review passed initial opening, opaque layer isolation without Builder/cyan bleed, Search → Review → Back, keyboard and Safari address-bar changes, portrait/landscape safe areas, poster layout, the then-current contained-title expansion, fixed Add action, source creation, duplicate handling, and usable interaction. Issue #166 later replaced that legacy expansion with the shared poster-only title Preview. No device model, OS version, or browser build was supplied or inferred.
- On 29 July 2026, Dave imported the production-generated fixture into current Nuvio Desktop. The source resolved the Harry Potter franchise and expected movie titles. Its exact immediate export expanded compact absent/empty fields into null/default fields while preserving provider `tmdb`, `COLLECTION`, ID `1241`, `MOVIE`, sort `original`, title, and no addon projection.
- Owner-observed current Desktop sorting is: `original` preserves TMDB collection API order; `primary_release_date.desc` works newest-first; unsupported `primary_release_date.asc` falls back to TMDB-provided order. The Builder keeps serialized `original` and displays `TMDB-provided order`; it exposes no sort control.
- Forty-one foundation tests and twenty-two UI tests cover the corrected deterministic boundary, including strict canonical Collection/People review links and responsive long-title handling. The production-generated sanitized fixture, exact Desktop export, and read-only checker live under [`manual-tests/nuvio-clients/issue-65-builder-add-source/`](../../manual-tests/nuvio-clients/issue-65-builder-add-source/). A second current Nuvio client is not tested and remains desirable but non-blocking unless conflicting behavior appears.
- Within issue #65, TMDB lists, companies, networks, people, directors, Discover, genres, decades, direct movies/TV/seasons, addons, manual/opaque sources, bulk creation, automatic hierarchy, source editing/sort/artwork, artwork-runtime integration, Trakt, v1, Worker, Pages contracts, dependencies, workflows, and `nuvio-assets` remained unchanged.

### Unified People Search/Add — issue #74

**Confirmed by focused deterministic repository tests, both owner Nuvio Desktop runs, final visual evidence, and the owner-supplied immediate export — issue [#74](https://github.com/davecollections/tmdb-id-lookup/issues/74), completed 2026-08-02:** People has a short selected-folder quick add and a collection-level multi-person folder action backed by the same provider, count, source, validation, duplicate, and artwork modules. The focused contract is documented in [`BUILDER_PEOPLE.md`](./BUILDER_PEOPLE.md).

- Name search preserves TMDB result order and displays canonical name/ID plus `Known for <department>`. Normalized state retains every valid TMDB-supplied `known_for` row without sorting or another request; desktop renders the first three and the 520px mobile breakpoint renders only the first, with natural wrapping and retained media/year metadata. Empty arrays render no known-for row. Exact TMDB IDs/links omit those rows when unavailable. Missing paths visibly say `No profile image`; failed expected images say `Image unavailable`, with distinct accessible announcements.
- Folder `Add source → People` selects one person and directly configures Acting Movies/Series and Directed Movies/Series. An empty Builder-generated `Untitled Folder`/numbered default with no raw snapshot or deliberate artwork is atomically renamed to the canonical person, receives independently resolved final artwork, and receives the sources in one revision. Named, populated, imported, and deliberately artworked folders remain preservation-only and perform no artwork request/preview.
- At the historical issue #74 checkpoint, Folders exposed `Add people` for the selected collection. Exact-ID checkbox selection persisted across query/page/scroll/Back, was independently removable, rejected duplicates, and capped that UI at 20 with visible copy. The limit was never a controller, serializer, or durable hierarchy limit. Issue #118 / PR #119 supersedes that collection-batch shortcut with the shared uncapped New Collection/New Folder People hierarchy while preserving Add Source. The issue #74 evidence remains useful for its selected-only detail loading, canonical-name folder/source construction, single-revision atomicity, order preservation, and full rollback behavior.
- The four direct combination controls show friendly title-count states without a separate count matrix. Acting/Directing defaults select every positive media combination in the indicated role. Unsupported departments select the strictly larger non-zero supported role; ties, unavailable, and all-zero counts select nothing. Defaults run once and Retry/Refresh/Back/rerender preserve manual choices.
- `PERSON` counts distinct cast Movie/TV IDs only; `DIRECTOR` counts distinct crew Movie/TV IDs with case-insensitive `Director` jobs. Zero/unavailable counts remain selectable and non-blocking. No global count sidecar, scheduled work, or per-result credit request is involved; ordinary Company/Network catalogue maintenance remains separate.
- Native identity remains `tmdb|sourceType|personId|mediaType`. Source titles now match stable v1 exactly: `Movie Credits`, `Series Credits`, `Directed Movies`, and `Directed Series`; the folder retains the canonical person name. `BOTH`, generic all credits, unsupported crew roles, source/Builder IDs, counts, UI metadata, and native `catalogSources` projections remain rejected or absent.
- Destination-folder duplicates are actionable, elsewhere matches informational, the main quick-add inserts missing identities only, and Add all anyway is bound to the exact selected folder plus current identities. Validation previously presented on Review remains at the final Configure submission gate.
- New and promoted-folder artwork resolves independently per exact person ID plus creation context: validated runtime orientation first, TMDB `w500` Poster second, then empty URL/visible title/`👤`. Configuration replaces the TMDB profile with one final applied artwork representation rather than repeating an artwork panel. Exact keyed tokens prevent previous-person, failed-person, concurrent-person, and reopened-flow leakage. Existing-folder quick add performs no artwork request or preview.
- Content-based source chooser and short Search sizing remove empty vertical space while shared body lock, Visual Viewport geometry, single scroll owner, focus recovery, live regions, keyboard controls, safe areas, reduced motion, and one-column 360–412px behavior remain.
- The first Tom Hanks/Steven Spielberg Desktop import resolved all four role/media catalogues and preserved person IDs, source/media types, sort, order, grouping, and the immediate-export contract; explicit empty artwork and duplicate person-name tabs keep it bounded to source-contract evidence. On 2026-08-02, Dave imported the regenerated fixture into current Nuvio Desktop on Windows 11. Screenshots confirmed the curated Tom Hanks and Steven Spielberg Posters, four distinct stable-v1 tabs, and populated role/media catalogues. The owner-supplied immediate export preserved the exact SHA-versioned URLs, `hideTitle: true`, grouping, source order/titles/identities, and empty `catalogSources` with normal Desktop null/default expansion. The client version/build was not supplied. [`ASSET_GAPS.md`](../../manual-tests/nuvio-clients/issue-74-builder-add-people/ASSET_GAPS.md) records John Cena's missing curated Poster with an owner-test ID confirmation gate. V1, Worker, production data, dependencies, and `nuvio-assets` remain unchanged.

### Global Company and Network count pre-cache retirement — issue #90

**Confirmed repository direction through issue [#90](https://github.com/davecollections/tmdb-id-lookup/issues/90), 2026-08-06:** the not-yet-activated global Company Movie, Company Series, and Network Series pre-cache architecture is retired. Production no longer has typed target, progress, patch, completion, recovery, sidecar-publication, September-activation, or Company-Series-collector paths.

- The ordinary Company days 1–14 and Network days 1–2 catalogue refreshes remain. They still refresh entity details and the compact `t` / CSV `titles_count` values used by V1. For Networks, issue #102 makes explicit `t: 0` a confirmed zero-series result and an absent `t` an unknown current count; the existing nightly Network repair retries unknowns through its Network-only allocation.
- Export-coverage audit, capped repair, shared request-budget reservations and receipts, retry behavior, and TMDB host safeguards remain active. Historical files under `maintenance/tmdb-request-budget/` are immutable records and are not rewritten to match the retired architecture.
- Cached Company and Network catalogues remain the V2 Search/Add discovery source. Issue #92 consumes the Company catalogue for user-facing Studio search and issue #98 consumes the Network catalogue while discarding its legacy `t`; both fetch fresh media-specific counts only after exact selection. Counts are informational and non-blocking; unavailable counts must not prevent supported source creation.
- Issues [#71](https://github.com/davecollections/tmdb-id-lookup/issues/71) and [#73](https://github.com/davecollections/tmdb-id-lookup/issues/73), PR [#80](https://github.com/davecollections/tmdb-id-lookup/pull/80), and the removed typed-count pipeline/recovery documents remain historical architecture evidence in Git history. They are not active operating instructions.
- Stable V1 export, Builder source behavior, Worker routes, ordinary catalogue data, dependencies, lockfiles, Pages publication, and `nuvio-assets` remain unchanged by the retirement.

### Studio source creation — issue #92

**Merged through issue [#92](https://github.com/davecollections/tmdb-id-lookup/issues/92) / PR [#93](https://github.com/davecollections/tmdb-id-lookup/pull/93), 2026-08-08:** the Add Source chooser adds user-facing **Studios** while retaining internal TMDB/Nuvio `COMPANY` terminology. The focused implementation contract is documented in [`BUILDER_STUDIOS.md`](./BUILDER_STUDIOS.md).

- Movie franchise Search, People Search, and Studio Search now expose visible Back navigation to the source-type picker while preserving folder/project/modal state. Existing People Configure → Search behavior and Studio Configure → Search preserve query, results, page, practical scroll, folder context, and logical result focus; picker return restores the opening mode option without browser history.
- V1 Company discovery searches ID, name, parent, country code/full name/aliases, and headquarters, with contain logos and a safe external TMDB link. V2 adapts those concepts into accent-folded name/parent/country/headquarters search, deliberate represented two-letter country codes, compact non-address locations, normalized clipped contain logo frames, restrained missing/failed-logo text, and canonical TMDB Company links; it does not copy V1's table layout, raw addresses, default ID sort, or blank broken-logo behavior.
- Studio search lazily loads the ordinary compact Company catalogue, safely skips malformed rows, and pages by 20. A non-empty query with no explicit override uses hidden Best Match: exact ID, exact name, name prefix, name contains, parent, then country/location, with Movie count only as a within-tier tie-break. A–Z and Most movies pills toggle explicit overrides; **Hide studios with no movies** excludes only known zero values before pagination. Empty search automatically enters paged Browse, defaulting to visibly selected Most movies, with no separate Browse button. The native in-field clear returns directly to that state. These states survive Configure → Back. Compact cards always show `Movie Count`: positive and zero values are numeric, while genuinely absent/malformed values show `Unknown` and remain visible under the filter. The current catalogue's legacy all-implicit-zero encoding is recognized without editing the data artifact; the two existing Company compact writers now preserve explicit `t: 0` so future omissions remain distinguishable as unknown. Browsing performs no current-count request.
- Selecting one Studio starts parallel `/3/discover/movie?with_companies={id}` and `/3/discover/tv?with_companies={id}` requests. Each count independently represents checking, positive, zero, or quiet `Count unavailable`. Failure is sanitized and does not block an otherwise supported source. Successful results use the established five-minute, 40-entry bounded in-memory cache; counts load automatically with no manual Retry or Refresh, requests time out/abort, and stale generations cannot update a changed or closed selection.
- The Worker accepts only those two exact paths with exactly one canonical positive safe-integer `with_companies` parameter. Missing, duplicate, malformed, zero, signed, fractional, unsafe, unrelated, and extra filters fail before fetch. The fixed upstream host, browser-origin rules, authentication, CORS, successful response caching, API-key stripping on already-allowed requests, and sanitized errors remain intact.
- Stable V1 Company export proves the Movie shape. Retained current-client resolver evidence additionally records native `COMPANY` forcing its numeric `tmdbId` into `with_companies` for the selected Movie or TV endpoint, and the current sort matrix proves pass-through plus UI exposure for the four correct-media TV values. The all-native fixture now retains `COMPANY/MOVIE` and `COMPANY/TV` with the same ID, empty filters, authoritative `sources`, empty `catalogSources`, and media-correct Recent values; contract/construction/serializer/edit tests round-trip both.
- One compact no-icon semantic sort choice maps Popular to `popularity.desc`, Recent to Movie `primary_release_date.desc` or TV `first_air_date.desc`, Top rated to `vote_average.desc`, and Most voted to `vote_count.desc`. Only the selected pill's description is shown; Popular remains default and counts do not depend on sort.
- Same-folder Studio duplicates are marked inline as **Already added** and summarized by one compact outcome sentence. Normal Add remains visually dominant and commits only missing physical identities, while a quieter folder-and-identity-bound **Add all anyway** override permits the exact configured set after an immediate duplicate recheck. Elsewhere matches remain informational in a distinct teal panel: up to three unique physical locations render as `Folder · in Collection`, followed by `+ N more`; blank/invisible titles use UI-only **Hidden folder** / **Hidden collection** fallbacks.
- Duplicate identity remains media-specific. A same-folder Movie match disables only Studio Movie and a TV match disables only Studio Series; matches elsewhere are informational. The application service revalidates the selected folder and duplicates immediately before one atomic controller batch, appends supported missing drafts in Movie-then-Series order, preserves folder presentation and unrelated data, and focuses/announces the first created source.
- Complete native `COMPANY/MOVIE` and `COMPANY/TV` sources expose preservation-safe physical Edit. Company identity, provider, type, media, and filters remain fixed; display title and sort are difference-only fields. Cancel/unchanged Save make no mutation and untouched unusual imported sorts remain exact. The shared editor conflict/duplicate/round-trip contract applies and no source insertion occurs.
- Entity search fields for Franchise, People, and Studios use the ordinary native in-field `type="search"` clear affordance without an adjacent custom action. Studio logo frames clip to padded bounds with centered contain sizing; missing/load-failed images show text only. Studio and People Configure cards follow the Collection convention of an upper-right canonical TMDB entity link.
- Desktop primary hierarchy cards support double-click into existing Collection, Folder, or supported Source editors. Single click remains selection; nested interactive descendants, drag handles, and sibling action menus cannot trigger the shortcut, and explicit menu Edit remains the mobile route.
- After the reviewed narrow Worker route change was deployed manually, bounded desktop and same-network `/__tmdb_preview__/` acceptance returned Pixar (`3`) **136 Movies / 15 Series** and Warner Bros. Pictures (`174`) **3,123 Movies / 8 Series**; negative route checks passed. The real unavailable state remains validated and non-blocking. Explicit `TMDB_STUDIO_MOCK_COUNTS=1` remains a development-only presentation-mechanics aid, is disabled for production builds, and supplies synthetic results; it is not mounted/integration/end-to-end, owner-acceptance, or live-behaviour evidence. Those checks use the production integration path under [`docs/TESTING.md`](../TESTING.md).
- Studio logos/fallbacks and all count/catalogue metadata remain UI-only. No source artwork field, count field, projection-only field, global pre-cache, checked-in catalogue-data edit, dependency, or Network/Genre/Advanced Discover implementation is introduced. The final regression pass only makes existing Company compact writers retain explicit zero values on a future ordinary refresh; it does not dispatch that refresh or another deployment.

### Studio hierarchy creation — issue #124

**Merged through issue [#124](https://github.com/davecollections/tmdb-id-lookup/issues/124) / PR [#125](https://github.com/davecollections/tmdb-id-lookup/pull/125), with its narrow Worker contract manually deployed and live validated on 2026-08-19:** Studios is registered in both shared hierarchy scopes while selected-folder Add Source retains its merged issue #92 behavior. Overall V2 release status remains governed by the separate release and noindex boundary. The focused contract is documented in [`BUILDER_STUDIOS.md`](./BUILDER_STUDIOS.md).

- Select reuses the checked-in Company catalogue and its exact `Movie Count: N` / `Movie Count: Unknown` Search wording. Its hierarchy-only Movie count filter offers All, Exclude 0, 10+, 50+, 100+, and 500+ before paging; Unknown remains in All/Exclude 0 only. The checked-in filter makes no API request and selected-folder Add Source keeps its existing hide-zero control. Full-card native checkboxes build an insertion-ordered, uncapped selection; a notice begins at 50. The selected disclosure provides Remove only, while Search cards and Select contain no Preview.
- One batch configuration selects Movies, Series, or Movies + Series and one evidenced semantic sort; Movies and Popular are defaults. Configure directly lists every selected Studio with identity, catalogue/learned counts, reactive placement, Preview, and accessible remove. Last removal stays on a calm empty Configure state and disables Appearance progression; remove/reselect order stays canonical. Generated physical source titles are the generic `Movies` and `Series`, not the selected-folder Add Source naming. There is no Automatic or per-Studio configuration.
- Studio title Preview is explicit only and enters through an always-visible Configure-row action after media and sort are defined. Both opens Movies first and does not request Series until that tab is opened. The extracted structural `NestedPreviewDialog` is shared with Franchise Preview for portal/layer/dialog/focus/Close/Escape behavior while provider and data state remain family-owned. The final Appearance stage contains presentation and useful totals only, with no Studio rows, Preview/remove actions, counts, or routine placement review.
- One Company Discover response supplies `total_results` plus the ordered first result page. Normalization retains upstream order and stores title/name, date/year, poster path, and media. The shared bounded cache defaults to five minutes, 40 success-only LRU-style entries, and Company ID + media + concrete sort identity. Failures and aborts are not cached; successful zero results are. Presentation slices to 10 desktop or 5 at 520px and below without changing cache identity.
- Search Movie Count stays catalogue-backed and is never mutated by Preview. Only a successful explicit Series Preview adds transient Company/media Series count knowledge; it survives sort changes, is replaced by newer successful totals including zero, and never enters plan identity, duplicate classification, stale comparison, apply, or serialization.
- Browse, selection, Configure, Appearance, planning, revalidation, and apply make zero automatic title/count Discover requests. Plans contain all required source and artwork values before revalidation. One atomic controller batch applies eligible ordinary nodes, late failure rolls back, and New Folder preserves the parent apart from its intended ordered folder insertion.
- Logical Company placement omits both partial and complete matches in the destination collection without a hierarchy override. Elsewhere matches remain informational and addable. New Studio folders are fixed Landscape with one workspace runtime load, approved Company artwork first, checked-in `w500` logo second, then empty image plus `🎬`; generated titles default to Show everywhere.
- At the issue #124 checkpoint, the manually deployed Worker admitted the optional media-specific Company `sort_by` allowlist while retaining no-sort Company and Network paths, fixed upstream/auth/CORS/error behavior, and the origin-free service-token restriction. Issue #126 later preserved Network no-sort compatibility while adding its separate four-value TV allowlist. The exact Studio source and complete live acceptance/rejection matrix were validated on 2026-08-19 after separate owner authorization. Synthetic local Preview data cannot replace production-path integration or owner evidence; Worker deployment remains independent of Builder publication.
- The implementation reuses the existing native `COMPANY/MOVIE` and `COMPANY/TV` source evidence and does not claim a current-client Studio hierarchy result, new Nuvio contract, asset write, dependency, lockfile, V1, or catalogue-data change.

### Network source creation — issue #98

**Merged through issue [#98](https://github.com/davecollections/tmdb-id-lookup/issues/98) / PR [#99](https://github.com/davecollections/tmdb-id-lookup/pull/99) with owner acceptance complete, 2026-08-09:** the selected-folder Add Source chooser now includes **Networks** and creates exactly one native TMDB `NETWORK/TV` Series source. The focused contract is documented in [`BUILDER_NETWORKS.md`](./BUILDER_NETWORKS.md).

- Network discovery lazily adapts the ordinary checked-in TV Network catalogue. Empty input automatically browses deterministic A–Z results; typed hidden Best Match searches exact/prefix/contains name plus country code/full name/aliases and compact headquarters, while exact canonical positive integers select by TMDB ID. Results page by 20 and retain query/page/scroll/focus across Configure → Back.
- The legacy compact `t` field is dropped from the selected-folder Add Source projection and is absent from that flow's Network search text, ranking, filters, browse ordering, paging, cards, identity, construction, and serialization. Add Source cards show contained logo/text fallback, name, compact country/location, and quiet TMDB ID only; they never show or request a Series Count while browsing.
- Selecting one Network starts one abortable, stale-safe `/3/discover/tv?with_networks={id}` request with an approximately 12-second timeout. Successful non-negative safe integers, including zero, use the shared five-minute/40-entry bounded cache mechanics. Failure or malformed data renders quiet `Count unavailable`, exposes no Retry/Refresh, and never blocks Add or Edit Save.
- The tracked Worker code permits TV Discover with exactly one canonical `with_companies` or `with_networks` parameter and retains Movie Company-only behavior. Mixed, duplicate, missing, malformed, signed, zero, fractional, unsafe, unrelated, and extra filters fail closed. The reviewed source was manually deployed to `tmdb-id-lookup-proxy` on 2026-08-09. Bounded live acceptance returned ABC 2 → 1,616, SBS Australia 223 → 255, HBO 49 → 377, and Nine Network 66 → 354; existing Company 3 Movie/TV routes returned 136/15, representative rejected routes returned HTTP 403, no credential material leaked, and a production-built mock-disabled Builder matched the direct Worker results.
- Popular, Recent, Top rated, and Most voted map to `popularity.desc`, `first_air_date.desc`, `vote_average.desc`, and `vote_count.desc`. Popular is default. Construction strictly emits title, sort, positive `tmdbId`, empty filters, `tmdb`, `TV`, and `NETWORK`; it writes only authoritative `sources` and no native `catalogSources` projection.
- Duplicate identity is `tmdb|NETWORK|id|TV`. Destination matches warn and require a folder-and-identity-bound **Add anyway** decision followed by an immediate recheck; elsewhere matches are informational. One accepted submission uses the existing atomic controller path, creates one revision, focuses/announces the new source, and preserves raw/unknown/project data.
- Complete native Network Series sources use the existing physical-source editor, overflow-menu Edit, and safe desktop double-click. Display title and evidenced sort are difference-only edits; Network ID, `NETWORK`, `TV`, provider, filters, and raw identity remain fixed. Cached presentation, canonical TMDB Network link, and the same automatic quiet Series Count are UI-only.
- Shared extraction is deliberately limited to TMDB entity metadata/logo normalization, semantic sort presentation, elsewhere-location presentation, and Discover count request/cache mechanics. Network catalogue ranking, source identity/construction, flow navigation, and edit adapter remain explicit. No catalogue regeneration, writer/workflow change, dependency, artwork publication, collection/folder creation, multi-select, Genre, Advanced Discover, or recipe work is included.

### Network hierarchy creation — issue #126

**Closed/completed through issue [#126](https://github.com/davecollections/tmdb-id-lookup/issues/126) / merged [PR #127](https://github.com/davecollections/tmdb-id-lookup/pull/127) at `10a76aeffd351341321ab56658e69858fb85d39c`, with owner Worker deployment, live sorted-Preview validation, and owner review complete:** Networks extends the shared New Collection/New Folder launcher without changing selected-folder Add Source or physical Source Edit. The focused family contract is documented in [`BUILDER_NETWORKS.md`](./BUILDER_NETWORKS.md).

- Select reuses the checked-in catalogue and 20-row paging with zero live requests. A separate hierarchy-only projection preserves valid non-negative `t` as exact `Series Count: N`, including zero, while absence/invalidity becomes `Series Count: Unknown`; Add Source's public projection and count-free cards remain unchanged. The hierarchy-only filter is All, Exclude 0, 10+, 50+, 100+, and 500+ before paging. Unknown remains in All and Exclude 0 only. Filter changes reset page 1 without changing ordered selections, and there is no Most-series sort.
- Exact-ID selection is insertion-ordered, removable, uncapped, and retained through Back. An informational notice begins at 50. Configure applies one shared TV sort—Popular, Recent, Top rated, or Most voted—to generic `Series` sources and makes no automatic Discover request.
- Explicit Configure-row Preview makes one Network-and-sort TV Discover request. Before success, one visible `Series Count:` line shows the catalogue snapshot or Unknown; success replaces that same value with transient `total_results`, failure retains the snapshot, and sort changes may retain the latest successful value. The live value never enters plan data, identity, duplicate logic, stale comparison, or serialization. The response also supplies the upstream-ordered first page. The single Series poster grid renders at most 10 items at every viewport, omits posterless rows, backfills only within that page, and uses the exact empty state **No posters available.** The narrow requester/cache mechanics are shared with Studio behind thin family adapters; successful responses, including zero, use the five-minute/40-entry bound, while failures, malformed responses, timeouts, aborts, and stale completions are never cached.
- The tracked Worker change admits only one canonical positive `with_networks` and zero or one exact Network `sort_by` on `/3/discover/tv`; duplicate, extra, mixed, Movie, malformed, and unsupported-sort routes fail closed. It does not broaden Company, host, auth, CORS, API-key, or sanitized-error behavior, and Discover remains unavailable through the origin-free service-token path. On 2026-08-20, the owner manually deployed the complete reviewed Worker as version `f6bee241-afef-447f-b8f9-3d4b8da460cf` from recorded deployment-handoff source SHA-256 `612955AD3ECCEF16E12E05ABA6B672B0AD68BA825F13419FFA2A0A9346706AD4`; the post-deployment live sorted-Preview gate passed through the production integration path. The merged tracked Worker Git blob is separately `ceb37bb3711a43d6f25508a98943ce71b53baec2`. Deployment bytes, Git blob identity, and CRLF-transformed Windows working-tree bytes must not be conflated, and these recorded identities do not independently establish byte equivalence.
- New Collection defaults to `Networks`, visible Collection title, Tabs, Show All on, and Pin off. Generated folders use the canonical Network name, Show everywhere titles, and Poster by default. The shared Poster/Landscape choice resolves only the requested runtime orientation, then checked-in `w500` logo, then empty image plus 📺. The batch resolves before planning; plan creation, revalidation, and apply perform no artwork I/O or per-item customization.
- Logical Network placement uses only Ready, Already in this collection, and Exists elsewhere; the single-source family has no Partial state. A destination match is omitted in New Folder with no override, elsewhere remains informational and addable, and every New Collection project match is informational. Eligible bundles keep selected order, use the separate hierarchy `Series` constructor without weakening Add Source validation, and apply through one stale-revalidated atomic controller batch with full rollback.
- Existing native `NETWORK/TV` evidence is reused without a new current-client hierarchy import/runtime/export claim. The known client divergence remains unresolved: NuvioTV adds `with_status=0|3|4` and a null-to-current-date upper bound for native Network sources while NuvioMobile does not. Catalogue data/writers, V1, dependencies, and unrelated families remain unchanged. Issue #126 did not change the Pages publication mechanism, but merging PR #127 successfully triggered automatic Pages run [32326423777](https://github.com/davecollections/tmdb-id-lookup/actions/runs/32326423777); no manual frontend deployment or merge-time Worker redeployment occurred.

### Internal DISCOVER Core — issue #100

**Merged through issue [#100](https://github.com/davecollections/tmdb-id-lookup/issues/100) / PR #101, 2026-08-09:** V2 now has one framework-independent, non-user-facing native DISCOVER contract. The durable construction, descriptor, sort, comparison, classification, identity, preservation, and evolvability rules are documented in [`BUILDER_DISCOVER_CORE.md`](./BUILDER_DISCOVER_CORE.md).

- Canonical construction emits explicit null `tmdbId`, one supported Movie/TV sort, meaningful recognized filters only, `tmdb`/`DISCOVER`, and no native `catalogSources` projection. Movie `withNetworks` remains imported-preservable but is rejected as client-divergent canonical creation.
- Comparison is detached from serialization: proven pure comma-AND and pipe-OR ID expressions may normalize token order, provider-region and missing-sort defaults are effective-only, unknown JSON participates conservatively, and unsafe data returns non-comparable rather than a collision-prone key. Stored imports are never rewritten from comparison output.
- Functional identity includes provider, DISCOVER, media, effective sort, comparison filters, and a non-null imported `tmdbId` discriminator; display title is excluded. Existing Franchise, People, Studio, and Network identities are unchanged.
- No Add Source, filter, Source Edit, Streaming, Genre, Decade, Advanced Discover, Worker, API, artwork, count, provider-data, or source-led creation behavior is introduced.

**Updated through issue [#106](https://github.com/davecollections/tmdb-id-lookup/issues/106), 2026-08-12:** the Core now recognizes the current 18-field persisted contract. `withoutGenres`, `withoutKeywords`, `withoutCompanies`, and `withoutWatchProviders` use the existing strict ID-expression validator for canonical construction but remain exact/order-sensitive for comparison and are not added to expression normalization. Effective Watch Region now activates for included or excluded provider filters, uses comparison-only `US` when active and absent/blank, and remains inactive when neither provider field is meaningful. The shared importer/overlay/serializer architecture consumes the expanded known-field inventory without production edits, future unknown filter data remains exact across known edits and serialization cycles, and simple Streaming Source Edit remains restricted to `watchRegion` plus `withWatchProviders`.

### Streaming Services source creation — issue #104

**Merged through issue [#104](https://github.com/davecollections/tmdb-id-lookup/issues/104) / PR #105 after live Worker evidence and final hands-on owner flow refinement, 2026-08-11:** Streaming Services is the fifth selected-folder Add Source mode and the first user-facing DISCOVER Core consumer. The focused contract is documented in [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md).

- The deployed tracked Worker narrowly allows `/3/watch/providers/regions`, `/movie`, and `/tv` only with one exact `language=en-US` parameter. Fixed upstream/auth, browser origins, GET/OPTIONS, credential stripping, errors, and cache headers remain unchanged; the Region-first refinement adds no route or deployment.
- Builder requests the three official response shapes together and transiently merges positive provider ID, trimmed name, optional safe logo, and independent Movie/TV regional maps. Successful catalogues remain workspace-memory-only; failures retry. Top-level global `display_priority` is discarded because no accepted consumer remains; regional map codes retain conservative availability and nullable ranking evidence.
- Region(s) is first with zero selected. Its compact **Common | A–Z** switch defaults to an intentionally curated 12-code Common convenience list resolved only from live known Regions; missing configured codes are omitted safely. A–Z remains the complete known TMDB catalogue, while search always covers that complete catalogue regardless of browse mode. Each accessible row toggles naturally; selection and exact user order survive view switches, filtering, and clearing search; deselect/reselect appends at the end. A persistent count and disabled-until-valid **Next** preserve explicit selection without a separate checkbox mode, ranking claim, locale/default assumption, or geolocation.
- Provider eligibility requires common Movies and/or Series availability across every selected region. One region defaults to 30 **Top providers** from that region's real TMDB priority, explicitly not popularity, beside A–Z. Multi-region uses eligible A–Z/search only with no synthetic priority. Provider remains single-select.
- Configure retains the four DISCOVER semantic sorts and includes compact generated-source review rather than a separate Review screen. Each region/media row can independently customize its physical-source name or restore `<Provider>, <REGION> - Movies` or `<Provider>, <REGION> - Series`; sort preserves names, media prunes only disappeared candidates, and incompatible Provider/Region changes cannot leak them. Candidate order is selected-region order then Movie before TV. Construction remains projection-free native `DISCOVER` with explicit null `tmdbId` and only `watchRegion`/`withWatchProviders`; the hardened insertion validator varies only valid Nuvio titles.
- Issue #162 later supersedes only newly generated standalone default names with `<Provider> Movies (<REGION>)` / `<Provider> Series (<REGION>)`, shared with explicit Source Edit default regeneration. Persisted and custom titles are never migrated; the issue #104 wording above remains historical evidence of that merged checkpoint.
- Candidate and imported-node comparison use `discoverSourceIdentity` and `discoverSourceNodeIdentity`. Current-folder candidates are handled independently across every region/media identity, elsewhere matches stay informational, partial bundles add only missing candidates by default, exact overrides remain folder/candidate-bound, and one existing controller batch commits the final N-source set atomically.
- Simple Streaming-shaped physical `DISCOVER` sources now use the existing Source Edit registry for name and four semantic sort choices only. Provider, Region, media, and filters stay fixed; recognition rejects compound providers, meaningful extra/unknown fields, malformed or non-comparable data. Provider presentation hydrates non-blockingly from the existing catalogue with `Provider <id>` fallback, while default naming appears only after reliable resolution. Sort-changing duplicate checks use full DISCOVER Core identity; title remains excluded. The global **About & Credits** modal centralizes TMDB and JustWatch-via-TMDB attribution; Streaming screens do not repeat it. The 9 Aug stripping result remains historical because the controlled 10 Aug retest preserved both provider filters. Issue #106 recognizes current exclusion fields in Core only; exclusion creation/editing UI, runtime selection, multi-provider selection, source-led creation, recipes, and broader Help remain deferred.

### Official Genre source creation — issue #110

**Merged through issue [#110](https://github.com/davecollections/tmdb-id-lookup/issues/110) / PR #111 with deterministic repository evidence and completed desktop/physical-iPhone owner acceptance, 2026-08-15:** Genres is the sixth selected-folder Add Source mode. The single entry point remains selected folder → Add Source → Genres; the destination decision lives inside Configure & review and does not restore a Folders-header action. Browse does not auto-focus Search, its narrow selection toolbar remains stable, mobile exclusion detail keeps a persistent Back-to-Genres header with root-only Done semantics, and neutral review rows use destination-aware shared status wording. The focused contract is documented in [`BUILDER_GENRES.md`](./BUILDER_GENRES.md).

- An immutable Builder projection has deterministic parity with the 19 Movie plus 16 TV official rows in `data/genres.csv`. Exact-name grouping produces 27 concepts and only Animation, Comedy, Crime, Documentary, Drama, Family, Mystery, and Western pair. Musicals and curated Lists are absent. `Action & Adventure`, `Sci-Fi & Fantasy`, and `War & Politics` remain standalone Series-only concepts; V1 semantic merge rules are not reproduced.
- Browse/search is local, count-free, multi-select, and order-preserving. A compact count plus **Select all** / **Clear all** replaces the chip cloud; all 27 concepts are accepted in A–Z Select-all order with no artificial 20-item Genre limit. Exact shared concepts use one global Movies/Series/Both choice and expand Movie then TV; the control is absent when every selected concept has fixed media. The complete catalogue expands to 35 ordered candidates. One semantic sort applies to the reviewed operation.
- The collapsed Advanced area owns global optional inclusive release-year bounds, rating bounds, nonnegative minimum votes, and named common language/country selectors. Genre exclusions are instead stored per included Genre, so selected Genres may exclude one another while each source's own-Genre exclusion remains disabled/rejected. Compilation translates only that included Genre's ordered exclusions per media and omits unavailable mappings. Add/Edit share controlled secondary surfaces: contained inert-underlay overlays on desktop and opaque sequential Genre-list → picker task surfaces on mobile, with one active scroll owner, footer suppression, Escape/**Done** focus restoration, and no nested modal.
- **Configure & review** preserves all Browse and configuration state through the standard header **Back** action; no duplicate **Change genres** action remains. One Genre is fixed to current-folder mode and is shown as non-removable final context. Two through six use removable pills, larger selections use removable disclosure rows, removing down to one resets destination safely, and adding back to multi retains current-folder as default. Simple reviews omit redundant count boxes while large and attention outcomes stay compact and visible. The current-folder path adds missing identities and preserves presentation byte-for-byte; its exact folder/candidate-bound **Add all anyway** remains. The multi-only alternate destination atomically creates one sibling `LANDSCAPE` folder per ready Genre, groups paired Movie/TV sources together, and uses the existing V1 wide artwork mapping with visible-title/🎬 fallback.
- Current-folder duplicates are folder-scoped. Folder-destination planning is collection-scoped per configured Genre: complete identity sets are **Already exists**, partial paired sets are **Partly exists**, and both are omitted without bulk override; occurrences only in another collection are informational.
- Every candidate is constructed and revalidated through DISCOVER Core with explicit null `tmdbId` and no native `catalogSources` projection. One `addSourcesToFolder` call safely handles all 35 current candidates, while one `createFoldersWithSources` call safely handles all 27 folders/35 sources. If folder mode starts from the exact empty, non-imported, generated-default `Untitled Folder` shape, that placeholder is removed in the same controller commit after final duplicate planning; renamed, restyled, populated, imported, customized, and no-new-folder cases preserve it. Both success paths commit one content revision; atomic failure retains project, content revision, and selection while recording a diagnostic snapshot. The narrow replacement is not generic folder merge/copy. The stale shared 20-bundle ceiling had People-UI provenance rather than a domain/serialization contract and was removed without replacing it with another arbitrary ceiling.
- Genre Source Edit recognizes one official media-correct Genre plus only the same losslessly representable approved filters. It owns display name, the four semantic sorts, and those filters; Genre ID and media remain fixed. Add and Edit share the same Advanced presentation and subviews. Partial dates, pipe/mixed exclusions, self-exclusion, unsupported filters, compound included Genres, malformed or unknown evidence, custom `tmdbId`, Lists, and unknown-rich shapes remain Delete-only.
- The production-generated sanitized fixture under [`issue-110-builder-genres/`](../../manual-tests/nuvio-clients/issue-110-builder-genres/) contains Comedy Movie/TV plus Action & Adventure TV with representative advanced filters in one existing folder. The offline checker proves deterministic canonical Builder output. No current-client V2 import/export result is claimed; that evidence is deliberately deferred until V2 exposes export and is not an unfinished #110 gate.

### Bounded Decades planning and atomic hierarchy foundation — issue #112

**Merged through issue [#112](https://github.com/davecollections/tmdb-id-lookup/issues/112) / PR #114, 2026-08-15:** the non-UI Decades foundation owns the initial eight ordered presets, open-ended 1950s & Earlier and Before 1950 semantics, complete calendar years/decades, injected current-year expansion, additive whole/year/Decade-level-Genre composition, canonical DISCOVER drafts, exact duplicate placement, transient hierarchy plans, and atomic multi-collection application. The focused contract is documented in [`BUILDER_DECADES.md`](./BUILDER_DECADES.md).

- The bounded ephemeral plan remains outside the persisted domain/import/serialize model and uses `planType`, not schema versioning. Regenerated plan/source comparison requires exact dense JSON-compatible structures, ignores object-key insertion order, and preserves authoritative array/source order. New Collection supports Movie, TV, default separate Both, and mixed Both layouts with actual `TABBED_GRID`/`ROWS` values; New Folder captures an existing collection, inherits presentation for preview only, and reuses `createFoldersWithSources`.
- One narrow `createCollectionsWithFoldersAndSources` controller operation constructs all candidate collections/folders/sources before one commit. Validation, internal-ID, Nuvio-ID, and later-bundle failures retain content, selection, and content revision.
- Existing DISCOVER Core and the issue #110 official Genre catalogue remain authoritative. Approved Advanced filters exclude arbitrary dates and broad Discover. Genre breakdown is Decade-level only, with ordinary whole/year exclusions and per-included-Genre exclusions compiled per physical media. A Genre unavailable for the chosen single media rejects the complete configuration; Both accepts Movie-only or TV-only concepts and generates only their supported physical source.
- Canonical Decade classification is title-independent and fail-closed, including a strict `tmdbId` requirement of `null` or absent. The Genre recognizer explicitly declines canonical Decade-period recipes so future editor ownership does not depend on registry order.
- The deterministic maximum is 452 sources: 16 whole, 156 individual-period, and 280 Decade-level Genre sources across 2 collections/16 folders in Separate mode. It applies in one revision without a Year × Genre cross-product or arbitrary controller ceiling.
- No launcher, wizard, modal, presentation control, preview UI, visible Decade editor, blank-creation behavior change, V1/Worker/export change, live request, asset change, dependency, or lockfile change is included. Those visible Decades behaviors were reserved for issue #113 and are documented in the following merged section.

### Visible Decades creation and canonical source editing — issue #113

**Merged through issue [#113](https://github.com/davecollections/tmdb-id-lookup/issues/113) / PR #115 after completed desktop and physical-phone owner review on 2026-08-16:** all existing New Collection and New Folder controls now share one contextual **What would you like to create?** launcher. Its ordered creation registry keeps **Blank** first and **Decades** second. Blank immediately invokes the unchanged draft helpers and preserves their existing naming, presentation, navigation, selection, scrolling, and revision behavior without a naming wizard or review.

- Decades uses Presets → Configure Decades → Review & Appearance. Presets stay in catalogue order with dynamic Select all/Clear all. Configure owns content decisions only: editable selected Decades, reused compact Media and Sort pills, conditional Both-only structure, additive Decade overview/Individual years/Genre breakdown, contextual Genre customisation, applicable always-visible Ordering, and collapsed Advanced source filters. Visible defaults are Both media, Individual years on, Decade overview off, Genre breakdown off, Separate collections for New Collection, and **Newest Decades, Oldest Years** Display order; at least one content type is required. The visible current-Decade expansion remains 2020–2029 while #112 retains its lower-level modes, and no future-year message is shown.
- Configure uses shared removable pills for small Decade selections and a bounded removable disclosure for large selections; removal prunes stale per-Decade Genre and exclusion state. Genre breakdown opens one reused official catalogue with All-selected and per-Decade contexts rather than permanently mounting it in the main scroll. Configure Genres and ordinary exclusions consume one shared responsive context/catalogue shell: a bounded two-pane desktop layout and history-aware mobile context root → detail → Back flow with common focus, scroll, safe-area, and breakpoint behavior. Their consumers retain only label/help copy, selection callbacks, and validation/status differences. Shared/per-Decade selections survive context switching and every selected Decade still requires at least one Genre. Ordinary exclusions have the same shared/per-Decade hierarchy; per-included-Genre exclusions remain inside Genre customisation. The planner deterministically owns those ordered keyed maps without persistence metadata or any Year × Genre product.
- One user-facing Display order maps to the retained lower-level Decade-folder and individual-year chronology fields through exactly three presets: **Newest Decades, Oldest Years** (default: newest/oldest), **Newest throughout** (newest/newest), and **Oldest throughout** (oldest/oldest). Source grouping remains independent and appears only where Both media physically coexist in a mixed physical folder, including New Folder but not Separate Movie/TV collections. Aggregate sources remain first; oldest-first 1950s begins Before 1950 then 1950–1959, while newest-first runs 1959–1950 then Before 1950. Every preset and both mixed grouping modes reorder the same source identities and counts. Single-media physical folders use `All 2000s`, `2000`, and `Horror`-style names, while mixed folders retain Movies/Series suffixes. Naming never changes DISCOVER identity or filters.
- Review & Appearance leads with only concrete count cards, then editable collection names or compact New Folder destination followed by one always-visible shared **Title options** section, directly visible **Layout**, **Folder options**, and **View folder details**. New Folder inherits parent collection presentation read-only, exposes no collection-title edit, and can edit folder visibility and presentation only. New Collection reuses the exact manual hidden-title switch: all generated name inputs are blank/disabled while hidden, each references one shared status message, the plan emits one U+200E per collection, and every visible draft returns when revealed. The existing three folder-title outcomes are shared in both scopes. Layout owns Tabs/Rows, All tab, and pin only; Folder options own Poster/Landscape only. Decade overview and All tab remain independent, with a quiet conditional explanation when overview + Tabs + All tab are all enabled. This review-only recomposition changes no Decades defaults, plan values, JSON, duplicate behavior, or atomic application; permanent appearance/order/year/source-name summary boxes remain absent.
- Future cover, hero/backdrop, title-logo, runtime, ownership, and orientation artwork controls are explicitly deferred. Review & Appearance remains extensible through its option sections, but #113 adds neither artwork requests nor placeholder UI.
- Apply submits the exact reviewed plan to the #112 revalidator. Any relevant project, destination, duplicate, or plan change keeps the project untouched and requires a new review; success commits the complete hierarchy once. Cancel and Back are mutation-free. The task surface uses the existing body/Visual Viewport lock, inert underlay, focus containment/restoration, no initial text autofocus, mobile full-height treatment, natural desktop sizing, and reduced-motion behavior.
- Canonical Decade sources are now owned by `decade-editor.js` before Genre. Source name, four media-correct semantic sorts, and approved rating/vote/language/country/Genre-exclusion fields are editable. Period dates, media, and an optional included official Genre are fixed and visible; arbitrary From/To years, identity pickers, counts, requests, and links are absent. Candidate reconstruction and full DISCOVER identity make structural drift, invalid or incompatible filters, duplicates, stale sessions, and unsupported shapes fail closed with no partial edit.
- Deterministic focused coverage and local in-app browser QA exercised desktop plus 360, 384, 393, 402, and 412px widths, exact Blank behavior, Both/Separate defaults, all three Display-order mappings and independent Source-grouping interactions, shared hidden-title accessibility/draft restoration, conditional controls, invalid-name recovery, exact plan totals, New Folder destination/inheritance, the borderless Genre-exclusion backdrop with bounded inner panel, canonical Decade editing, focus restoration, body lock/inertness, and horizontal-overflow absence. No live TMDB request or client import/export result is claimed. V1, Worker, `nuvio-assets`, dependencies, and lockfiles are unchanged.

### Preservation-safe native physical-source editing — issue #78

**Merged through issue [#78](https://github.com/davecollections/tmdb-id-lookup/issues/78) / PR [#79](https://github.com/davecollections/tmdb-id-lookup/pull/79) after focused deterministic repository validation, bounded local desktop/mobile browser QA, and completed owner Round 2 desktop/physical-iPhone review, 2026-08-04:** supported Movie Collection and People source cards expose an exact physical-source editor. Studio issue #92 / PR #93 and Network issue #98 / PR #99 later extended the same registry seam with display-name/sort editors for their complete physical sources; Streaming issue #104 / PR #105, official Genre issue #110 / PR #111, and canonical Decade issue #113 / PR #115 add their narrow fail-closed adapters without making generic DISCOVER editable. The focused contract and deliberately deferred client round trip are documented in [`BUILDER_SOURCE_EDITING.md`](./BUILDER_SOURCE_EDITING.md).

- A framework-independent registry resolves exact Movie Collection, People, Studio, Network, canonical Decade, official Genre, and simple Streaming source shapes to family adapters with owned draft, identity, validation, and minimal-patch rules outside the controller, importer, serializer, and domain. Unsupported native, addon, and opaque sources retain Delete only.
- Movie Collection edits title and, only after explicit picker activation, the TMDB collection ID. Provider `tmdb`, `COLLECTION`, `MOVIE`, sort, and filters remain fixed. Merely opening/cancelling stays offline. Selecting another franchise immediately makes its canonical TMDB name the draft title and primary readable label, with `TMDB collection <id>` secondary; typing may customize it and **Use selected collection name** restores it.
- People keeps the person ID fixed while the physical source may change among `PERSON/MOVIE`, `PERSON/TV`, `DIRECTOR/MOVIE`, and `DIRECTOR/TV`. Approved default titles auto-follow role/media until manual typing makes them custom; **Use default title** restores session-local auto-management. The editor also owns `sortBy` only for stable-v1 `Popular`, media-specific `Recent`, and `Top rated`; untouched imported values preserve exact casing/value.
- People opens immediately from stored data, then reuses the shared issue #74 successful person-details cache or performs one bounded non-blocking combined-credit request. All four distinct cast/Director counts appear without refetching on choice changes; failure is sanitized and retryable, Save stays enabled, and no folder artwork request occurs.
- Title remains difference-only. Untouched missing, null, unusual, and custom imported values remain untouched; deliberate or approved automatic title intent must satisfy the existing Nuvio title rule, including the established intentional U+200E value. A real save patches only changed owned fields through exactly one `controller.updateNode` call and one revision. Cancel, unchanged save, failure, duplicate, and conflict paths make zero mutations.
- The edit session binds immutable project reference, exact collection/folder/source IDs, physical source index/category, adapter, and original identity. Save re-resolves and refuses deleted, moved, reordered, reclassified, identity-changed, or otherwise stale state rather than merging it.
- Duplicate identity is folder-local, excludes the edited physical source, and blocks only a changed identity. Existing imported duplicate identities therefore do not prevent a title-only or sort-only correction. Editing has no duplicate override. Duplicate, validation, and conflict failures use a prominent bordered alert; failed Save scrolls/focuses it and retains the draft.
- Existing source/folder order, raw IDs, unknown/community fields, addon projections, null/default fields, untouched sort/filters, presentation, compact/verbose representation, and unrelated sources survive deterministic and second-cycle checks. Builder internal/edit/count/title-mode state remains absent from serialization.
- The dedicated portalled modal uses natural content height on desktop/tablet, capped by the viewport with the action footer attached and one content scroll region only when needed. Below 900px it retains the established full-height surface, inert underlay, body lock, Visual Viewport geometry, sticky header/footer, safe areas, focus trap, Escape/Cancel, trigger restoration, alert focus, and mobile single-column patterns. Count resolution does not switch sizing modes. Success retains selection, returns focus to the exact source card, and uses a polite source-edit status.
- Add Source Collection Review and People Configure now turn only validated positive numeric collection/person IDs into canonical `themoviedb.org` entity links. The shared allowlist excludes Discover, watch providers, internal/unsupported types, and malformed IDs; links open in a new tab with `noopener noreferrer`, visible external indication, meaningful accessible names, keyboard focus styling, and long-name mobile wrapping.
- The sanitized manual package covers Collection identity/canonical-title change, People automatic-title/role/media/sort/count evidence, separate custom-title preservation, and one no-op cycle. Dave completed Round 2 desktop and physical-iPhone review and confirmed mobile passed; no device/version details were supplied or inferred. Because V2 does not yet expose export, a complete current-client V2 edit/export round trip is deliberately deferred until export exists. No Nuvio client result is claimed for that round trip, and the deferral is not a blocker or unfinished pre-merge gate.

## 12. Shared artwork runtime foundation — issue #45

**Confirmed from the final `davecollections/nuvio-assets` publication handover and current published schema, and confirmed by repository tests — issue [#45](https://github.com/davecollections/tmdb-id-lookup/issues/45), 2026-07-21:** a pure shared runtime lookup client now establishes one future artwork resolution contract for v1 and the builder without changing either consumer yet. The focused contract and API documentation is in [`ARTWORK_RUNTIME.md`](./ARTWORK_RUNTIME.md).

- The public runtime is read from `https://raw.githubusercontent.com/davecollections/nuvio-assets/main/assets/collection_covers/runtime-lookup.json`; the application does not hard-code its current fingerprint, file SHA, counts, or representative IDs.
- Exact positive TMDB numeric IDs are authoritative only inside the explicit `companies`, `networks`, or `people` map selected by `company`, `network`, or `person`. Names and aliases are display metadata, ID spaces remain separate, and the resolver never scans other maps.
- Company and network support landscape 1200×675 WebP only. People support landscape 1200×675 WebP and poster 1000×1500 WebP. Unsupported orientations return an expected result and are never substituted, stretched, or cropped.
- Repository-relative published paths resolve beneath a configurable base URL. Every ready URL appends `?v=` plus the first 12 characters of that orientation's lowercase SHA-256.
- Only globally published data and entries with `status: "published"` and `reviewRequired: false` are safe for automatic use. A published `fallbackUsed: true` record remains approved; the resolver propagates the flag rather than replacing the asset.
- A missing typed key returns `missing` without guessing a numeric path, searching another map, or substituting a TMDB image. Malformed, unpublished, or unsafe data throws a typed error and cannot be mistaken for absence.
- The loader uses injectable fetch, validates the minimum complete consumer contract, deduplicates simultaneous loads, caches only successful data in memory per client, and permits retry after failure. Narrow resolver/cache unit tests use injected synthetic fixtures. Mounted, integration, owner-review, and live-behaviour checks that exercise the runtime follow [`docs/TESTING.md`](../TESTING.md) and use the approved production path; the canonical Network mounted suite requests the live runtime and real image resources.
- The browser needs no write credential, login, secret, or personal data. On-demand artwork requests and publication remain a deferred workflow outside browser code.
- Persistent browser caching, refresh intervals, manual refresh, and multi-tab behaviour remain separate later product decisions.
- `js/artwork-runtime.mjs` is deployed with the existing v1 `js/` tree and supports a thin v1 module adapter plus a future direct Vite import. Issue #45 itself did not change any consumer.

### V1 company and network artwork migration — issue #46

**Confirmed by repository tests, owner testing, and live development smoke verification — issue [#46](https://github.com/davecollections/tmdb-id-lookup/issues/46), refined 2026-07-22:** v1 company and network exports consume the shared published runtime and degrade automatically without changing v2 or people exports.

- `js/artwork-runtime-v1.mjs` creates one shared client per page and exposes only explicit company/network landscape batch resolution to the classic scripts. Module loading is harmless; runtime JSON loading remains lazy until a company/network export modal prepares its payload.
- Ready published artwork supplies the exact SHA-versioned runtime URL. `fallbackUsed: true` remains approved published artwork and always wins over the cached TMDB logo.
- Missing or unexpected non-ready results next use the selected cached entity's standard `w500` TMDB `logo_path` URL without a new API request and keep the title visible. Entities with neither image use an empty `coverImageUrl`, visible selected cached name, and 🎬/📺 fallback. This TMDB fallback is interim and remains outside the shared runtime.
- Runtime load or validation failure logs one concise warning and prepares the same valid TMDB-or-emoji fallback output. Copy/Download are disabled only during transient preparation; there is no technical count/error panel, artwork toggle, or manual retry loop.
- Prepared export caching preserves Copy/Download JSON and ID parity for unchanged selection/settings, deduplicates concurrent preparation, and invalidates on relevant changes. Company remains `COMPANY/MOVIE`; network remains `NETWORK/TV`; folder and preset order remain stable.
- The borrowed v1 network focus-GIF option, maps, URLs, and LuckyNumbers claims/credit were removed. Exported `focusGifUrl` remains empty and `focusGifEnabled` remains false; `focusGlowEnabled` and builder import/preservation support are unchanged.
- At the issue #46 checkpoint, ordinary v1 company/network lookup-table logos remained TMDB thumbnails, People runtime migration was pending, and the Builder did not consume the runtime. Later issue #118 moved active V2 People to its dedicated manifest, issue #124 added V2 Company Landscape consumption, and issue #126 added V2 Network Poster/Landscape consumption; v1 lookup thumbnails remain unchanged.

### Strict artwork runtime v1/v2 resolver compatibility — issue #55

**Confirmed by repository tests against the separately reviewed assets-side schema-v2 contract — issue [#55](https://github.com/davecollections/tmdb-id-lookup/issues/55), 2026-07-26:** the shared consumer resolver now accepts exactly numeric runtime schema versions 1 and 2. At the issue #55 merge checkpoint, the live `nuvio-assets` runtime was still schemaVersion 1.

- Schema v1 remains unchanged: Company and Network support Landscape only, while Person supports Landscape and Poster.
- Schema v2 preserves those orientations and adds required Network Poster. Every v2 Network entry must contain both Landscape and Poster. Company Poster remains unsupported in both versions.
- Required artwork paths must exactly match the entity family, entry ID, and orientation: Company and Network Landscape retain their existing paths; Network Poster uses `assets/collection_covers/networks/poster/{id}.webp`; Person retains separate `people/landscape/` and `people/poster/` paths.
- Missing required orientation data, forbidden orientation data, exact path mismatches, malformed schema versions, and non-lowercase or non-64-character SHA-256 values remain hard validation failures with stable resolver error codes. A valid unsupported orientation request remains the non-error `unsupported-orientation` result.
- Ready URLs retain `?v=` plus the first 12 characters of the exact orientation SHA-256. Existing schema-v1 ready, missing, unsupported, cache, retry, and result-shape behaviour remains unchanged.
- `js/artwork-runtime-v1.mjs` remains company/network Landscape-only with the same public batch interface. Tests prove that it consumes both v1 and test-only v2 lookups without selecting Network Poster.
- Cached company/network exports remain byte-identical between equivalent v1 and test-only v2 lookups, preserve configured nonblank backdrop artwork and existing fallback order, and require no production exporter change.
- At that checkpoint, runtime v2 had not been published by issue #55; its tests used explicit test-only v2 fixtures, and assets publication still required its own review, push, merge, and atomic publication process. Network Poster was not exposed in Builder UI, source creation, Search/Add, templates, recipes, or stable v1 exports.

### Live artwork runtime v2 publication — assets PR #3

**Confirmed by the completed assets publication and bounded live verification — assets [PR #3](https://github.com/davecollections/nuvio-assets/pull/3) and TMDB issue [#57](https://github.com/davecollections/tmdb-id-lookup/issues/57), 2026-07-26:** runtime schemaVersion 2 is now publicly live with `status: "published"`.

- All 572 Network entries contain published Landscape and required Poster data. Network Landscape paths remain `assets/collection_covers/networks/{id}.webp`; Network Poster paths use `assets/collection_covers/networks/poster/{id}.webp`.
- Company retains Landscape only and Company Poster remains unsupported. Person retains both Landscape and Poster.
- The shared TMDB resolver passed live validation and representative Company, Network and Person resolution against the published runtime. The external v1 bridge also passed live verification, remains Company/Network Landscape-only, does not select Network Poster, and continues to reject Person.
- At this 2026-07-26 publication checkpoint, Builder UI and source creation did not consume Network Poster. Issue #126 later added the batch-safe Network hierarchy Poster/Landscape choice; selected-folder Add Source, templates, recipes, per-item/raw artwork controls, and stable v1 company/network exports remain outside that change or Landscape-only as applicable.
- The release is `studio-network-posters-v2-2026-07-26`, published at `2026-07-26T03:19:39.353Z`. It contains 1,820 companies, 572 networks and 817 people: 3,209 entities and 4,598 assets.
- The runtime SHA-256 is `3b04e76eec24922c59404712a46245ff9fd8da1c7c1f508c19f6f69d4884f4af`, with fingerprint `900b25771f70365218754af18588ed1212e7669903027795d613eb427c143c58`.
- The studio/network manifest SHA-256 is `9c214428194d24653892177919f60d3e070b6f5d9491aac4bd70045bae5c7079`, with fingerprint `b1460d79e4ecc73a836676c85a1294bd424af4d58c69014e352073b830d4098c`.
- Publication commit [`815c0d5ada61c88a8f681cd12edaa8932ea320e4`](https://github.com/davecollections/nuvio-assets/commit/815c0d5ada61c88a8f681cd12edaa8932ea320e4) was merged as [`d34560a06469ce13af6fe1a3a5b299ffb3748560`](https://github.com/davecollections/nuvio-assets/commit/d34560a06469ce13af6fe1a3a5b299ffb3748560). These release values are recorded evidence rather than hardcoded consumer rules.

## 13. TMDB Discover compatibility audit — issue #47

**Confirmed from official TMDB documentation/OAS, pinned NuvioTV/NuvioMobile source, and repository tests — issue [#47](https://github.com/davecollections/tmdb-id-lookup/issues/47), 2026-07-23:** the Discover compatibility boundary is now normalized without expanding production schema or UI.

This section is dated historical evidence for the July 14-field client snapshots and fixtures. Issue #106 supersedes only the current persisted-contract and Builder-known-field conclusions with the 18-field contract; it does not rewrite the historical matrix, fixture bytes, owner results, or build-scoped counts below.

- Official inventories contain 38 Movie and 33 TV parameters, including request controls and `sort_by`; filter/context subsets are 35 and 29.
- At the issue #47 checkpoint, Nuvio collection JSON had 14 common filter fields, 0 TV-only fields, and 0 Mobile-only fields. Static request construction mapped 13 fields to official Movie parameters and all 14 to official TV parameters.
- At that checkpoint, Movie had 22 and TV had 15 official filter/context parameters that were not independently controllable through the reviewed JSON contract. Automatic localization/pagination and hidden provider monetization behavior remained distinct from user fields.
- Both pinned July clients accepted raw `sortBy`; all 14 official Movie and 12 official TV values could reach the correct endpoint in code, but only four per media were offered in the reviewed client editors. Owner evidence covered the official S1/S2/S3/S4C/S5/S6C rows on two specific builds; other official sorts and current-client parity remained pending.
- Owner-supplied Windows editor screenshots from that checkpoint visually confirmed the same 14 fields, the four visible sort choices, the Movie-visible “For series only” Network IDs helper, and explicit genre/provider comma-AND and pipe-OR help. The exact app version was not captured, so this is UI-only historical evidence and does not change any static or controlled-result count; language/country comma placeholders remain non-authoritative.
- Owner-controlled 2026-07-23 testing covered 29/29 sources on Nuvio Desktop `0.1.14-alpha` (14), Windows 11 Home 25H2 build `26200.8875`, and 29/29 on retained official iOS `1.2.23` (96). The retained official build was previously installed before distribution was removed, with no sideload; it is frozen historical evidence rather than current/future NuvioMobile proof. NuvioTV remains 0/29 pending because a practical device run was unavailable.
- Controlled pairs visibly distinguished M2/M1, T2/T1, W2/W1, genre AND/OR, keyword AND/OR, and provider AND/OR on both tested builds. S3 `original_title.asc` worked on retained iOS but produced an endless spinner on Desktop alpha; date aliases matched correct-media comparators; S7/S8 matched the popularity baseline without identifying the fallback layer.
- The earlier unpaired Shark Movies observation remains valid at its narrower unknown-client/version scope. A3/A4 now add build-scoped paired keyword evidence, but neither observation becomes universal current-both-client proof.
- NuvioTV alone adds `with_status=0|3|4` and a null-to-current-date upper bound for native NETWORK sources. Mobile alone sends Movie `withNetworks`; NuvioTV omits it.
- NuvioTV typed persistence and Desktop alpha import/export drop unknown filter keys. Pinned current NuvioMobile source accepts them during decode but loses the entire unknown nested portion through a shallow `filters` overlay. Retained official iOS `1.2.23` (96) instead preserved U1–U6 in Copy/Export without visibly applying them; preservation does not prove forwarding. The builder preserves unknown imported filter keys on unrelated edits, but that does not make them usable or durable in Nuvio.
- At the issue #47 checkpoint, the builder recognized the same 14 fields, validated only a plain filter object, preserved raw imported data through its overlay, and exposed no source/filter editing UI. Issue #106 now recognizes 18 persisted fields through that same preservation architecture; the July audit conclusion remains historical.
- The manual package contains a deterministic combined fixture that is the exact ordered concatenation of four retained component collections: 4 collections, 4 folders, 29 sources total, 19 essential. A generated presentation-only variant retains the same collection/source objects and order in 29 one-source folders for clients that import without multiple source tabs. The alternate is recorded separately and does not double the 29-source audit count.
- Desktop export preserved recognized fields/order, normalized structure/default/null fields, removed U1–U6, and retained raw sorts. Retained iOS Copy/Export preserved all unknown/provider/raw/alias/unofficial/invalid values on both fixture shapes without source/folder loss.
- A Nuvio.tv account-management profile export removed provider/region and U1–U6 fields, changed S2/S3/S5/S8 sorts, retained S4/S4C/S6/S6C/S7, and added structural/default fields. The responsible layer was not isolated, so this remains pipeline evidence rather than attribution to iOS sync, account storage, web normalization, or export serialization.
- Direct TMDB evidence remains pending because no local bearer token was available. The offline plan contains exactly 60 bounded requests at its hard cap, including exact US/AU provider-8 effective-query shapes and the required all-monetization union on every provider case; no live request is part of repository checks.

## 14. V2 collection compatibility regression corpus — issue #49

**Confirmed by repository tests — issue [#49](https://github.com/davecollections/tmdb-id-lookup/issues/49), 2026-07-24:** the builder now has a small, manifest-driven, file-backed compatibility corpus that connects the existing focused import, domain, migration, serialization, validation, ID, and controller contracts without changing production modules.

- Eight manifest entries separate canonical, preservation, identity, migration, and invalid pipelines and record counts, exact traversal/projection order, diagnostics, assertion mode, field policy, coverage tags, and references to existing Discover and migration evidence.
- The compact canonical profile covers all seven supported native TMDB types across the evidence-backed Movie/TV combinations plus basic, genre, multiple, and duplicate-identity addon projections in two ordered collections, three folders, and fifteen sources.
- The comprehensive preservation profile combines native, addon, imported Trakt, and opaque/community evidence across two collections, three folders, and six sources. It covers presentation/artwork fields, raw-only collection/source fields, unknown fields at every supported preservation level, missing/null/empty/false/zero distinctions, Discover-filter replacement, and stable second-cycle serialization.
- File-backed identity cases distinguish permissive direct import from deterministic controller repair of missing, invalid, whitespace-padded, non-string, and duplicate collection/folder IDs. Builder-only internal IDs remain unique and absent from output, while serializer-required ID/title diagnostics stay explicit.
- A controller removal/insertion case proves stable unaffected-source order, removal of the deleted source and its raw evidence, and no transfer of that evidence to the inserted source or projection.
- The corpus reuses the issue #37/#38 projection-only addon migration and Desktop artifacts, asserting deterministic promotion order, exact `"None"` to `null` normalization, unknown projection preservation, and migration idempotence. It also reuses the issue #31 invalid fixtures instead of duplicating them.
- Ten focused tests validate the manifest, fixture coverage, taxonomy, safety, Pages exclusion, exact/semantic/targeted policies, round trips, migration, and validation diagnostics. No production module, dependency, v1 runtime, Worker, route, or deployment policy changed.
- No confirmed source-artwork field exists in current repository or client evidence. The corpus records that boundary and does not invent one.

## 15. Official Nuvio API and integration boundary

**Confirmed by current official Nuvio documentation, reviewed 2026-07-25:** the [Nuvio Public API](https://nuvio.tv/docs) documents Supabase-based email/password authentication, access/refresh tokens, up to six profiles, profile-scoped resources, and collection pull/push operations. A collection push replaces the profile's complete `collections_json` blob; omission removes collections and an empty array clears it. No separate collection import, merge, or targeted-update endpoint was identified on the reviewed page. This confirms an authenticated collection transport, but not a safe merge/update UI, device-pairing contract, or approved Builder credential-storage design.

The official API's small addon-backed collection example is not presented as a complete collection-source schema. It does not override repository tests and manual evidence that `sources` is authoritative and `catalogSources` is an addon compatibility projection/fallback.

Keep three manifest/source concepts separate:

- **Collection sources** are source objects inside Nuvio collection/folder JSON and follow this repository's evidence-backed source rules.
- **Stremio-style addons** are profile-scoped addon manifest URLs documented by the API's addon sync.
- **Nuvio integrations/plugins** are supplementary profile-scoped URLs. The separate [Nuvio Integration Development Guide](https://nuvio.tv/docs?doc=plugins-repo) documents repository `manifest.json` files that register integration JavaScript for local Hermes execution.

The API documents addon and plugin sync as full-replace operations too. Plugin-repository manifests are not collection sources or Stremio addon manifests, and plugin repositories are not core Builder scope. The official account page at [nuvio.tv/account/login](https://nuvio.tv/account/login) confirms an account-management surface for synced integrations and collections, but it was not used for authenticated testing. The reviewed public documentation did not establish device-pairing endpoints or behaviour; device pairing remains unverified.

## 16. Roadmap checkpoint

The current dependency-aware sequence is:

1. product/workflow recovery — complete;
2. collection/folder presentation settings and owner-review corrections — integrated and complete;
3. focused Dave UI/flow review and independent follow-up review — complete for issue #53;
4. bulk presentation settings remain desired but deferred to a separate focused issue;
5. collection/folder/source reordering — integrated through issue #59 / PR #60, with Windows evidence-check hardening through issue #61 / PR #62; owner local review and the bounded Desktop/web/mobile/TV evidence gate are complete;
6. persistent hierarchy creation and safe deletion — integrated through issue #63 / PR #64 after owner desktop/mobile review;
7. first source creation and Search/Add — integrated through issue #65 / PR #66 after the owner UI and current Nuvio Desktop evidence gates completed;
8. Collection and Folder settings polish — integrated through issue #69 / PR #70 after independent and owner UI/flow review.
9. Unified People Search/Add — integrated through issue #74 / PR #75; the first Desktop source-contract evidence passed, then the regenerated distinct-title/curated-artwork fixture passed owner visual/import/immediate-export validation. Both curated Posters, all four distinct tabs, populated Acting/Directing catalogues, exact SHA-versioned URLs, `hideTitle: true`, IDs, grouping, source order, and native `catalogSources: []` survived; the client version/build remains unknown.
10. Native physical-source editing — integrated through issue #78 / PR #79 for Movie Collection and People, extended through Studio issue #92 / PR #93 and Network issue #98 / PR #99, then narrowly extended through Streaming issue #104 / PR #105, official Genre issue #110 / PR #111, and canonical Decade issue #113 / PR #115. Generic DISCOVER remains non-editable. The full current-client V2 edit/export round trip is deliberately deferred until V2 exposes export and is not an unfinished integration gate.
11. Studio Movie/Series source creation and physical-source editing — merged through issue #92 / PR #93 using V1-informed Company metadata discovery, automatic empty-state Browse plus pill/filter controls, native search clear and shared identity-link patterns, contained text-fallback logos, quiet informational Movie/Series counts only after selection, retained media-specific sorts, and display-name/sort-only physical updates.
12. Network Series source creation and physical-source editing — merged through issue #98 / PR #99 with cached A–Z/hidden-relevance discovery that completely ignores legacy `t`, one automatic informational post-selection Series Count, strict `NETWORK/TV` construction, folder-aware duplicate review, display-name/sort-only updates, and completed owner plus deployed-Worker acceptance.
13. Internal DISCOVER Core — the original 14-field milestone merged through issue #100 / PR #101, and issue #106 / PR #107 updates it to the current 18-field persisted contract with no intended user-facing behavior; see [`BUILDER_DISCOVER_CORE.md`](./BUILDER_DISCOVER_CORE.md).
14. Streaming Services merged through issue #104 / PR #105; tracked Worker and owner evidence are complete, while a Builder-generated client/export round trip is deferred until V2 export exists and is not an unfinished gate.
15. Genres merged through issue #110 / PR #111 with desktop/physical-iPhone owner acceptance complete. Current-client V2 export testing is deliberately deferred until export exists and is not an unfinished gate.
16. Decades — issue #112 / PR #114 provides the non-UI catalogue/source/hierarchy planning, duplicate/stale validation, canonical classification, and atomic multi-collection foundation. Issue #113 / PR #115 provides the shared Blank-first launcher, visible defaults/order/grouping, responsive per-Decade Genre configuration, physical-folder-aware naming, shared hidden-title accessibility, review/apply flow, presentation controls, scalable summaries, and narrow canonical Decade Source Edit after completed owner review.
17. Reuse-first hierarchy-family migration through the existing launcher — People is the first post-Decades proof through issue #118 / PR #119. Issue #120 codifies the cross-family standard in [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md). Franchises merged through issue #122 / PR #123, Studios through issue #124 / PR #125, Networks through issue #126 / PR #127, Genres through issue #130 / PR #131, Streaming Services through issue #162 / PR #163, and TMDB Lists through issue #170 / PR #171. The eight guided families are complete.
18. Genre hierarchy — issue #130 / PR #131 is merged at `817b4b8c46ca135e2badcfc6ca903dde3f824222` after final desktop/physical-phone owner acceptance. It uses Select → Configure → Structure → Appearance → Create, four plan-derived structures, three focused combined-Series placement rules, structure-specific duplicates, and atomic multi-collection application. Structure presents the unchanged choices as first-time-user visual cards with compact illustrative hierarchy wireframes and visible plan-derived Collection/Folder counts; Source counts remain authoritative internally and in later summaries. Owner Worker version `857c1fa3-e62d-4fd8-9321-9573aedb1906` and its production Worker/TMDB/image-CDN validation remain first-correction evidence. Dave confirmed the corrected 10,479-byte source requiring canonical `include_adult=false` was deployed on 2026-08-21, and direct rejection checks plus the complete mounted production path passed. The presentation-only corrections change no Worker contract. Selected-folder Add Source and Source Edit are unchanged.
19. Whole-Builder tightening/consolidation follows the eight-family milestone. Issue #132 removed the legacy People panel launcher and restored browse-first focus for guided People selection; issue #160 added singular Decade to Add Source; issue #166 completed ordinary Add Source Preview coverage; issue #170 added TMDB Lists; issue #172 made title visibility live and reversible across all guided families and Node Editor; and issue #176 closes People Add Source Sort parity with an enforceable cross-family capability contract. No next implementation issue is selected by that reconciliation.
20. Advanced Discover.
21. Broader deliberate V2 UI/UX and artwork-runtime work follows at the appropriate source stages.

Quick Setup, Dave's 1-Click Setup, templates/recipes, the Kaptain comparison, privacy positioning, branding, and optional Nuvio connection are product-plan topics in [`BUILDER_PRODUCT_PLAN.md`](./BUILDER_PRODUCT_PLAN.md). They do not change the current technical implementation gate.

## 17. Open questions

- Can a future Nuvio source model support direct individual movies or series?
- Can multiple direct item references ever be exposed as a folder source?
- Which build-scoped Discover observations reproduce on current NuvioTV and current NuvioMobile?
- How should existing v1 exporters be consolidated without changing output?
- Which future controls need explicit property-removal semantics beyond assigning supported empty or null values?
- How should future public TMDB list search slot into the architecture?
- Can origin-country and original-language filters be represented compatibly across the supported clients?
- Can TMDB Recommendations be supported by the runtime and persisted schema without guessing a source type?
- Can a separately approved Worker expansion safely support a shared Preview of up to 100 titles?
- Can the documented full-replace collection API support a safe product-level Add, Merge, or targeted Update journey, and what backup/conflict protocol would be required?
- Does Nuvio publish a supported device-pairing or browser-handoff contract suitable for third-party builders?

## 18. Update rules

- Update this file when a test becomes confirmed or disproved.
- Include the evidence level, review/test date, and source.
- Do not silently replace prior conclusions.
- Move superseded findings into the decision history below.
- Keep large copied sections from GPL or unlicensed repositories out of this document.
- GitHub issues remain the source of truth for implementation scope.

## Evidence register

| Reviewed | Evidence | Source |
| --- | --- | --- |
| 2026-07-10 | Current Mobile collection model, resolver, folder loading, validation, and unknown-field preservation | [NuvioMobile at `4e17faa`](https://github.com/NuvioMedia/NuvioMobile/tree/4e17faa5b8c280f404ee77a811deb9eb7e160c87) |
| 2026-07-10 | Current TV collection model, resolver, folder loading, import, and export behaviour | [NuvioTV at `0a36607`](https://github.com/NuvioMedia/NuvioTV/tree/0a36607f949afc99b282829d7934f69730300311) |
| 2026-07-10 | Catalog IDs, resource types, and metadata prefixes | [Nuvio Catalog Addon manifest](https://catalog.nuvio.tv/manifest.json) |
| 2026-07-10 | Shark Movies, addon catalog, genre, disable-addon, and failed identifier tests | Project manual-test notes recorded during the v2 investigation |
| 2026-07-10 | Existing v1 generators, checks, deployment, and Worker boundaries | [TMDB ID Lookup at `62b0ba2`](https://github.com/davecollections/tmdb-id-lookup/tree/62b0ba25f1dd49ad1a1bb515ee2461f61bc18681) |
| 2026-07-10 | Deterministic canonical, invalid, and import-preservation contract fixtures | [TMDB ID Lookup issue #31](https://github.com/davecollections/tmdb-id-lookup/issues/31) |
| 2026-07-10 | Mixed native/addon runtime order, addon isolation, projection deduplication, and exported order | Manual tests in Nuvio Desktop for Windows and Nuvio Mobile on iOS using `mixed-native-and-addon.json` |
| 2026-07-10 | Opaque sentinel import/export preservation and client normalisation behaviour | Manual tests in Nuvio Desktop for Windows and Nuvio Mobile on iOS using `opaque-community-import.json` |
| 2026-07-10 | Isolated React/Vite build, combined v1 plus `/builder/` Pages staging, and successful live deployment checks for assets, the v1 backlink, mobile widths, and unlinked `noindex, nofollow` behaviour | [TMDB ID Lookup issue #33](https://github.com/davecollections/tmdb-id-lookup/issues/33) |
| 2026-07-10 | Plain-data builder hierarchy, stable internal identity, immutable operations, raw snapshot preservation, and explicit source categories | [TMDB ID Lookup issue #34](https://github.com/davecollections/tmdb-id-lookup/issues/34) |
| 2026-07-11 | Atomic preservation-first import, provider-led source classification, conservative editable extraction, raw snapshot detachment, and legacy compatibility detection | [TMDB ID Lookup issue #35](https://github.com/davecollections/tmdb-id-lookup/issues/35) |
| 2026-07-11 | Atomic preservation-first serialization, compact new-node output, editable-over-raw overlay, addon projection matching, legacy blocking, and deterministic JSON output | [TMDB ID Lookup issue #36](https://github.com/davecollections/tmdb-id-lookup/issues/36) |
| 2026-07-11 | Owner-reviewed Nuvio Desktop pairs for compact active-addon expansion and deterministic addon projection-only promotion, plus sanitised repository reproductions and explicit migration tests | [TMDB ID Lookup issue #37](https://github.com/davecollections/tmdb-id-lookup/issues/37) |
| 2026-07-11 | Manually confirmed sanitised builder-generated Nuvio Desktop migration round trip with untouched export, additive-only semantic comparison, exact hashes, and canonical validation | [TMDB ID Lookup issue #38](https://github.com/davecollections/tmdb-id-lookup/issues/38) |
| 2026-07-11 | Reverted Pages exposure, recovered unchanged issue #38 evidence, and shared allowlist enforcement for v1 runtime files, compiled builder output, and repository-only path exclusion | [TMDB ID Lookup issue #38](https://github.com/davecollections/tmdb-id-lookup/issues/38) |
| 2026-07-11 | Framework-independent controller ownership, frozen external-store snapshots, guarded replacement, hierarchical selection, disposable production migration preview, explicit migration application, coordinated edits, diagnostics, and export lifecycle | [TMDB ID Lookup issue #39](https://github.com/davecollections/tmdb-id-lookup/issues/39) |
| 2026-07-11 | First visible controller-connected builder shell, ordered hierarchy navigator, deterministic collection/folder drafts, read-only summaries, mobile drill-down, desktop panels, accessibility, and real-shell Pages markers | [TMDB ID Lookup issue #40](https://github.com/davecollections/tmdb-id-lookup/issues/40) |
| 2026-07-12 | Corrected TMDB Collection Builder naming, local-only welcome entry, controller-delegated new-project and JSON import flows, stable diagnostics, warning summary, responsive accessibility, and welcome Pages marker | [TMDB ID Lookup issue #41](https://github.com/davecollections/tmdb-id-lookup/issues/41) |
| 2026-07-12 | UI-only collection/folder ID and title drafts, minimal controller patches, navigation locking, unusual imported-value handling, accessibility, and raw/unknown/source-order preservation | [TMDB ID Lookup issue #42](https://github.com/davecollections/tmdb-id-lookup/issues/42) |
| 2026-07-12 | Hidden automatic collection/folder Nuvio IDs, deterministic import repair, title-only editing, guarded builder-home return, and functional hierarchy empty states | [TMDB ID Lookup issue #43](https://github.com/davecollections/tmdb-id-lookup/issues/43) |
| 2026-07-21 | Final published typed artwork identity, orientation, SHA-version, review-safety, and missing-key contract | [`davecollections/nuvio-assets` runtime lookup and schema](https://github.com/davecollections/nuvio-assets/tree/main/assets/collection_covers) |
| 2026-07-21 | Shared pure artwork validation, resolution, loading, in-memory caching, retry, and offline fixture behaviour | [TMDB ID Lookup issue #45](https://github.com/davecollections/tmdb-id-lookup/issues/45) |
| 2026-07-22 | V1 company/network lazy runtime consumption, automatic curated/TMDB/emoji fallback, async preparation, Copy/Download parity, and focus-GIF removal | [TMDB ID Lookup issue #46](https://github.com/davecollections/tmdb-id-lookup/issues/46) |
| 2026-07-23 | Official TMDB Discover inventory/OAS, pinned NuvioTV `dev` and `0.7.19-beta`, pinned NuvioMobile `cmp-rewrite` and `0.3.1`, builder preservation audit, normalized compatibility matrix, bounded manual/direct test plans, and 29/29 owner observations on Desktop alpha plus retained official iOS | [TMDB ID Lookup issue #47](https://github.com/davecollections/tmdb-id-lookup/issues/47), [`TMDB_DISCOVER_COMPATIBILITY.md`](./TMDB_DISCOVER_COMPATIBILITY.md), and [`OWNER_RESULTS_2026-07-23.md`](../../manual-tests/tmdb-discover/OWNER_RESULTS_2026-07-23.md) |
| 2026-07-24 | Manifest-driven canonical, preservation, identity, migration, and invalid profile corpus connecting current builder compatibility contracts without production changes | [TMDB ID Lookup issue #49](https://github.com/davecollections/tmdb-id-lookup/issues/49) and [`v2-compatibility/README.md`](../../tests/fixtures/nuvio/v2-compatibility/README.md) |
| 2026-07-24 | Full owner-supplied V1 and V2 conversation histories, reviewed to recover explicit product and workflow intent without committing or reproducing the exports | Private owner-supplied research evidence for [issue #51](https://github.com/davecollections/tmdb-id-lookup/issues/51) |
| 2026-07-25 | Current public authentication, profiles, full-replace collection sync, addon/plugin sync, account-management surface, and integration-repository boundary | [Nuvio Public API](https://nuvio.tv/docs), [Nuvio Integration Development Guide](https://nuvio.tv/docs?doc=plugins-repo), and [Nuvio account login](https://nuvio.tv/account/login) |
| 2026-07-25 | Collection/folder presentation defaults, direct per-card Edit actions, responsive inert-background modal, collection/folder U+200E creation, three-outcome folder title visibility, imported hidden-title preservation, home-screen-only `hideTitle` scope, collection `focusGlowEnabled`, touched-only canonical patches, Follow Layout/Square preservation, accessible fallbacks, and serializer-cycle preservation | [TMDB ID Lookup issue #53](https://github.com/davecollections/tmdb-id-lookup/issues/53) |
| 2026-07-26 | Strict numeric artwork runtime schema-v1/v2 dispatch, exact entity/ID/orientation paths, v2 Network Poster requirements, unchanged v1 adapter interface, and cached-export parity against test-only v2 fixtures | [TMDB ID Lookup issue #55](https://github.com/davecollections/tmdb-id-lookup/issues/55) and the separately reviewed `nuvio-assets` commits `366bb3e` / `f627428` |
| 2026-07-26 | Completed public artwork runtime-v2 publication, all 572 Network Landscape/Poster records, release hashes and counts, and live shared-resolver/v1-bridge verification | [`nuvio-assets` PR #3](https://github.com/davecollections/nuvio-assets/pull/3) and [TMDB ID Lookup issue #57](https://github.com/davecollections/tmdb-id-lookup/issues/57) |
| 2026-07-27 | Compact pointer/touch and keyboard collection/folder/source reordering plus bounded Desktop/web/mobile/TV ordering evidence, exact Desktop/web exports, explicit mobile raw-artifact and TV no-export limitations, deterministic normalization checks, and completed same-issue client gate | [TMDB ID Lookup issue #59](https://github.com/davecollections/tmdb-id-lookup/issues/59) and [`issue-59-builder-reordering/`](../../manual-tests/nuvio-clients/issue-59-builder-reordering/) |
| 2026-07-28 | Persistent populated-list collection/folder Add actions; viewport-aware body-portalled collection/folder/source menus with upward flipping and no-scroll initial focus; mobile context quick rename; mandatory source confirmation; imported/projection-aware collection/folder confirmation; deterministic selection/mobile/focus recovery; addon projection removal; canonical and second-cycle stability | [TMDB ID Lookup issue #63](https://github.com/davecollections/tmdb-id-lookup/issues/63) |
| 2026-07-29 | First selected-folder Search/Add flow; final owner physical-iPhone acceptance; successful current Nuvio Desktop import, Harry Potter runtime resolution, and exact immediate export; bounded `original`/descending/unsupported-ascending sort observations; strict exact input, TMDB-backed result classification, normalized poster/contained-title review data, visible selection failure recovery, robust Worker configuration and async contracts; exact native `COLLECTION` recipe; duplicate/submit protection; and isolated mobile Visual Viewport coverage | [TMDB ID Lookup issue #65](https://github.com/davecollections/tmdb-id-lookup/issues/65), [`BUILDER_SOURCE_ADD.md`](./BUILDER_SOURCE_ADD.md), and [`issue-65-builder-add-source/`](../../manual-tests/nuvio-clients/issue-65-builder-add-source/) |
| 2026-07-30 | Exact Collection source-presentation wording, `Tabs (recommended)` over unchanged `TABBED_GRID`, hidden focus-glow control with true manual default and preservation-first import/export behavior, Folder Basic details/Display groups, compact title-visibility radios, visual tile-shape cards, and no Artwork controls/schema expansion | [TMDB ID Lookup issue #69](https://github.com/davecollections/tmdb-id-lookup/issues/69) |
| 2026-07-31 | Historical architecture evidence, superseded by issue #90: reuse-first typed Company Movie, Company Series, and Network Series maintenance, immutable progress, sidecar publication, and request reservations were designed without a Builder consumer | [TMDB ID Lookup issue #71](https://github.com/davecollections/tmdb-id-lookup/issues/71) and repository Git history |
| 2026-08-06 | Historical architecture evidence, superseded by issue #90: durable typed-count recovery and its hosted zero-TMDB-request producer/consumer drill were completed before the underlying global pre-cache architecture was retired | [TMDB ID Lookup issue #73](https://github.com/davecollections/tmdb-id-lookup/issues/73), [PR #80](https://github.com/davecollections/tmdb-id-lookup/pull/80), and repository Git history |
| 2026-08-06 | Global typed Company/Network count pre-cache retired before activation; ordinary Company and Network catalogues, audit/repair, request budgeting, retries, and host safeguards retained; future V2 counts moved to informational post-selection requests | [TMDB ID Lookup issue #90](https://github.com/davecollections/tmdb-id-lookup/issues/90) |
| 2026-08-06 | Canonical repository Worker synchronized with the already deployed narrow People service-token behavior; browser CORS and other path rules remain unchanged and no secret values are recorded | [TMDB ID Lookup issue #81](https://github.com/davecollections/tmdb-id-lookup/issues/81) and [PR #82](https://github.com/davecollections/tmdb-id-lookup/pull/82) |
| 2026-08-01 | Unified People Search/Add final owner refinement; untouched-default-folder atomic promotion; preservation-only existing folders; stable v1 tab titles; one final artwork representation; per-person/context artwork isolation; successful first Desktop source-contract run; subsequent regenerated SHA-versioned curated-art fixture visual/import/immediate-export acceptance; and explicit asset-gap tracking | [TMDB ID Lookup issue #74](https://github.com/davecollections/tmdb-id-lookup/issues/74), [`BUILDER_PEOPLE.md`](./BUILDER_PEOPLE.md), and [`issue-74-builder-add-people/`](../../manual-tests/nuvio-clients/issue-74-builder-add-people/) |
| 2026-08-04 | Registry-backed physical Movie Collection and People source editing merged after deterministic validation and owner desktop/physical-phone review; the complete current-client V2 edit/export round trip is deliberately deferred until V2 exposes export | [TMDB ID Lookup issue #78](https://github.com/davecollections/tmdb-id-lookup/issues/78), [PR #79](https://github.com/davecollections/tmdb-id-lookup/pull/79), [`BUILDER_SOURCE_EDITING.md`](./BUILDER_SOURCE_EDITING.md), and [`issue-78-source-editing/`](../../manual-tests/nuvio-clients/issue-78-source-editing/) |
| 2026-08-08 | Refined user-facing Studio flow backed internally by `COMPANY`: hidden Best Match plus automatic Browse/A–Z/Most movies/known-zero filtering, native entity-search clear and identity links, safely contained logos with text fallback, automatic quiet post-selection Movie/Series count states, compact duplicate action hierarchy and elsewhere-location panel, retained `COMPANY/MOVIE` + `COMPANY/TV` fixture/resolver/sort contracts, no-icon media-aware sort cards, independent display-name/sort editing, safe desktop double-click, and bounded 1280px plus 360–412px browser QA. After the reviewed narrow Worker route change was deployed manually, desktop and LAN acceptance returned Pixar 136/15 and Warner Bros. Pictures 3,123/8 Movie/Series totals. | [TMDB ID Lookup issue #92](https://github.com/davecollections/tmdb-id-lookup/issues/92) and [`BUILDER_STUDIOS.md`](./BUILDER_STUDIOS.md) |
| 2026-08-09 | First Network Series Add/Edit flow: cached A–Z and hidden relevance search across identity/country/location while completely discarding legacy `t`; contained logo and canonical TMDB Network identity; one automatic informational post-selection Series Count using strict TV `with_networks`; exact `NETWORK/TV` source construction with no native projection; folder-aware duplicate override and atomic insertion; title/sort-only physical edit; deterministic domain/UI/Worker/mobile contracts. The reviewed Worker route was manually deployed and bounded live Network/Company acceptance plus a production-built mock-disabled Builder passed; no Nuvio client result is claimed. | [TMDB ID Lookup issue #98](https://github.com/davecollections/tmdb-id-lookup/issues/98) and [`BUILDER_NETWORKS.md`](./BUILDER_NETWORKS.md) |
| 2026-08-10 | Ordinary Network title totals distinguish confirmed zero (`t: 0`) from unknown (absent `t`); legacy omitted zeros migrated explicitly; monthly refresh and nightly Network repair preserve/retry the distinction; V1 displays unknown honestly; Builder continues to discard the field; Company maintenance remains unchanged. | [TMDB ID Lookup issue #102](https://github.com/davecollections/tmdb-id-lookup/issues/102) |
| 2026-08-09 | Thin internal DISCOVER Core: canonical native construction, 14 evidenced filter descriptors, four Movie/TV semantic sorts, comparison-only conservative normalization, sort-aware/title-free functional identity, small capability classification, deterministic future-consumer specification cases, and no UI/importer/serializer/Worker behavior change | [TMDB ID Lookup issue #100](https://github.com/davecollections/tmdb-id-lookup/issues/100) and [`BUILDER_DISCOVER_CORE.md`](./BUILDER_DISCOVER_CORE.md) |
| 2026-08-12 | Current 18-field DISCOVER persisted contract from NuvioTV/Mobile; four nullable-string exclusions; exact/order-sensitive exclusion comparison; include-or-exclude effective Watch Region; shared import/overlay/serialization preservation; unchanged fail-closed simple Streaming recognition; no UI or Worker change | [TMDB ID Lookup issue #106](https://github.com/davecollections/tmdb-id-lookup/issues/106) and [`BUILDER_DISCOVER_CORE.md`](./BUILDER_DISCOVER_CORE.md) |
| 2026-08-11 | Streaming Services merged through issue #104 / PR #105: deployed exact three-route Worker contract retained; natural ordered known-Region selection first with curated live-resolved Common default, complete A–Z, and complete-catalogue search; locally derived common-media provider eligibility; one-region regional Top 30; multi-region A–Z/search without synthetic ranking; global priority removed; strict common media; per-physical-source creation names; Configure-generated review; independent partial duplicates and atomic N-source insertion; narrow name/sort Source Edit for simple Streaming DISCOVER; centralized attribution; historical-only nuvio.tv warning evidence | [TMDB ID Lookup issue #104](https://github.com/davecollections/tmdb-id-lookup/issues/104) and [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md) |
| 2026-08-15 | Official Genre merged through issue #110 / PR #111 after desktop/physical-iPhone owner acceptance: browse-first Search focus, stable 360–412px selection toolbar, persistent mobile exclusion-detail Back with root-only Done semantics, neutral review rows, shared exact-duplicate/elsewhere wording and notice treatment, unchanged DISCOVER identity, and explicit deferral of related-variant detection; no current-client V2 import/export result is claimed, and that evidence is deliberately deferred until export exists | [TMDB ID Lookup issue #110](https://github.com/davecollections/tmdb-id-lookup/issues/110), [`BUILDER_GENRES.md`](./BUILDER_GENRES.md), and [`issue-110-builder-genres/`](../../manual-tests/nuvio-clients/issue-110-builder-genres/) |
| 2026-08-16 | Visible Decades merged through issue #113 / PR #115 after completed owner desktop/physical-phone review: shared Blank-first launcher and top-left Back; content-only Configure Decades with Both/Separate plus Newest-Decades/Oldest-Years defaults, three exact Display-order presets, mixed-folder-only independent Source grouping, contextual Genres/exclusions, and bounded Advanced; count-led Review & Appearance later recomposed presentation-only into shared Title options, direct Layout, Folder options, and details without changing defaults, plan values, JSON, duplicate behavior, or atomic application; conditional overview/All-tab note, New Folder read-only inheritance, and future artwork boundary; exact #112 planning/atomic revalidation; desktop plus 360–412px focus, one-scroll-owner, action reachability, overflow, and recovery checks; no client import/export result is claimed | [TMDB ID Lookup issue #113](https://github.com/davecollections/tmdb-id-lookup/issues/113), [`BUILDER_DECADES.md`](./BUILDER_DECADES.md), and [`BUILDER_SOURCE_EDITING.md`](./BUILDER_SOURCE_EDITING.md) |
| 2026-08-16 | Issue #118 People hierarchy and active V2 authority owner-review checkpoint, subsequently merged through PR #119: People joins Blank/Decades in both launcher scopes; the retained `+ People` action aliases New Folder; ordered selection has no hard ceiling and uses one compact selected-summary row, a collapsed bounded removable list, and a tunable 50-person notice. Configure exposes only Automatic/Same for all around four always-visible direct tick/count pills; individual changes are retained as internal overrides without a mode transition, shared changes preserve those overrides, and the bounded list receives the available vertical space. A shared verified three-option semantic sort remains Popular/Recent/Top-rated; Most votes was investigated but not added because retained `PERSON`/`DIRECTOR` evidence does not establish `vote_count.desc`. Preview titles is a body-portalled, focus-contained poster-only modal capped at 10 desktop/5 mobile. People Review keeps New Collection presentation controls directly visible, adds one shared Poster/Landscape choice for generated folders, explains that remaining artwork uses canonical defaults and can be customised later through ordinary Folder editing, and collapses only large person detail. New Collection creates editable `People` → canonical-name person folders while New Folder keeps parent presentation read-only and byte-preserved; exact source/person-aware destination outcomes prevent duplicate logical folders; sort stays outside identity; ephemeral plans preserve concrete sort and appearance values, revalidate and apply atomically. One schema-v2 `nuvio-people-assets` manifest load supplies registered canonical names, actor/director membership and four core plus optional paired focus assets by numeric TMDB identity; missing IDs use TMDB/emoji fallback without a legacy People path. `poster`/`landscape`, separate `hero`/`titleLogo`, and complete focus pair map to existing fields only, with transient SHA evidence; hierarchy Review has no per-person URL, focus, or reset controls. The shared known-field controls separately expose the same artwork properties in the manual Folder editor. The Builder treats `focusGifUrl` as a format-agnostic string and preserves static WebP, but no issue #118 Nuvio render is claimed. Company/Network, V1, both asset repositories, Worker/deployment and legacy deletion remain unchanged. | [TMDB ID Lookup issue #118](https://github.com/davecollections/tmdb-id-lookup/issues/118), [PR #119](https://github.com/davecollections/tmdb-id-lookup/pull/119), [`BUILDER_PEOPLE.md`](./BUILDER_PEOPLE.md), and [`issue-118-people-hierarchy/`](../../manual-tests/nuvio-clients/issue-118-people-hierarchy/) |
| 2026-08-17 | Final issue #118 owner-review presentation and interaction corrections, merged through PR #119 after owner physical-keyboard and responsive acceptance: People Search retains full-card click and native checkbox accessibility while showing circular empty/filled-tick indicators. The selectable label contains the visually hidden checkbox's positioning context, so partially clipped pointer/focus interaction keeps the outer dialog, document, and sticky Configure action fixed while only the inner result scroll owner may move. People Review reuses the same Title options composition as Decades, places it before direct Layout, exposes all three generated-folder title outcomes with Hide on Home screen only as the People default, and carries that value through review, stale revalidation, and atomic apply. New Folder exposes the folder-title choice without collection-title or layout editing and keeps its parent byte-identical. Decades adopts the same Title options → Layout composition without changing its defaults, plan values, JSON, duplicates, or atomic application. | [TMDB ID Lookup issue #118](https://github.com/davecollections/tmdb-id-lookup/issues/118), [PR #119](https://github.com/davecollections/tmdb-id-lookup/pull/119), [`BUILDER_PEOPLE.md`](./BUILDER_PEOPLE.md), and [`BUILDER_DECADES.md`](./BUILDER_DECADES.md) |
| 2026-08-18 | Historical issue #122 Franchise hierarchy owner-review checkpoint: Franchises joins both contextual launcher scopes while selected-folder Add Source → Movie franchise remains separate. The two-stage Select → Review & Appearance flow reuses one TMDB Collection provider/cache/parser, does not auto-focus Search, keeps ordered full-card checkbox selection uncapped with a compact disclosure and 50-item notice, and provides a body-portalled 10-desktop/5-mobile contained-title preview. Each exact TMDB Collection ID creates one canonical-name Folder and the existing fixed native `COLLECTION`/`MOVIE`, `original` source. Complete canonical wording including leading `The`, Collection, Saga, and Trilogy is preserved. Poster is the product-plan default; reviewed V1 behavior supplies poster/backdrop orientation fallback evidence, now emitted through bounded TMDB `w500` image URLs without per-item controls. Review reuses shared title/layout/folder presentation, New Folder preserves its parent byte-identical, destination exact IDs are omitted, elsewhere matches remain informational, and one ephemeral revalidated plan applies through existing atomic controller operations. Repository verification and owner browser acceptance remain pending at this checkpoint; no merge, release, Worker, V1, dependency, or new Nuvio-client claim is made. | [TMDB ID Lookup issue #122](https://github.com/davecollections/tmdb-id-lookup/issues/122) and [`BUILDER_FRANCHISES.md`](./BUILDER_FRANCHISES.md) |
| 2026-08-19 | Issue #122 focused owner-review correction supersedes the initial orientation choice: Franchise hierarchy folders are fixed Poster tiles, only the TMDB Collection poster maps to `coverImageUrl`, missing posters use the emoji fallback, and backdrops are not repurposed as covers or newly mapped to Hero. Franchise Preview titles now shares the established nested-modal layer with People above the active creation modal, with focus containment, Escape/Close, and exact trigger restoration from both Select and Review. Selected-list actions are compact, touch-safe, and long-name safe. One shared hierarchy Layout control now exposes Show All only for Tabs; Rows hides it and all Franchise, People, and Decades plans normalize `showAllTab: true`, while New Folder remains parent-preserving. Owner desktop and physical mobile review are complete and accepted. | Completed owner review for [issue #122](https://github.com/davecollections/tmdb-id-lookup/issues/122) and [`BUILDER_FRANCHISES.md`](./BUILDER_FRANCHISES.md) |
| 2026-08-19 | Issue #124 completes owner-reviewed Studio hierarchy creation in both launcher scopes. It reuses Studio catalogue discovery and native Company sources, adds a checked-in hierarchy Movie-count filter without changing Add Source, places selected identity/count/placement/Preview/remove rows in Configure, and keeps the final Appearance stage presentation-only. It extracts a shared structural nested Preview shell and bounded cache, adds explicit lazy per-media Company Preview with one response for count plus ordered results, separates People Preview into request-free media-specific views over existing combined credits, retains Series count only as transient Company/media context, omits destination partial/complete logical matches, resolves fixed Landscape folder artwork before planning, and performs zero network work in plan/revalidation/apply. After separate owner authorization, the narrow Company sort-aware Worker source was manually deployed and the complete live acceptance/rejection matrix was validated; overall V2 release status remains separately governed, and no current-client hierarchy evidence is claimed. | Completed owner review for [issue #124](https://github.com/davecollections/tmdb-id-lookup/issues/124) and [`BUILDER_STUDIOS.md`](./BUILDER_STUDIOS.md) |
| 2026-08-20 | Network hierarchy closed/completed through issue #126 / merged PR #127 at `10a76aeffd351341321ab56658e69858fb85d39c`: owner Worker version `f6bee241-afef-447f-b8f9-3d4b8da460cf`, recorded deployment-handoff source SHA-256 `612955AD3ECCEF16E12E05ABA6B672B0AD68BA825F13419FFA2A0A9346706AD4`, and separately merged Worker blob `ceb37bb3711a43d6f25508a98943ce71b53baec2`; Worker 16/16; repeated and full-check mounted 27/27 through production Worker, real TMDB, and real image resources; post-merge Nuvio Contract Validation run 32326423651 success; automatic Pages run 32326423777 success; no merge-time Worker redeployment or manual frontend deployment. Current-client Network hierarchy round trip remains unverified and the NuvioTV/Mobile native-Network resolver divergence remains unresolved. | [Issue #126](https://github.com/davecollections/tmdb-id-lookup/issues/126), [PR #127](https://github.com/davecollections/tmdb-id-lookup/pull/127), [validation run](https://github.com/davecollections/tmdb-id-lookup/actions/runs/32326423651), [Pages run](https://github.com/davecollections/tmdb-id-lookup/actions/runs/32326423777), and [`BUILDER_NETWORKS.md`](./BUILDER_NETWORKS.md) |
| 2026-08-20 | Genre hierarchy issue #130 owner-correction checkpoint, uncommitted at the Worker-deployment gate: the approved Select stage remains stable. Configure now groups Media/Sort, preserves Advanced, removes both duplicate selected/configured disclosures, and always renders one ordered logical row with placement, Remove, and exact-config Preview per Genre. Preview uses the exact compiled physical draft—including Advanced and per-Genre exclusions—through DISCOVER descriptors, lazy media views, the shared nested poster-only shell, and a complete canonical-query cache. Appearance adds Landscape-default/Poster using explicit published 27-concept `wide`/`vertical` mappings with same-orientation fallback; issue #110 Add Source remains Landscape. The tracked Worker adds only a fail-closed single-Genre Discover shape and leaves Company/Network/generic/service-token boundaries intact. Deterministic domain/provider/Worker evidence precedes the required complete-source SHA-256 handoff; Dave manually deploys, then live production Worker/TMDB/CDN validation resumes. No commit, push, PR, merge, deployment, dependency, V1, asset-repository, or client claim has been made. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130) and [`BUILDER_GENRES.md`](./BUILDER_GENRES.md) |
| 2026-08-21 | Genre hierarchy issue #130 owner Worker/live-integration checkpoint: Dave manually deployed the complete reviewed working-tree source as version `857c1fa3-e62d-4fd8-9321-9573aedb1906`, from branch `work/130-genre-hierarchy` at HEAD `ae01541ba2d6915e0b0c71f060a68ec6cee6c1ff`; deployment-handoff source SHA-256 `6F67C0576470CE18901BCF7ADE433F38E411057CB27A2092D014D3185ED8A4B2`. Direct production acceptance returned HTTP 200 for representative Movie/TV Genre requests and HTTP 403 for generic Discover. Mounted 393/900 validation then used the production Worker, real TMDB, and real `image.tmdb.org`: Movie requested first, TV only after the shared-media switch, and one exact Recent Movie draft carried 2020–2026, rating 6–9, 100 votes, English, US, and Family exclusion. Counts reconciled to each live response, real posters loaded in response order, mobile rendered five versus desktop ten, successful exact-query cache replay added no request, Close restored focus, and project state remained unchanged. That first-correction deployment/live gate was complete; the later correction below establishes a new gate. Owner review, commit, PR, merge, Pages publication, and current-client evidence remain separate and pending. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130), [`BUILDER_GENRES.md`](./BUILDER_GENRES.md), and [`cloudflare-worker/README.md`](../../cloudflare-worker/README.md) |
| 2026-08-21 | Genre hierarchy issue #130 second owner-correction checkpoint: restore shared compact Media/Sort pills and fixed unavailable-media notes; insert Structure so the flow is Select → Configure → Structure → Appearance → Create; derive counts and ordinary nodes for Genre folders, fixed visible-title Media folders, separate media-specific Genre folders, and New-Collection-only separate Movie/Series collections; limit composite placement to the evidenced Action & Adventure, Sci-Fi & Fantasy, and War & Politics TV concepts with standalone, self-describing merged-target, or intentional Add-to-both nodes; apply structure-specific omissions and atomic two-collection rollback. Every Genre Preview sends exactly one canonical `include_adult=false`, the Worker requires it without widening Company/Network/generic/service-token access, and the full cache identity includes it. The handoff source is 10,479 bytes with SHA-256 `45AE6323195F067BCC6428CA8D70889640C35B661B509F47A6069EE906F12539`. Dave confirmed deployment; no second version identifier was supplied. Direct production acceptance passed canonical false and rejected missing, true, duplicate, and generic Discover, while the 29-scenario mounted suite passed through production Worker/TMDB/image-CDN at mobile and desktop widths. The branch remains unstaged/uncommitted for renewed owner review. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130), [`BUILDER_GENRES.md`](./BUILDER_GENRES.md), and [`cloudflare-worker/README.md`](../../cloudflare-worker/README.md) |
| 2026-08-21 | Genre hierarchy issue #130 third owner-correction checkpoint: keep all approved Structure modes, counts, composites, naming, flow, and state while replacing text-heavy Structure options with true visual choice cards. Each card has a title, concise subtitle, compact illustrative Collection → Folder → Source wireframe, live plan-derived counts, native radio semantics, visible focus, and the established restrained selected border/surface. Composite placement remains conditional below the cards. Local browser measurement at 360, 384, 393, 402, 412, 899, 900, 901, and 1280 pixels confirms bounded previews, readable counts, clear selection, one scroll owner, reachable sticky actions, and no horizontal overflow. This is UI-only: no Worker source, production route, source contract, or deployment changed. The branch remains unstaged/uncommitted for renewed owner review. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130) and [`BUILDER_GENRES.md`](./BUILDER_GENRES.md) |
| 2026-08-21 | Genre hierarchy issue #130 fourth owner-correction checkpoint: retain the approved Structure diagrams and every planner/composite behavior while simplifying only visible information hierarchy. Remove the redundant intermediate heading; use first-time-user descriptions and the explicit **Separate Movie & Series Genre folders** title; show only plan-derived Collection/Folder counts on choice cards while retaining Source totals internally and later; explain the three narrow combined-Series mappings without taxonomy/Source terminology; and rename the standalone action to **Keep its own folder** without changing its `standalone` planner value. Responsive copy review later passed at 360, 384, 393, 402, 412, 899, 900, 901, and 1280 pixels. No Worker change or redeployment is involved. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130) and [`BUILDER_GENRES.md`](./BUILDER_GENRES.md) |
| 2026-08-21 | Genre hierarchy issue #130 final owner-acceptance checkpoint: one shared Structure-card grid correction replaced bottom alignment of the preview area with stretched, space-between description/preview allocation. This removes the longer third title's excess empty space without a card-specific override, preserves card dimensions, diagrams, selected treatment, and bottom count alignment, and measures a consistent 12-pixel description-to-diagram gap at 360, 393, 412, 899, 900, 901, and 1280 pixels with no clipping, horizontal overflow, awkward wrapping, diagram compression, or count drift. Dave accepted the final desktop and physical-phone result. Worker deployment/live validation remain complete and unchanged; implementation and durable evidence remain unstaged/uncommitted with no push or PR pending explicit commit authorization. | [Issue #130](https://github.com/davecollections/tmdb-id-lookup/issues/130) and [`BUILDER_GENRES.md`](./BUILDER_GENRES.md) |
| 2026-08-28 | Streaming Services hierarchy issue #162 owner-review checkpoint: seventh guided family after Genres in both scopes; ordered multi-Region/global-media/uncapped-provider selection; grouped or provider/Region folders; context-aware naming; exact text-only `P × R × M` plans; title-independent trusted partial placement with ambiguity and different-Sort blocking; exact lazy production-path Preview; and one narrow family-independent atomic existing-Collection extension after the focused audit proved the prior operation gap. Controller, domain, UI, scale, and mounted 360–1280 evidence pass, including production Worker/TMDB/image-CDN checks at 393 and 900. No Worker, schema/model, importer/serializer/export, V1, dependency, deploy, publication, stage, commit, push, PR, merge, owner physical-device, or Nuvio-client claim is made. | [Issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162), [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md), and [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md) |
| 2026-08-29 | Streaming Services hierarchy issue #162 duplicate-title/copy refinement: equal visible Collection titles now receive UI-only project-order `· Collection N` labels on destination cards, matching disclosures, selected Review, and zero-change notices; unique titles remain unsuffixed and internal identity is unchanged. Destination cards use plain exact delta copy and reliable Folder/Source context, the separate route is **Create new collection instead**, and complete reuse says no project changes are needed. The temporary artwork warning is removed without changing the artwork model: reused custom cover/hero/logo/focus fields remain exact, new unknown art stays unassigned, and provider logos remain transient. Responsive mounted coverage uses three same-title candidates at all nine widths and retains the live production path at 393 and 900. No Worker, schema, planning, identity, duplicate, stale-validation, controller, source, Preview, naming, Region/media/Sort, grouping, deployment, publication, stage, commit, push, PR, merge, owner physical-device, or Nuvio-client change is claimed. | [Issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162), [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md), and [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md) |
| 2026-08-29 | Streaming Services hierarchy issue #162 collection-affinity refinement: New Collection destination discovery now recognizes a zero-exact-overlap existing Collection from effective preserved/imported native Movie/TV Discover content with a valid positive Watch Provider expression and uppercase Watch Region, including compound providers, extra filters, and canonical/preserved aliases. Titles, Lists, artwork, addon sources, malformed/conflicting aliases, and network-only Discover do not qualify. This is Collection-level discovery only: exact duplicate identity and canonical-simple Folder placement remain strict, every candidate still rebuilds the same safe existing-Collection plan, and a new provider receives a new sibling Folder. Exact overlap ranks first by match count, affinity-only candidates follow in project order, and explicit zero-overlap copy avoids claiming a match. Focused and nine-width mounted owner-style coverage preserves imported Collection/Folder/source/raw/serialized content exactly while atomically adding only one two-source sibling. No Worker, schema/model, persisted representation, importer/serializer/export behavior, physical Add Source/Edit, other hierarchy, deployment, publication, stage, commit, push, PR, merge, owner physical-device, or Nuvio-client change is claimed. | [Issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162), [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md), and [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md) |
| 2026-08-29 | Streaming Services hierarchy issue #162 final owner-acceptance checkpoint: desktop and physical-phone review approved the complete guided hierarchy, including Region/Media/provider selection and reconciliation, multi-Region configuration, exact Preview, New Collection duplicate/elsewhere and destination switching, duplicate-title disambiguation, zero-overlap imported Streaming destinations, existing-folder augmentation, new sibling creation, complete-overlap **Nothing to add**, new-Folder naming, and mobile fixed-footer/scroll behavior. Final copy reads `None of the selected sources are here yet · all <count> source(s) will be added` and `Currently: <folder count> folder(s) · <source count> source(s)`. Current-Nuvio runtime/import/export round-trip validation remains outside issue #162 and is not claimed. No Worker, V1, schema/model, importer/serializer/export, dependency, deployment, publication, push, PR, or merge is included. | [Issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162) and [`BUILDER_STREAMING_SERVICES.md`](./BUILDER_STREAMING_SERVICES.md) |
| 2026-08-29 | Issue #166 ordinary Add Source Preview parity final owner-acceptance checkpoint: Movie franchise, People, Studios, Networks, Streaming services, and Genres now join the already-complete Decade Add flow behind one shared #158 request/dialog/poster seam. Each Preview is explicit and lazy, receives the exact current canonical detached Save draft, renders one physical source at a time, defaults to deterministic Save order, retains complete configured candidates including destination duplicates, and never enters controller mutation or Save. People separates Role and Media without changing the hierarchy flow's combined-credit Preview; Studios uses complete configured Movie/Series candidates; Streaming uses selected Region order then Movie/Series with custom names and exact filters; Genres uses selected Genre order then media with full Advanced/exclusion filters; Movie franchise and People reuse successful details caches; Network remains single-Series; Decade is functionally unchanged. Deterministic, UI, build, and mounted production Worker/TMDB/image-CDN evidence for the six newly wired families passes at 393 and 900, including bounded real posters, selector-specific exact queries, cache reuse, focus containment/restoration, responsive shared geometry, Source Edit non-regression, zero Preview mutation, and exact configure → Preview → close → Save parity across all seven families. Dave approved the complete desktop and physical-phone behavior, including nested layout, selectors, posters, focus restoration, removal of the Movie-franchise legacy contained-title accordion, and removal of the blanket TMDB chooser statement. A transient local review mismatch was traced to a stale preview server and was not a product defect. Current-Nuvio validation is outside this issue's accepted scope, is not required or pending for #166, and no Nuvio-client result is claimed. No Worker, schema/model, controller, importer/serializer/export, V1, dependency, deployment, publication, push, PR, or merge is included. | [Issue #166](https://github.com/davecollections/tmdb-id-lookup/issues/166) |
| 2026-09-01 | TMDB Lists issue #170 closed through merged PR #171 at `3e1ceace849d137e17ddfedb8637bc147f511285`; Pages publication, the owner-deployed narrow List Worker, bounded live production Worker/TMDB/image-CDN acceptance, current-Nuvio Movie-only/Series-only/mixed List behavior and immediate export, and desktop/physical-phone owner review passed. | [Issue #170](https://github.com/davecollections/tmdb-id-lookup/issues/170), [PR #171](https://github.com/davecollections/tmdb-id-lookup/pull/171), and [`BUILDER_TMDB_LISTS.md`](./BUILDER_TMDB_LISTS.md) |
| 2026-09-02 | Title visibility issue #172 closed through merged PR #173 at `459be092738b2a70884347fd87a782d099986512`; exact-merge CI and Pages passed. Shared title-field and transition helpers provide live reversible drafts for all eight guided families and Node Editor, with independent stable-key restoration and unchanged persisted U+200E/`hideTitle`, schema, source identity, and atomic mutation contracts. | [Issue #172](https://github.com/davecollections/tmdb-id-lookup/issues/172), [PR #173](https://github.com/davecollections/tmdb-id-lookup/pull/173), and [`BUILDER_HIERARCHY_CREATION.md`](./BUILDER_HIERARCHY_CREATION.md) |

## Decision history

- **2026-08-28 — Streaming Services guided hierarchy and narrow controller extension:** expose Streaming Services after Genres through both shared hierarchy scopes while preserving selected-folder Add Source and Source Edit. Use ordered Regions, one global Movies/Series/Both intersection, uncapped ordered providers, one global evidenced Sort, grouped-by-service default, optional multi-Region provider/Region folders, explicit naming contexts, text-only folders, and exact lazy Preview. Derive placement solely from consistent canonical simple Streaming children; extend trusted partials, block different-Sort slots and ambiguity, keep elsewhere informational, and revalidate before atomic Apply. After auditing all existing guided-family mutations, add only the missing existing-Collection batch that can append sources to multiple internal-ID folder targets and create new folders in one detached candidate/commit while preserving imports and rollback; do not create a generic transaction or retrofit other families. Keep Worker, schema/model, importer/serializer/export, V1, dependencies, deployment/publication, commits, and client claims unchanged. See [issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162).

- **2026-08-29 — Streaming Collection affinity without Folder-trust expansion:** for New Collection only, allow an existing Collection with zero proposed exact matches to enter destination choice when its effective preserved/imported native Movie/TV Discover content proves a valid Watch Provider expression plus uppercase Watch Region. Accept compound provider expressions, extra filters, and canonical/preserved aliases read-only; reject title, List, artwork, addon, network-only, malformed, and conflicting-alias signals. Keep exact duplicate identity and canonical-simple existing-Folder placement unchanged. Rebuild the ordinary existing-Collection plan for every candidate, rank exact overlap before affinity-only project order, require explicit selection, communicate zero overlap honestly, and create a new sibling Folder when strict placement has no trusted target. Preserve all existing imported nodes/raw/serialized output and apply only the new sibling atomically. See [issue #162](https://github.com/davecollections/tmdb-id-lookup/issues/162).

- **2026-08-22 — Shape-aware exact-curated Folder Tile and Focus switching:** on an explicit current-session `POSTER`/`LANDSCAPE` change only, independently inspect the current draft `coverImageUrl` and `focusGifUrl` against the published same-field candidates for the exact existing #140 Folder identity. If and only if a field is an exact candidate, select that field's requested-orientation published counterpart; otherwise preserve it byte-for-byte. Blank fields remain #140 opt-in assistance, and custom, TMDB fallback, imported/unknown, wrong-identity, ambiguous-identity, and exact candidates from another artwork field are never automatic replacement authority. Focus switching applies even while disabled but never changes `focusGifEnabled`. When a recognized curated Tile lacks a requested counterpart, keep the requested shape and current URL and show `Curated <orientation> artwork isn't available for this folder, so the current tile artwork will be kept.` directly beneath Tile shape, before the independent sibling notice. When an exact curated Focus lacks a counterpart, retain it and show the equivalent compact Focus-specific notice beside that field. The schema-v2 People authority currently accepts optional Focus only as a complete pair, so its valid live records ordinarily have both or neither; the missing branch is fail-safe. The current draft drives recognition, so manual custom replacement stops later switching, repeated exact-curated changes are reversible, preview failure does not revoke authority, and no open/import/serialize/unrelated-Apply normalization occurs. After explicit shape touch only, summarize other saved Folder siblings under the same Collection; show `Other folders in this collection use <shape> tiles.` only for one exact supported opposite-shape consensus, and fail closed for none, mixed, or unsupported state. All effects stay in one draft until one touched-only Apply revision; Cancel is zero mutation and exact return-to-original remains a no-op. Guided creation, Hero/Logo/video/emoji, bulk/multi-Folder normalization, Collection rules, Worker, V1, and asset repositories remain unchanged. See [issue #142](https://github.com/davecollections/tmdb-id-lookup/issues/142) and [`ARTWORK_RUNTIME.md`](./ARTWORK_RUNTIME.md).

- **2026-08-22 — Exact-identity curated Folder artwork assistance, simplified after owner review:** derive transient Folder artwork identity only from the saved child-source contract, never from Folder title, approximate text, current image URL, or hostname. Require every child to be a supported native TMDB source and prove one exact Person, Company, Network, or official logical Genre; reuse the existing People/Studio/Network identity helpers, official Genre classifier, and Genre composite-placement rules, and fail closed for mixed IDs, duplicate/unsupported semantics, addon/opaque content, Franchises, Decades, Streaming, and empty folders. Reuse the schema-v2 People manifest for Poster/Landscape Tile, Hero, Title Logo, and optional shape-paired Focus; the published runtime for Company Landscape and Network Poster/Landscape; and the explicit official Genre Poster/Landscape mapping. Persist and export no provenance. The final visible contract is blank versus nonblank per field: a blank supported field with a published candidate receives compact **Curated artwork** / **Use curated artwork** UI; a blank supported field whose successfully read authority proves the asset missing receives **Request artwork**; every nonblank curated, TMDB fallback, imported, custom, arbitrary TMDB, or unknown URL receives no status, request, or replacement assistance. Choosing curated artwork changes only that draft field and hides its assistance; clearing it makes assistance eligible again. Focus acceptance does not toggle Focus, blank Tile availability follows the draft shape without automatic URL switching, Cancel mutates nothing, and Apply uses the existing one-revision touched-only patch while preserving raw/unknown data. Requests are real safe new-tab anchors to prefilled GitHub issues in `davecollections/nuvio-people-assets` or `davecollections/nuvio-assets`, both publicly Issues-enabled when verified; they use canonical identity, include deterministic maintenance paths where proven, carry no credential/private project data, call no GitHub API/backend, create nothing silently, and never assign unpublished URLs. The manifest/runtime remains publication authority. Keep guided creation-time automatic artwork unchanged. Any future automatic switch may replace only an exact positively recognized curated URL, never fallback/imported/custom artwork. Defer automatic shape switching, opposite-orientation substitution, missing-orientation and sibling notices, bulk editing, Collection Focus Glow/card artwork, Source Edit imagery cleanup, new families, Worker, V1, asset-repository changes, and exported metadata. See [issue #140](https://github.com/davecollections/tmdb-id-lookup/issues/140) and [`ARTWORK_RUNTIME.md`](./ARTWORK_RUNTIME.md).

- **2026-08-22 — Collection Modern View backdrop setting and owner-observed fallback semantics:** make existing Collection `backdropImageUrl` optional and first-class in the preservation-first importer, serializer, and Collection editor without assigning a default to new or existing Collections. Exact imported strings, blanks, absence, unusual values, unknown Collection fields, and no-op/unrelated edits retain the existing raw-overlay guarantees until this field is deliberately replaced or cleared. Group Collection Settings as Basic details, Display, and a small Artwork section labelled **Backdrop Image or GIF URL** with the conservative helper **Used as fallback folder artwork in Modern View.** Reuse the #136 exact-draft image/GIF preview contract: host-agnostic native image load, no referrer, lazy/async/decorative rendering, no URL normalization or proxy, nonblocking **Preview unavailable**, and exact-URL-scoped A → B → A retry behavior. Collection workspace cards remain text-only; Focus Glow remains hidden and behaviorally unchanged. **Manually confirmed in Nuvio — owner-observed current-client behavior, 2026-08-22; exact client build not captured:** in Modern View, missing Folder cover and hero/background both fell back to the Collection backdrop; when only a Folder hero was set, the Collection backdrop supplied the cover and the Folder hero supplied the background; explicit Folder cover and hero each took precedence for their respective surfaces. Folder video remained separate: a static Folder hero appeared first and the video later played while focused, without establishing guaranteed timing or a Collection override. No visible effect was observed in Grid or Classic View. Treat these examples as bounded current-client observations, not a universal timeless contract. Curated suggestions, automatic assignment, Folder inference or shape switching, bulk editing, Source imagery cleanup, Worker, V1, dependency, deployment, and asset work remain deferred. See [issue #138](https://github.com/davecollections/tmdb-id-lookup/issues/138).

- **2026-08-22 — Complete Folder visual-field settings and exact draft previews:** extend the existing preservation-first Folder editor, grouping the ordinary visible controls as Tile, Hero / Background, Branding, and Focus without adding schema. Known arbitrary-string `coverEmoji` remains model-supported, creation-flow-compatible, and exactly preserved, but owner desktop/physical-phone review removed it from the visible Settings UI to reduce cumulative density. Treat `heroVideoUrl` as compatibility-only rather than an ordinary Folder option: absent, blank, and unusual unsupported opening values show no Backdrop Video URL, Preview, Add action, or default; only a supported nonblank opening value exposes inspect/preview/replace/clear UI, and that visibility is fixed for the session. Clear then Apply/reopen hides it, clear then Cancel/reopen restores it, and replace A → B keeps it visible while resetting Preview and saving B. Preview only each visible URL field's current local draft string: Poster/Landscape-or-neutral Tile and Focus, wide Backdrop, contained Logo, and, for compatible existing video, explicit native preview with controls, inline playback, metadata preload, and no autoplay. Opening settings creates no video element or request. Image/GIF loads remain lazy, async, decorative, no-referrer, and host-agnostic; failures show nonblocking **Preview unavailable**, retain the input and project, and reset per exact URL so A → B → A retries A. Concise per-field helpers, restrained mobile wide previews, and an integrated compact focus switch reduce vertical weight without changing touch targets or behavior. Cancel mutates nothing; Apply uses the existing touched-only patch and one revision. Do no runtime lookup, TMDB/title/source inference, host allowlisting, URL rewrite, proxy, download, orientation detection, sibling warning, curated suggestion, Collection backdrop, focus-glow, bulk, Worker, V1, or asset work. Owner observation, not a Builder timing or playback contract: in current Nuvio Modern View, a static backdrop appeared first, a working MP4 began after sustained focus, leaving and returning restarted that sequence, and the tested GIF did not work. Owner desktop and physical-phone review approved this contract for the final issue #136 commit.

- **2026-08-22 — Assigned Folder artwork workspace contract:** display every exact nonblank string `coverImageUrl` already assigned to a Folder as a compact decorative card thumbnail, regardless of curated-runtime, TMDB, imported, or custom origin. Use Poster/Landscape proportions and a neutral contained fallback for unsupported shapes without rewriting them. Keep blank, absent, null, non-string, and load-failed images on the original text-only card; image failure is presentation-only and preserves URL, raw data, revision, and serialization. Request the native image without disclosing the Builder page as a referrer so referrer-sensitive arbitrary origins are not rejected, without host allowlists or URL rewriting. Scope failure state to the exact attempted URL and reset it when the assignment changes so an old failure cannot poison a replacement or later reassignment. Use native lazy loading and async decoding, make the image non-draggable, preserve the card's existing selection/accessibility/reorder semantics, and perform no resolver, manifest, TMDB, migration, or replacement request. At the #134 checkpoint, Folder Settings current-URL previews and exact-authority opt-in suggestions were deferred; the later #136 decision above implements only exact draft previews and keeps suggestions deferred. See [issue #134](https://github.com/davecollections/tmdb-id-lookup/issues/134) and [`ARTWORK_RUNTIME.md`](./ARTWORK_RUNTIME.md).

- **2026-08-21 — People entry and browse-first cleanup:** remove the temporary Folders-header People shortcut and its dedicated session path. Keep `New Collection → People` and `New Folder → People` as the hierarchy routes, keep `Selected Folder → Add Source → People` as the physical-source route, focus the stable People browse heading for guided hierarchy, and retain typing-first Search focus for direct Add Source.

- **2026-08-20 — Genre hierarchy owner-review contract, superseded in part before commit:** expose Genres through both shared creation scopes while keeping selected-folder Add Source and Source Edit unchanged; reuse the complete local Genre catalogue, ordered uncapped selection, current media/sort/Advanced semantics, full physical Discover identity, logical collection placement, shared title/layout controls, and existing atomic batches. Create exactly one Genre folder containing contextual `Movies` / `Series` sources. Default New Collection to `Genres`, Tabs, Show All on, Pin off, Landscape, and Hide on home screen only; keep New Folder parent presentation byte-identical and never clean up Untitled. Omit complete/partial destination sets without override, keep configured variants and elsewhere-only matches addable, rebuild placement before apply, and roll back stale/ID/late failures. The original fixed-Landscape/no-Preview/no-Worker clauses are superseded by the correction below.
- **2026-08-20 — Genre hierarchy owner-review correction before commit:** preserve Select; make Configure a deliberate shared Media/Sort surface with existing Advanced and always-visible logical rows, removing its duplicate disclosures; add exact-source-draft lazy poster Preview whose full canonical query/cache includes every current Advanced field and per-Genre exclusion; add Landscape-default/Poster from explicit official `wide`/`vertical` mappings without cross-orientation fallback or Add Source change; and narrowly admit only that single-Genre query through the tracked fail-closed Worker. Codex supplies complete reviewed Worker bytes, SHA-256, branch/HEAD, and deterministic evidence, then stops for Dave's manual deployment and `Worker deployed` reply before real production Worker/TMDB/CDN validation. No generic Discover, automatic count, service-token expansion, commit, push, PR, merge, or deployment is authorized by this correction.
- **2026-08-21 — Genre hierarchy Worker/live-validation gate:** record owner Worker version `857c1fa3-e62d-4fd8-9321-9573aedb1906` against the supplied source-byte SHA-256 and uncommitted branch/HEAD context; retain generic Discover rejection; activate a repeatable mounted Genre Preview scenario over the production Worker, real TMDB, and real image CDN; prove Movie-first/TV-lazy shared media, an exact Advanced/exclusion Movie query, responsive 10/5 poster bounds, success-cache reuse, focus restoration, and zero project mutation; leave owner acceptance and repository publication separately gated.
- **2026-08-21 — Genre hierarchy second owner correction before commit:** restore Media and Sort to the shared compact pill presentation with fixed unavailable-media notes; make Structure a first-class stage between Configure and Appearance; support Genre folders, fixed visible-title Movie/Series Media folders, separate media-specific Genre folders, and New-Collection-only separate Movie/Series collections with plan-derived counts and atomic application. Under Genre folders only, expose standalone/merged-target/Add-to-both placement for the three evidenced composite-TV concepts, keep merged titles self-describing, and make duplicate/omission behavior structure-specific. Make exactly one lowercase `include_adult=false` mandatory in every Genre Preview request, full cache identity, and Worker Genre validation without widening other branches. Treat the older deployed Worker's 403 as the expected second deployment gate, never a reason to synthesize live evidence; supply complete corrected bytes, hash, size, branch/HEAD, deterministic results, and delta, then wait for `Worker deployed` before postdeployment live validation and preview handoff.
- **2026-08-21 — Genre hierarchy third owner correction before commit:** preserve the approved Structure contract and replace its text-heavy option cards with the established visual-choice-card language: concise labels, illustrative nested Collection → Folder → Source wireframes, live plan-derived counts, native radio semantics, and clear selected/focus treatment. Keep composite controls below and conditional, keep the illustration presentation-only, and change no hierarchy behavior or Worker contract.
- **2026-08-21 — Genre hierarchy fourth owner correction before commit:** preserve the approved diagrams and all Structure/composite logic while removing the redundant visible grouping legend, replacing technical card/helper copy with the approved first-time-user wording, limiting visible card totals to plan-derived Collection/Folder counts, and renaming only the standalone action label to **Keep its own folder**. Source totals remain authoritative in planning and later summaries; the exact three combined-Series mappings and `standalone`/target/`both` values are unchanged; no Worker change is required.
- **2026-08-21 — Genre hierarchy final owner acceptance before commit:** correct only the shared Structure-card content distribution so wrapped titles no longer leave an inconsistent empty area above the diagram; preserve diagrams, counts, dimensions, copy, selected treatment, and all hierarchy behavior. Record responsive 12-pixel description-to-diagram spacing and aligned counts at 360, 393, 412, 899, 900, 901, and 1280 pixels, final desktop/physical-phone acceptance, and the still-separate explicit commit/PR/merge gates.

- **2026-08-20 — Network hierarchy final merged contract:** expose Networks through both shared hierarchy scopes without changing selected-folder Add Source or Source Edit; reuse ordered uncapped catalogue selection with the hierarchy-only Series-count filter; keep Discover explicit through one poster-only Series Preview with transient count and shared requester/cache mechanics behind a thin family adapter; support only the four evidenced TV sorts; resolve batch-safe Poster/Landscape artwork before planning; omit destination logical matches; apply one stale-revalidated atomic plan; keep mobile at five usable posters for issue #126 while deferring a possible 10-poster/two-row layout to whole-Builder review; retain owner-only Worker deployment and production-path live evidence; distinguish deployment handoff hash, Git blob, and CRLF working bytes; record automatic Pages publication separately from Worker deployment; and claim no current-client hierarchy result while retaining the known client resolver divergence.

- **2026-08-19 — Studio hierarchy owner-reviewed implementation:** expose Studios through both shared hierarchy scopes without changing selected-folder Add Source; preserve catalogue Search wording while adding the hierarchy-only All/Exclude 0/10+/50+/100+/500+ checked-in filter; use ordered uncapped checkbox selection and one shared Movies/Series/Both plus sort configuration; put selected Studio identity/count/placement/Preview/remove rows in Configure and keep final Appearance presentation-only; require explicit lazy media Preview with one count/result response and sort-aware bounded caching; require applicable Movie/Series Preview views to remain separate, including request-free People tab switching over existing combined credits; retain successful Series totals transiently by Company/media only; keep planning, stale validation, and atomic apply network-free; omit logical destination partial/complete matches; fix generated folders to Landscape with Company runtime → checked-in logo → emoji; manually deploy and live validate the narrow Company `sort_by` Worker allowlist only after separate owner authorization while leaving overall V2 release status to its separate boundary; and claim no new Nuvio-client result.

- **2026-08-19 — Franchise owner-review artwork, nested preview, and shared Rows correction:** make Franchise folder tiles fixed `POSTER`, map only the Collection poster to `coverImageUrl`, use emoji when absent, and do not use the backdrop as a landscape cover or invent a Hero mapping. Remove the Franchise shape selector and retain later individual customisation in Edit Folder. Reuse one named nested-modal layer for People and Franchise previews above creation. Use one shared hierarchy Layout control in Franchises, People, and Decades: Tabs shows a normal Show All switch; Rows hides it and produces `showAllTab: true`; Rows → Tabs restores it enabled. Keep New Folder parent presentation byte-identical.

- **2026-08-18 — Franchise hierarchy owner-review contract:** use exact TMDB Collection ID and preserve the canonical collection name without removing a leading `The` or semantic Collection/Saga/Trilogy wording. Generate exactly one ordinary folder and one existing native movie-franchise source per selected collection in selection order. Keep Add Source separate; use no Configure stage or unsupported sort. Default to Poster from the durable product matrix and use V1-evidenced poster/backdrop safe fallback at bounded `w500`, with no per-item artwork or focus controls. Reuse the shared title/layout/folder presentation controls, leave a New Folder parent byte-identical, omit exact destination duplicates, keep elsewhere matches addable, and apply one revalidated atomic plan. Keep implementation uncommitted until owner UI review, and make no V1, Worker, dependency, production, deployment, or new client-evidence change.

- **2026-08-17 — Final #118 People/Decades owner-review correction:** preserve the accessible native People result checkbox and full-card click target while replacing only its visible square treatment with a circular empty/filled-tick indicator. Make the label the visually hidden checkbox's positioning container so a partially clipped card retains correct inner-scroll coordinates and native focus cannot reposition the fixed dialog; do not add focus handlers, scroll resets, or keyboard interception. Add generated People-folder title visibility by reusing the existing Show everywhere / Hide on Home screen only / Hide everywhere semantics, default People to Hide on Home screen only, and carry the selected value through state, plan, stale revalidation, Back/Review, and atomic apply. Reuse one shared Title options composition in People and Decades, place it immediately before direct Layout, and keep New Folder collection presentation unavailable and byte-identical. Treat the Decades change as presentation-only: no default, plan, JSON, duplicate, or atomic-apply change.
- **2026-08-16 — People hierarchy/authority migration owner-review contract:** make People the second shared hierarchy family while preserving selected-folder Add Source; route the temporary Folders-header shortcut into the same New Folder flow; remove the arbitrary 20-person ceiling and keep large configuration bounded; create one canonical person folder with established role/media sources and no nested role/media hierarchy; use one compact Search summary; expose only Automatic/Same for all; retain direct per-person edits as internal overrides while shared changes preserve them; and show four direct pills in every Configure row. Apply one shared verified Popular/Recent/Top-rated sort, withholding Most votes because no retained native Person/Director contract establishes it. Offer a bounded body-portalled, focus-contained poster-only preview derived from existing combined credits. Keep sort outside source identity and plan/apply contracts unchanged. Review stays flat and exposes only one shared generated-folder Poster/Landscape choice with canonical-default/later-Edit-Folder guidance; New Folder keeps the captured parent byte-identical. Block complete/partial logical-person duplication in the destination but keep elsewhere informational; capture and revalidate a family-specific ephemeral plan before existing atomic controller operations. Load/index the canonical public People manifest once, keep numeric TMDB ID permanent, use manifest name/membership/assets when registered, retain TMDB search/fallback when absent, map Poster/Landscape/Hero/TitleLogo and optional complete static focus pair only to evidenced existing fields, retain SHA values transiently, and never fall back to old People URLs. Do not expose per-person artwork overrides, URL inputs, focus toggles, or reset actions during hierarchy creation; keep the extracted field group available for later ordinary manual Folder editing. The Builder persisted field is format-agnostic and preserves static WebP in `focusGifUrl`; defer any current-client render claim. Keep Company/Network on `nuvio-assets`; defer V1 and legacy deletion; make no asset/Worker/deployment/live-Nuvio changes.
- **2026-08-16 — Final #113 Configure-Genres consistency correction:** move Decades Genre inclusion off its older horizontal context strip and onto the same extracted responsive context/catalogue shell already used by the accepted Genre flow and Decades ordinary exclusions. Preserve the official inclusion catalogue, compatibility labels, All-selected/per-Decade semantics, required-Genre validation, source counts, planning, identity, ordering, and atomic apply while sharing desktop panes, mobile root/detail history, Back/Done focus, safe areas, scroll ownership, and breakpoints.
- **2026-08-16 — Final #113 Genre-exclusions responsive correction:** replace the Decades ordinary-exclusion horizontal context strip with the established shared Genre context/catalogue shell. Desktop uses one bounded All-selected-plus-each-Decade pane beside the unchanged toolbar/catalogue; mobile uses the accepted context root → catalogue detail → Back pattern with root-only Done, safe-area padding, heading focus, trigger restoration, and at most one active scroll owner. Preserve the borderless viewport/backdrop plus bounded bordered panel, all shared/per-Decade exclusion state, official catalogue compatibility/order, planner identity, counts, sources, and atomic apply.
- **2026-08-16 — Visible Decades flow and shared creation entry seam, refined after owner desktop/physical-phone review:** retain every existing New Collection/New Folder control but route it through one contextual ordered registry with Blank first and Decades second. Blank calls the unchanged draft helpers immediately. Decades owns Presets → content-only Configure Decades → count-led Review & Appearance while the #112 plan remains authoritative for construction, counts, duplicates, stale detection, and atomic apply. Configure reuses removable selections, compact Media/Sort, Genre catalogue/subviews, visible ordering previews, and bounded Advanced controls; visible defaults are Both media, Individual years on, Decade overview and Genre breakdown off, Separate for New Collection, and Newest Decades/Oldest Years. Expose exactly three Display-order presets mapped to the two retained planner chronology fields and retain Source grouping independently only for mixed physical folders. Review uses one shared accessible status for all blank/disabled hidden collection-title inputs while preserving each visible draft and U+200E output; its presentation sequence is shared Title options, direct Layout, Folder options, then View folder details. Keep Decade overview independent from All tab and explain the simultaneous state quietly. New Folder keeps captured parent presentation read-only and has no collection-title edit. The recomposition changes no defaults, plan values, JSON, duplicate behavior, or atomic application. Future artwork extends these Review option sections only under a separate issue; #113 adds no artwork or placeholders. Keep source identity/naming, canonical Decade editing, V1, Worker, export, assets, dependencies, live requests, and client acceptance outside this refinement.
- **2026-08-13 — Official Genre Builder boundary, corrected after the second owner UI review:** project the 35 official Movie/TV CSV references into 27 exact-name concepts and pair only the eight identical names; exclude Musicals, curated Lists, and V1 semantic merges; expose Genres only through selected-folder Add Source; accept all 27 concepts and expand to 35 ordered sources without an artificial 20 limit; place current-folder (default) versus sibling-folder-per-Genre destination inside Configure & review; preserve existing-folder presentation; use the published V1 wide mapping/fallback only for generated Genre folders; retain full Core identity with destination-specific duplicate scopes and atomic controller batches; compact large selection/review UI; and share styled responsive Advanced exclusion/help subviews across Add/Edit. Keep live Genre catalogue/count/artwork requests, Worker/Core/V1/assets/dependency changes, generic Advanced Discover, public Lists, generic frameworks, and claimed owner/client acceptance outside issue #110.
- **2026-08-12 — DISCOVER Core 18-field supersession:** retain issue #100 as the original 14-field milestone, then recognize the four current nullable-string exclusions through the shared known-field/descriptors architecture. Validate canonical exclusions with the existing strict ID-expression rules but do not normalize their comparison ordering, duplicates, separators, leading zeros, oversized numeric strings, whitespace, or malformed mixtures. Activate comparison-only Watch Region for included or excluded provider filtering, default active absence/blank to effective `US`, never persist that inference, and keep Streaming creation/editing fail-closed without exclusion UI.
- **2026-08-11 — Streaming Services final Region-first completion:** load the official Region/Movie-provider/TV-provider responses through the deployed exact thin Worker routes and retain successful data only in workspace memory; begin with natural ordered known-Region selection, curated live-resolved Common default, complete A–Z/search, and an explicit count/Next; derive only providers with common media across the selected regions; use actual regional priority for a one-region Top 30 and honest A–Z/search-only browsing for multiple regions; discard unused global priority; keep Provider single-select and strict common media; build Movie-before-TV candidates solely through DISCOVER Core while allowing independent valid physical-source names; review generated/duplicate outcomes inside Configure and insert the remaining ordered set through one atomic controller batch; expose the existing physical Source Edit only for simple Streaming-shaped DISCOVER nodes and only for name/sort changes. Keep exclusions, persistence, multi-provider semantics, recipes, and broader Help separate.
- **2026-08-09 — Internal DISCOVER Core pivot:** after Networks, establish one thin, evolvable, preservation-safe internal DISCOVER contract before user-facing consumers. Use the dependency-aware direction Networks complete → DISCOVER Core → Streaming Services → Genres → Decades → reassess source-led creation → Advanced Discover. This supersedes the earlier automatic Genres-next wording but is not a permanently frozen release schedule. Keep importer/serializer preservation authoritative, include sort in DISCOVER identity, exclude title, and introduce no user-facing workflow in the Core issue.
- **2026-08-06 — Global Company and Network count pre-cache retirement:** retire all not-yet-activated global typed target/progress/patch/completion/recovery/sidecar machinery and the Company Series collector. Keep Company days 1–14 and Network days 1–2 ordinary catalogue refreshes, legacy V1 totals, audit/repair, request budgeting, retries, and host safeguards. Use cached catalogues for future V2 discovery, then fetch fresh Company/Network counts only after exact selection; counts are informational and non-blocking. Treat issues #71/#73 and their removed documents as historical architecture evidence, not active instructions.
- **2026-08-07 — Refined Studio sources:** call the user-facing mode Studios while retaining internal `COMPANY`; adapt V1 Company metadata into deterministic search and paged browse; share clear/link conventions; contain logos with text-only fallback; fetch Movie/Series counts only after selection with bounded success caching and exact Worker allowlisting; retain the resolver/fixture/round-trip evidence for `COMPANY/MOVIE` and `COMPANY/TV`; map four semantic sort cards to correct media values; permit only display-name/sort physical edits; and add safe desktop double-click entry to existing editors. Keep the roadmap order Studios → Networks → Genres → Advanced Discover.
- **2026-08-08 — First Network Series source:** use the ordinary cached Network catalogue for 20-row A–Z browse and hidden-relevance discovery, but discard legacy `t` completely; fetch one informational Series Count only after exact selection; allowlist only canonical TV `with_networks` beside the existing Company TV route; construct one native projection-free `NETWORK/TV` source; bind duplicate override to folder plus identity; support display-name/sort physical edits through the registry; and keep Worker deployment as a separate explicitly authorized acceptance action, completed on 2026-08-09. Catalogue generation, production data, other source types, and recipes remain outside issue #98. Genre is the next source-creation milestone.
- **2026-08-06 — TMDB attribution release gate:** make V1 attribution compliant with the official TMDB mark and exact notice; record the same visible attribution as mandatory V2 shell work before `noindex` removal, release, or public promotion; and keep V2 UI implementation outside issue #85.

- **2026-07-10 — Planning checkpoint:** treat `sources` as authoritative, use `catalogSources` only as the addon compatibility projection/fallback, preserve unknown imported data, and gate the isolated React/Vite candidate behind contract and deployment proof.
- **2026-07-10 — Contract baseline:** add evidence-classified fixtures and stable invariant checks before any builder framework, production parser, serializer, or exporter work.
- **2026-07-10 — Deployment coexistence:** completed branch validation and recorded live GitHub Pages checks confirmed stable v1 at the project root and the isolated React/Vite builder under `/builder/`, including generated assets, the v1 backlink, mobile-width behaviour, and the unlinked `noindex, nofollow` state. Adopt isolated React/Vite under `/builder/` as the confirmed direction while leaving history-based direct subroutes unsupported.
- **2026-07-10 — Builder domain model:** adopt a framework-independent plain-data hierarchy with stable builder-only IDs, explicit source categories, authoritative editable `sources`, detached raw import snapshots, and small immutable operations before importer or serializer work.
- **2026-07-11 — Preservation-first importer:** accept JSON text and parsed collection arrays through an atomic importer; hydrate only recognised editable fields, classify from explicit providers, preserve unknown and unsupported sources as opaque, retain complete detached raw snapshots, and detect `catalogSources`-only compatibility data without migration or export assumptions.
- **2026-07-11 — Preservation-first serializer:** serialize compact new nodes and preservation-based imported nodes atomically; replace raw child arrays from current domain state; generate projections only for addon-category sources; preserve matched projection metadata; warn when unmatched projections are removed; and block unresolved legacy projection-only data without migration. Explicit property deletion, Ultra MAX conversion, language support, and visible UI integration remain deferred.
- **2026-07-11 — Evidence-based addon projection migration:** add an explicit atomic migration for the confirmed addon projection-only shape; create compact authoritative addon sources in projection order; normalize only exact `"None"` to `null`; preserve raw projection evidence for serializer overlay; and use a narrow no-genre identity alias for matching. Keep import automaticity, manifests, networking, UI, Ultra MAX conversion, and AIO Metadata runtime integration deferred.
- **2026-07-11 — Reproducible manual migration round trip:** generate the sanitised checkpoint through production importer, explicit migration, and serializer APIs; preserve the untouched Nuvio Desktop export; verify exact hashes and additive-only normalization; and adopt manual compatibility evidence for this generated collection shape while keeping live addon resolution, playback, automatic migration, and broader runtime conversions outside scope.
- **2026-07-11 — Pages publication boundary recovery:** after the controlled issue #38 merge exposed repository-only manual evidence through broad tracked-file staging, revert production, recover the approved evidence unchanged, and replace default publication with one shared explicit v1-plus-compiled-builder allowlist enforced by preparation, exhaustive validation, and regression tests. Keep the evidence public in source control but absent from the deployed website, with no runtime or workflow change.
- **2026-07-11 — Application controller boundary:** place immutable current-project ownership, subscriptions, hierarchical selection, dirty-state replacement protection, explicit migration preview/application, coordinated domain edits, diagnostic scopes, and serialization calls under `builder/src/application/`. Keep domain, parsing, classification, migration eligibility/transformation, raw preservation, projection generation, and export validation in their existing production modules. Leave visible React integration, browser file handling, persistence, and undo/redo for later issues.
- **2026-07-11 — First visible builder shell:** connect React to the controller through one external-store adapter and one production controller singleton; derive mobile drill-down directly from hierarchical selection; display only safe known summaries; and permit deterministic draft collection/folder creation without starting editing, import/export, source creation, persistence, or migration workflows.
- **2026-07-12 — Welcome and local JSON import:** make TMDB Collection Builder the visible product name; keep welcome/workspace selection UI-only above one subscribed controller; delegate new-project and JSON parsing/import to public controller APIs; keep browser file transport local, explicit, size-limited, and structured; and preserve the existing workspace without adding export, editing, persistence, migration actions, or networking.
- **2026-07-12 — Essential hierarchy editing:** add one UI-only inline draft for collection/folder Nuvio-facing IDs and titles; target controller nodes only by stable internal ID; validate required text locally; send only minimal changed fields through `updateNode`; lock hierarchy navigation until Apply or Cancel; and preserve raw/unknown/source evidence without changing controller, domain, importer, serializer, migration, v1, Worker, or dependency contracts.
- **2026-07-21 — Shared artwork runtime foundation:** adopt one pure root-level ES module for typed published artwork validation, loading, and resolution; resolve only explicit company, network, and person maps; version every ready asset URL from its orientation SHA; keep successful caching memory-only per client; and defer v1 export, builder UI, persistent caching, refresh policy, and artwork-request integration to separately reviewed issues.
- **2026-07-21 — V1 company/network runtime migration:** route classic company and network export artwork through one thin module adapter and the shared runtime; prepare and cache exports asynchronously; accept published text fallbacks; use visible title/emoji for missing or disabled artwork; block and retry runtime failures; remove borrowed focus animations; and leave people plus v2 consumption deferred.
- **2026-07-23 — Discover compatibility boundary:** treat official TMDB acceptance, client deserialization, request construction, hidden transformations, persistence, direct result effects, and visible device effects as separate evidence levels; retain the 14-field production contract unchanged; use the generated row-level matrix as the normalized research source; and defer all UI/schema expansion until bounded owner evidence and a separately approved issue.
- **2026-07-23 — Discover owner-evidence checkpoint:** retain the official counts, 14-field contract, and static mappings; record the complete Desktop alpha and retained official iOS runs as build-specific observations; add a deterministic one-source-per-folder presentation without changing source identity or aggregate counts; keep NuvioTV/direct TMDB pending; and attribute account-export changes only to the observed pipeline until its responsible layer is isolated.
- **2026-07-24 — Profile-level compatibility corpus:** connect existing focused builder contracts through a small manifest-driven fixture taxonomy; use exact equality only for intentional canonical output, semantic cycle stability for preservation profiles, and targeted assertions for identity, removal, migration, and invalid boundaries; reuse the existing Discover and addon-migration artifacts; and retain a strict test-only production boundary.
- **2026-07-24 — Product and workflow history recovery:** review the complete owner-supplied V1 and V2 conversation exports to recover explicit product and process intent; reconcile it through current implementation, tests, manual evidence, and GitHub history; preserve unaccepted proposals as proposals or open decisions; and keep the private exports and conversational material outside the repository.
- **2026-07-25 — Official Nuvio documentation review:** recognise the documented authenticated, profile-scoped, full-replace collection transport while keeping optional connection design deferred; distinguish collection sources, Stremio-style addon manifests, and Nuvio plugin-repository manifests; keep plugin repositories outside core Builder scope; and leave device pairing, safe merge/update semantics, token handling, and recovery unverified.
- **2026-07-25 — Collection and folder presentation settings:** after owner visual review superseded the original inline-editor direction, place one compact Edit action on every collection and folder card and remove the Rename/Settings pair, then-current quick rename, and large selected-entity blocks; directly target unselected cards without dirtying or advancing revision; use one responsive, focus-contained modal over an inert workspace; describe Tabs/Rows as each folder's source presentation and limit the visible All tab to tabbed folders with two or more sources; allow deliberate U+200E creation for both collection titles and folder titles while preserving imported repeated values; combine the folder's title and native home-screen-only `hideTitle` behavior into Show everywhere, Hide on home screen only, and Hide everywhere outcomes with restoration and minimal-patch guarantees; default manual folders to `hideTitle: true`; retain conditional All-tab preference, Pin to top, Poster/Landscape, Follow Layout/Square preservation, and touched-only `updateNode` patches; recognise collection-level `focusGlowEnabled` with a true manual default and preservation-first imported behavior; retain desktop creation drill-down while mobile collection/folder creation stays at its current hierarchy level through the shared 900px UI breakpoint; simplify startup wording around collections without changing import contracts; defer artwork/focus-GIF controls, bulk settings, and reordering; recommend a separate reordering milestone before Search/Add; and require focused owner review before PR consideration. Issue #63 later supersedes only the action placement and introduces a smaller mobile context rename variant.
- **2026-07-26 — Strict artwork runtime v1/v2 compatibility:** accept exactly numeric shared runtime schemas 1 and 2; preserve v1 results and URL versioning; require exact entity, ID, and orientation paths; reject forbidden Company Poster and v1 Network Poster data; require both Network orientations in v2; keep the classic adapter and cached exporter Landscape-only; and leave the live schema-v1 runtime, Builder controls, assets, and publication process unchanged.
- **2026-07-26 — Live artwork runtime v2 publication:** adopt the completed assets PR #3 publication as the current public baseline; record its release identity, counts, hashes and fingerprints as evidence rather than consumer rules; keep Company Poster unsupported and the external v1 bridge Landscape-only; and leave Builder Network Poster consumption, UI and source creation for a separate issue.
- **2026-07-26 — Accessible hierarchy reordering:** add one compact six-dot handle to existing collection, folder, and source rows for pointer/touch dragging plus Enter/Space and Arrow-key reordering; keep pinned and ordinary collections as stable visible groups and map final visible destinations back to authoritative raw-array indices; preserve parent/category boundaries, stable internal identity, selection, raw/unknown/artwork/projection evidence, and serialized order; retain exactly one revision for a changed drop or keyboard move and none for hover, cancellation, invalid boundaries, or same-position movement; keep focus on the moved handle and announce successful moves politely; remove the redundant Selection details surfaces; and defer Nuvio client evidence, bulk movement, Search/Add, artwork-runtime consumption, client packages, PR, and release work until the required review gates.
- **2026-07-27 — Reordering client-evidence checkpoint:** accept the bounded Desktop and web visual-plus-exact-export results, the mobile visual-plus-export-text result with no raw artifact or hash, and the TV visual-plus-synced-web result with no independent export; constrain observed normalization without treating sentinel loss as order loss; mark the issue #59 client gate complete; and retain the separate owner approval requirement before creating a PR or beginning Search/Add.
- **2026-07-28 — Persistent hierarchy creation and safe deletion:** retain the existing header and empty-state creation actions while adding secondary collection/folder Add rows below populated lists; replace the external Edit/Delete rail with one reusable in-card overflow menu; portal the measured menu below modal layers, place it within current Visual Viewport bounds with upward flipping and a 10px margin, and focus without automatic page scroll; add mobile selected-context quick rename with preservation-correct invisibility switches; confirm every source deletion and only populated/import-bearing collection/folder deletions; calculate visible-order selection, mobile level, and focus recovery before one existing `removeNode` mutation; rebuild addon projections from surviving authoritative sources; and keep source Add/Search, source creation/editing, bulk operations, undo/redo, broader presentation, artwork runtime, Network Poster, v1, Worker, Pages, workflow, dependency, and assets work deferred.
- **2026-07-29 — First Add Source slice and owner-acceptance checkpoint:** show Add Source only for a selected folder; expose only Movie franchise · TMDB through explicit Search and Review stages in one responsive, body-portalled, focus-contained dialog; accept title search or strict exact collection ID/URL input; resolve through only the existing collection Worker routes with `include_adult=false`, exclusion only for search result objects explicitly marked `adult: true`, provider-returned result admission/order, no custom word classifier or age guarantee, safe poster paths, ordered contained titles, Search-resident selected-result loading and visible failure recovery, abort, timeout, monotonic stale protection, conditional pagination, retry, rate-limit handling, and bounded success-only memory caching; derive and strictly validate the Worker HTTPS origin from the stable v1 configuration at Builder build time without executing it; keep poster/title-part data UI-only; construct and semantically validate exactly one native `COLLECTION`/`MOVIE` recipe with serialized `sortBy: "original"` and visible `TMDB-provided order`; bind Add anyway to the exact current duplicate identity and gate rapid submission synchronously; preserve Search state on Back; isolate opaque mobile layout-viewport and Visual Viewport coverage above Builder layers; establish body lock/geometry pre-paint; constrain the uncropped mobile poster to 180–220px; restore exact body state; perform one existing controller source insertion plus selection-only focus recovery; preserve raw data and addon projections; accept the completed owner physical-iPhone gate and successful current Nuvio Desktop import/runtime/immediate-export evidence; record `original` as TMDB API order, descending release date as working newest-first, and ascending release date as unsupported/falling back; and leave all later providers, source editing, Quick Add/multi-add, bulk lookup, suggestions, sort controls, automatic hierarchy/artwork controls, v1, Worker, Pages, dependency, workflow, and assets changes deferred.
- **2026-07-30 — Collection and Folder settings polish:** keep the issue #53 recognised fields, defaults, minimal-patch lifecycle, and export meanings; present `TABBED_GRID` as **Tabs (recommended)** beneath the approved Collection heading/helper; remove only the user-facing focus-glow control while retaining explicit-true manual creation and exact imported missing/true/false/unusual preservation; group Folder Title under **Basic details** and visibility/shape under **Display**; use compact native visibility radios and CSS-preview shape cards with non-colour selected state; add only a reusable semantic-section seam for future groups; and leave Artwork, source editing, provider expansion, recipes, V1, Worker, dependencies, Pages, and assets unchanged.
- **2026-07-31 — Historical typed entity title-count design, superseded by issue #90:** issue #71 established a global Company Movie/Series and Network Series pre-cache, progress, sidecar, publication, and request-budget contract. It was never consumed by Builder and is retained only as architecture history.
- **2026-08-06 — Historical typed writer recovery completion, superseded by issue #90:** issue #73 and PR #80 validated recovery safety before activation. The recovery machinery was later removed with its retired global pre-cache producer; its issue, PR, and Git history remain evidence rather than operating instructions.
- **2026-08-06 — Canonical Worker synchronization:** accept PR #82 as aligning the repository Worker with the already deployed narrow origin-free People service-token behavior, without expanding browser CORS, other routes, or recording secret values.
- **2026-08-01 — Unified People Search/Add final owner refinement:** promote only empty Builder-generated Untitled defaults, with canonical person title, final artwork, and sources in one rollback-safe revision; preserve every non-eligible destination; use stable v1 role/media source titles; show only final applied artwork for new/promoted folders; key artwork by exact person and context; record missing curated assets by exact identity; retain the first Desktop run as source-contract evidence; retain the subsequent regenerated-fixture owner visual/import/immediate-export acceptance as final artwork and tab evidence; defer generic multi-item creation and native source editing; and leave v1, Worker, Company/Network, Basic Discover, dependencies, production data, and assets unchanged.
- **2026-08-02 — Same-network owner preview transport:** RFC1918 HTTP preview origins route Builder TMDB requests through a reserved Vite development/preview proxy on the same PC. The proxy forwards only to the existing Worker, which retains its current method/path validation and receives an already-approved localhost development origin. GitHub Pages and localhost behavior remain direct, and no Worker route, production CORS allowlist, credential, dependency, or production-data change is involved.
- **2026-08-02 — People final Nuvio Desktop acceptance:** accept the regenerated issue #74 fixture as owner-validated current Desktop evidence for curated Tom Hanks/Steven Spielberg Posters, hidden external titles, distinct stable-v1 source tabs, populated Acting/Directing Movie/TV catalogues, canonical grouping/order, and immediate-export preservation. Retain the supplied expanded export as reproducible evidence; record the unreported client version/build as unknown rather than inferring it; and keep v1, Worker policy, dependencies, production data, and `nuvio-assets` unchanged.
- **2026-08-02 — People mobile search-card refinement:** retain every valid TMDB `known_for` entry in normalized provider order, render at most the first three on desktop, and use the existing 520px CSS breakpoint to show only the first row on mobile. Allow its title to wrap without ellipsis or horizontal overflow, retain supplied media/year metadata, omit the row when absent, and add no sorting, total inference, responsive data mutation, or request.
- **2026-08-02 — First native physical-source editing, owner-approved Round 2:** expose Edit source only for recognised Movie Collection and People sources; keep the editor registry and per-family draft/validation/patch logic framework-independent; bind one exact physical source and reject stale project, parent, order, category, adapter, or identity state; auto-apply a newly selected Collection's canonical name while preserving Cancel/custom/reset behavior; auto-manage only approved People default titles until manual customization; reuse the shared bounded successful People count cache/request without artwork or Save blocking; expose only stable-v1 Popular/Recent/Top-rated People sorts while preserving untouched imported values; render and focus prominent duplicate/validation/stale alerts; reject changed folder-local duplicates without an override; delegate a real minimal patch through one existing controller update and keep no-op/failure paths mutation-free; preserve raw/unknown IDs, ordering, projections, defaults, filters, presentation, and deterministic cycles; prepare but do not fabricate current-client evidence; and leave logical bundle editing, additional adapters, Collection sort/filter controls, v1, Worker, dependencies, workflows, production data, and `nuvio-assets` unchanged.
